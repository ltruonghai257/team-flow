# Phase 20 compatibility delegate — canonical path: app.core.config
# Review for removal in Phase 22 after runtime/Docker/Azure verification.
from app.core.config import Settings, settings  # noqa: F401
