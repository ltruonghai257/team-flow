---
phase: 26-navigation-information-architecture
plan: 02
type: execute
wave: 2
depends_on:
  - "26-01"
files_modified:
  - frontend/src/routes/+layout.svelte
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - NAV-05
user_setup: []
must_haves:
  truths:
    - "Desktop and mobile navigation render the same workflow groups and child links."
    - "Opening a child page highlights both the parent section and the specific child route."
    - "Parent rows expand and collapse without navigating, and the active parent auto-expands on page load."
    - "On mobile, choosing a child route closes the drawer after navigation."
  artifacts:
    - path: "frontend/src/routes/+layout.svelte"
      provides: "Grouped desktop sidebar and mobile drawer rendering with shared expansion state"
      min_lines: 80
      contains: "sidebarOpen"
  key_links:
    - from: "frontend/src/routes/+layout.svelte"
      to: "frontend/src/lib/navigation/sidebar.ts"
      via: "imported grouped navigation metadata and helpers"
      pattern: "sidebar"
    - from: "frontend/src/routes/+layout.svelte"
      to: "$page.url.pathname"
      via: "active parent and child detection"
      pattern: "pathname"
---

<objective>
Refactor the existing layout shell to use grouped navigation across desktop and mobile.

Purpose: Deliver the visible information-architecture change without disturbing route URLs, auth redirects, notification placement, or the admin sub-team switcher.
Output: A grouped sidebar/drawer implementation inside `+layout.svelte`.
</objective>

<execution_context>
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-navigation-information-architecture/26-CONTEXT.md
@.planning/phases/26-navigation-information-architecture/26-RESEARCH.md
@.planning/phases/26-navigation-information-architecture/26-01-SUMMARY.md
@frontend/src/routes/+layout.svelte
@frontend/src/lib/navigation/sidebar.ts
@frontend/src/lib/components/NotificationBell.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the flat sidebar render path with grouped sections</name>
  <files>frontend/src/routes/+layout.svelte</files>
  <read_first>frontend/src/routes/+layout.svelte, frontend/src/lib/navigation/sidebar.ts, .planning/phases/26-navigation-information-architecture/26-CONTEXT.md</read_first>
  <action>Refactor `+layout.svelte` to consume the grouped navigation helpers from Plan 01 instead of the inline `navItems` array. Implement D-04, D-05, D-07, D-08, D-09, and D-10 exactly: auto-expand the active parent, allow manual expansion/collapse of other parents for the current browser session only, render parent rows as non-link toggles with `ChevronDown` / `ChevronRight`, and keep the existing stronger child active styling (blue-tinted background/text plus right chevron) while adding a lighter active-parent signal. Preserve `NotificationBell`, the admin sub-team switcher, auth redirects, and logout shell behavior already present in the file.</action>
  <verify><automated>cd frontend && bun run check</automated></verify>
  <acceptance_criteria>
    - `+layout.svelte` no longer renders a flat `sidebarItems` loop
    - parent rows are buttons or equivalent non-navigating controls, not links
    - child active styling still includes the right chevron for the active child item
  </acceptance_criteria>
  <done>The desktop sidebar renders grouped parent/child navigation with distinct parent and child active states.</done>
</task>

<task type="auto">
  <name>Task 2: Keep mobile drawer behavior aligned with the grouped desktop nav</name>
  <files>frontend/src/routes/+layout.svelte</files>
  <read_first>frontend/src/routes/+layout.svelte, frontend/tests/mobile/sidebar.spec.ts</read_first>
  <action>Use the same grouped navigation structure for the mobile drawer (NAV-04). Implement D-06 by ensuring child-link navigation closes the drawer, while parent taps only expand/collapse. Keep `sidebarOpen` as the mobile shell state, preserve the existing overlay/backdrop pattern, and make sure the active parent group auto-expands when the drawer opens on nested routes. Do not move or duplicate the mobile top-bar `NotificationBell`.</action>
  <verify><automated>cd frontend && bun run build</automated></verify>
  <acceptance_criteria>
    - mobile nav and desktop nav are sourced from the same grouped metadata
    - child navigation still closes the drawer on mobile
    - no new routes or route renames appear in `+layout.svelte`
  </acceptance_criteria>
  <done>The mobile drawer matches the grouped desktop IA and preserves the current close-on-navigation behavior for child routes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && bun run check`
- [ ] `cd frontend && bun run build`
- [ ] `frontend/src/routes/+layout.svelte` renders grouped navigation from the shared metadata module
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Desktop and mobile use the same grouped nav data and parent/child active logic
- Route URLs, notification placement, and current auth redirect behavior remain intact
</success_criteria>

<output>
After completion, create `.planning/phases/26-navigation-information-architecture/26-02-SUMMARY.md`
</output>
