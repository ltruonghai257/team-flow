---
phase: 26-navigation-information-architecture
plan: 03
type: execute
wave: 3
depends_on:
  - "26-02"
files_modified:
  - frontend/tests/mobile/sidebar.spec.ts
  - frontend/tests/navigation-groups.spec.ts
autonomous: true
requirements:
  - NAV-02
  - NAV-03
  - NAV-04
  - NAV-05
user_setup: []
must_haves:
  truths:
    - "Automated regression coverage proves grouped navigation works on mobile and desktop without breaking route stability."
    - "Privileged-only navigation items remain hidden for non-supervisors while visible to allowed roles."
    - "The final grouped navigation UI is visually confirmed after automated checks pass."
  artifacts:
    - path: "frontend/tests/mobile/sidebar.spec.ts"
      provides: "Mobile grouped-navigation drawer regression coverage"
      min_lines: 30
    - path: "frontend/tests/navigation-groups.spec.ts"
      provides: "Desktop grouped-navigation and visibility regression coverage"
      min_lines: 40
  key_links:
    - from: "frontend/tests/mobile/sidebar.spec.ts"
      to: "frontend/src/routes/+layout.svelte"
      via: "drawer interactions and child-route navigation assertions"
      pattern: "Tasks|Open menu|Close menu"
    - from: "frontend/tests/navigation-groups.spec.ts"
      to: "frontend/src/routes/+layout.svelte"
      via: "desktop group expansion, active-state, and visibility assertions"
      pattern: "Performance|Timeline|AI Assistant"
---

<objective>
Lock in the grouped navigation behavior with regression coverage and a final UI check.

Purpose: Catch regressions in the new information architecture before Phase 27 builds on it.
Output: Updated mobile nav tests, a new desktop grouped-navigation spec, and final verification guidance for the grouped UI.
</objective>

<execution_context>
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-navigation-information-architecture/26-CONTEXT.md
@.planning/phases/26-navigation-information-architecture/26-RESEARCH.md
@.planning/phases/26-navigation-information-architecture/26-02-SUMMARY.md
@frontend/src/routes/+layout.svelte
@frontend/tests/mobile/sidebar.spec.ts
@frontend/tests/helpers/auth.ts
@frontend/playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend automated grouped-navigation regression coverage</name>
  <files>frontend/tests/mobile/sidebar.spec.ts, frontend/tests/navigation-groups.spec.ts</files>
  <read_first>frontend/tests/mobile/sidebar.spec.ts, frontend/tests/helpers/auth.ts, frontend/playwright.config.ts, frontend/src/routes/+layout.svelte</read_first>
  <action>Update `frontend/tests/mobile/sidebar.spec.ts` so it validates grouped navigation behavior instead of the old flat route list: parent expansion, child navigation closing the drawer, and active-group visibility on mobile. Add `frontend/tests/navigation-groups.spec.ts` for desktop assertions covering the exact Phase 26 grouping, active parent/child affordances, preserved URLs, and role-aware visibility for `Performance`. Reuse the existing auth helper and Playwright config rather than inventing a new test harness.</action>
  <verify><automated>cd frontend && bunx playwright test tests/mobile/sidebar.spec.ts tests/navigation-groups.spec.ts</automated></verify>
  <acceptance_criteria>
    - mobile sidebar tests assert grouped parent/child interactions rather than a flat nav list
    - `frontend/tests/navigation-groups.spec.ts` verifies desktop grouped nav behavior and privileged visibility
    - automated tests navigate existing URLs without requiring route changes
  </acceptance_criteria>
  <done>Playwright coverage exists for the grouped desktop/mobile navigation behaviors introduced in Phase 26.</done>
</task>

<task type="auto">
  <name>Task 2: Run release checks for the navigation refactor</name>
  <files>frontend/tests/mobile/sidebar.spec.ts, frontend/tests/navigation-groups.spec.ts</files>
  <read_first>frontend/package.json, frontend/playwright.config.ts</read_first>
  <action>Run the phase release checks from the frontend workspace: `bun run check`, `bun run build`, and the targeted Playwright specs from Task 1. Keep the verification narrow to Phase 26 surfaces; do not broaden into unrelated feature rewrites or flaky whole-suite changes.</action>
  <verify><automated>cd frontend && bun run check && bun run build && bunx playwright test tests/mobile/sidebar.spec.ts tests/navigation-groups.spec.ts</automated></verify>
  <acceptance_criteria>
    - `bun run check` succeeds
    - `bun run build` succeeds
    - the targeted Playwright specs pass
  </acceptance_criteria>
  <done>The grouped navigation implementation passes the phase's automated release checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && bun run check`
- [ ] `cd frontend && bun run build`
- [ ] `cd frontend && bunx playwright test tests/mobile/sidebar.spec.ts tests/navigation-groups.spec.ts`
- [ ] Run the frontend locally and visually confirm: the five parent groups use the exact Phase 26 labels, parent rows toggle without navigating, active parent and child states are both visible, mobile child taps close the drawer, and unauthorized users do not see Performance
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 26 behavior is covered by targeted desktop and mobile regression tests
- The plan tells the executor exactly what to verify visually after automated checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-navigation-information-architecture/26-03-SUMMARY.md`
</output>
