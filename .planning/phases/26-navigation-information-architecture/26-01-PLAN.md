---
phase: 26-navigation-information-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/navigation/sidebar.ts
  - frontend/src/lib/stores/auth.ts
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-04
  - NAV-05
user_setup: []
must_haves:
  truths:
    - "Workflow-based navigation groups exist as structured metadata rather than a flat inline route list."
    - "Navigation metadata can hide privileged child routes and drop empty parent groups before rendering."
    - "Nested route matching can identify the correct child item for URLs like /performance/[id] and /schedule/knowledge-sessions."
  artifacts:
    - path: "frontend/src/lib/navigation/sidebar.ts"
      provides: "Grouped navigation definitions, role-aware filtering helpers, and active-route matching helpers"
      min_lines: 60
      exports:
        - "navigationGroups"
        - "filterNavigationGroups"
        - "getActiveNavigationState"
    - path: "frontend/src/lib/stores/auth.ts"
      provides: "Reusable role typing or helpers consumed by grouped navigation metadata"
      contains: "role"
  key_links:
    - from: "frontend/src/lib/navigation/sidebar.ts"
      to: "frontend/src/lib/stores/auth.ts"
      via: "role-aware visibility predicates"
      pattern: "admin|supervisor|member"
---

<objective>
Define the grouped navigation source of truth before touching the UI shell.

Purpose: Keep workflow groups, role rules, and active-route logic explicit so the layout refactor stays small and Phase 29 has a future-ready place for deeper visibility metadata.
Output: A reusable grouped navigation module plus any minimal auth-store typing exports it needs.
</objective>

<execution_context>
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-navigation-information-architecture/26-CONTEXT.md
@.planning/phases/26-navigation-information-architecture/26-RESEARCH.md
@frontend/src/routes/+layout.svelte
@frontend/src/lib/stores/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create grouped navigation metadata module</name>
  <files>frontend/src/lib/navigation/sidebar.ts</files>
  <read_first>frontend/src/routes/+layout.svelte, .planning/phases/26-navigation-information-architecture/26-CONTEXT.md</read_first>
  <action>Create `frontend/src/lib/navigation/sidebar.ts` as the single source of truth for grouped navigation. Encode D-01 through D-03 exactly: parent groups `Dashboard`, `Work`, `Planning`, `Team`, and `AI`; child routes `/`, `/projects`, `/tasks`, `/milestones`, `/timeline`, `/schedule`, `/team`, `/updates`, `/board`, `/performance`, and `/ai`; and keep `Performance` under `Team`. Reuse existing `lucide-svelte` icons that already appear in `+layout.svelte`. Make the parent rows non-navigating containers by modeling parent groups separately from child links. Do not introduce new routes or rename any public paths (NAV-03).</action>
  <verify><automated>cd frontend && bun run check</automated></verify>
  <acceptance_criteria>
    - `frontend/src/lib/navigation/sidebar.ts` exports grouped parent/child metadata for the five workflow sections
    - `frontend/src/lib/navigation/sidebar.ts` contains `/performance` under the `Team` group, not as a top-level item
    - no child route path differs from the URLs listed in D-02
  </acceptance_criteria>
  <done>Grouped navigation data exists as a standalone module with the exact phase-approved labels and child routes.</done>
</task>

<task type="auto">
  <name>Task 2: Add visibility and active-route helpers</name>
  <files>frontend/src/lib/navigation/sidebar.ts, frontend/src/lib/stores/auth.ts</files>
  <read_first>frontend/src/lib/stores/auth.ts, frontend/src/routes/+layout.svelte, frontend/tests/mobile/sidebar.spec.ts</read_first>
  <action>Add helpers in `sidebar.ts` that implement D-11 through D-15: prefix-based child matching for nested URLs, role-aware child filtering that preserves current member vs supervisor/admin behavior, and removal of empty parent groups after filtering. Export only the minimal auth typing/helpers needed from `frontend/src/lib/stores/auth.ts`; keep the current auth store behavior intact. Preserve current-role behavior for `/performance` and `/admin` rather than inventing Phase 29 scope rules early.</action>
  <verify><automated>cd frontend && bun run check</automated></verify>
  <acceptance_criteria>
    - `sidebar.ts` exports a helper that can identify active children with prefix matching for non-root routes
    - `sidebar.ts` contains logic that removes hidden child items and omits parent groups with zero visible children
    - `auth.ts` keeps `admin` / `supervisor` / `member` semantics unchanged while exposing any typing/helper needed by `sidebar.ts`
  </acceptance_criteria>
  <done>The grouped navigation module can tell the layout which groups to show and which child item is active for nested paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && bun run check`
- [ ] `frontend/src/lib/navigation/sidebar.ts` exports grouped metadata plus filtering and active-route helpers
- [ ] Existing route URLs remain unchanged in the metadata
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Grouped navigation data reflects D-01 through D-03 exactly
- Visibility helpers preserve current privileged-route behavior while hiding empty groups and unauthorized children
</success_criteria>

<output>
After completion, create `.planning/phases/26-navigation-information-architecture/26-01-SUMMARY.md`
</output>
