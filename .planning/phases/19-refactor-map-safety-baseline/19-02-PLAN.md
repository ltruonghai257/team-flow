---
phase: "19-refactor-map-safety-baseline"
plan: 2
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - ".planning/phases/19-refactor-map-safety-baseline/19-BACKEND-MAP.md"
autonomous: true
user_setup: []
requirements: ["STRUCT-01", "STRUCT-03"]
must_haves:
  truths:
    - "D-01: Keep `backend/app` as the backend package root while reorganizing internal folders."
    - "D-02: Use Open WebUI as structural inspiration, not product vocabulary."
    - "D-04: Expose current coupling and target module candidates without forcing the full split depth up front."
    - "D-05: Temporary compatibility shims are allowed only when small, documented, and paired with removal notes."
    - "D-06: Preserve FastAPI startup, router registration, auth/session behavior, rate limiting, CORS, scheduler startup/shutdown, WebSocket routing, health checks, Alembic history, and test imports."
  artifacts:
    - path: ".planning/phases/19-refactor-map-safety-baseline/19-BACKEND-MAP.md"
      provides: "Backend target tree and old-to-new file map"
      contains: "## Backend Target Structure"
    - path: ".planning/phases/19-refactor-map-safety-baseline/19-BACKEND-MAP.md"
      provides: "Backend migration slices for Phase 20"
      contains: "## Migration Slices"
  key_links: []
---

# Phase 19 Plan 02: Backend Target Map

<objective>
Document the TeamFlow-native backend target structure, current-to-target file map, coupling notes, and Phase 20 migration slices without moving backend code.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-refactor-map-safety-baseline/19-CONTEXT.md
@.planning/phases/19-refactor-map-safety-baseline/19-RESEARCH.md
@.planning/phases/19-refactor-map-safety-baseline/19-VALIDATION.md
@.planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task id="19-02-01" type="execute">
  <name>Document backend target structure</name>
  <files>.planning/phases/19-refactor-map-safety-baseline/19-BACKEND-MAP.md</files>
  <read_first>backend/app, backend/alembic, backend/tests, .planning/phases/19-refactor-map-safety-baseline/19-RESEARCH.md</read_first>
  <action>
Create `.planning/phases/19-refactor-map-safety-baseline/19-BACKEND-MAP.md`.

Document an adapted Open WebUI-style target tree that keeps `backend/app` as root and includes:

- `main.py` app entrypoint
- `config.py` / `database.py` / `limiter.py`
- `routers/`
- domain model modules or `models/` candidates
- schema modules or `schemas/` candidates
- `services/`
- `utils/` candidates
- `websocket/` or `socket/` target naming decision
- `scripts/`
- Alembic files and test imports

Explicitly state which Open WebUI groups are not being copied because they do not fit TeamFlow.
  </action>
  <acceptance_criteria>
    - `19-BACKEND-MAP.md` contains `## Backend Target Structure`
    - It keeps `backend/app` as the package root
    - It covers routers, models/domain modules, schemas, config, migrations, utils, WebSocket, and app entrypoint
    - It names Open WebUI as inspiration only
  </acceptance_criteria>
  <verify>`rtk proxy rg "## Backend Target Structure|backend/app|routers|schemas|models|websocket|Alembic" .planning/phases/19-refactor-map-safety-baseline/19-BACKEND-MAP.md`</verify>
  <done>Backend target tree exists and covers every required backend structure category.</done>
</task>

<task id="19-02-02" type="execute">
  <name>Map current backend files to target locations</name>
  <files>.planning/phases/19-refactor-map-safety-baseline/19-BACKEND-MAP.md</files>
  <read_first>backend/app/models.py, backend/app/schemas.py, backend/app/routers, backend/app/services, backend/alembic/env.py, backend/tests/conftest.py, supervisord.conf, backend/Dockerfile</read_first>
  <action>
Add a current-to-target table for backend files and directories. At minimum cover:

- `backend/app/main.py`
- `backend/app/models.py`
- `backend/app/schemas.py`
- each `backend/app/routers/*.py`
- `backend/app/auth.py`, `ai_client.py`, `email_service.py`, `scheduler_jobs.py`, `config.py`, `database.py`, `limiter.py`
- `backend/app/services/reminder_notifications.py`
- `backend/app/websocket/manager.py`
- `backend/app/scripts/*`
- `backend/alembic.ini`, `backend/alembic/env.py`, migration versions
- `backend/tests/*.py`
- runtime references in `backend/Dockerfile`, root `Dockerfile`, `docker-compose.yml`, and `supervisord.conf`

For every move candidate, record target path, reason, risk, import-update scope, and whether a temporary shim may be useful.
  </action>
  <acceptance_criteria>
    - Map includes `backend/app/main.py`, `backend/app/models.py`, and `backend/app/schemas.py`
    - Map includes every backend router file
    - Map includes Alembic and backend tests
    - Map includes runtime targets that reference `app.main:app`
    - Shim candidates include removal notes
  </acceptance_criteria>
  <verify>`rtk proxy rg "backend/app/main.py|backend/app/models.py|backend/app/schemas.py|backend/alembic/env.py|backend/tests|app.main:app" .planning/phases/19-refactor-map-safety-baseline/19-BACKEND-MAP.md`</verify>
  <done>Backend map includes current path, target path, reason, risk, import scope, and shim notes for required files.</done>
</task>

<task id="19-02-03" type="execute">
  <name>Define backend migration slices for Phase 20</name>
  <files>.planning/phases/19-refactor-map-safety-baseline/19-BACKEND-MAP.md</files>
  <read_first>.planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md, .planning/phases/19-refactor-map-safety-baseline/19-BACKEND-MAP.md</read_first>
  <action>
Add `## Migration Slices` to `19-BACKEND-MAP.md`.

Define small Phase 20 slices with dependencies and verification after each slice. Include:

- non-moving prep and import inventory
- service/helper extraction candidates
- schemas split candidates
- models/domain split candidates
- router import updates
- Alembic/test/runtime import update pass
- final backend regression baseline

Each slice must name the protected behavior it verifies and avoid speculative abstractions.
  </action>
  <acceptance_criteria>
    - Migration slices are ordered
    - Each slice names files and verification commands
    - The Alembic/test/runtime update slice is explicit
    - No slice changes API behavior by intent
  </acceptance_criteria>
  <verify>`rtk proxy rg "## Migration Slices|Phase 20|Alembic|runtime|backend tests" .planning/phases/19-refactor-map-safety-baseline/19-BACKEND-MAP.md`</verify>
  <done>Phase 20 backend migration slices are ordered and have verification after each slice.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Tampering | Backend file map | mitigate | Require current path, target path, import scope, and risk for every backend move candidate |
| T-19-05 | Availability | Alembic/runtime imports | mitigate | Include Alembic env, tests, Docker, compose, and uvicorn targets in the backend map |
| T-19-06 | Maintainability | Over-splitting models and schemas | mitigate | Mark split depth as Phase 20 discretion and document coupling before enforcing a split |
</threat_model>

<verification>
- `rtk proxy rg "backend/app/main.py|backend/alembic/env.py|backend/tests|app.main:app" .planning/phases/19-refactor-map-safety-baseline/19-BACKEND-MAP.md`
- `rtk proxy rg "STRUCT-01|STRUCT-03|D-01|D-06" .planning/phases/19-refactor-map-safety-baseline/19-BACKEND-MAP.md`
- Manually confirm no backend source files were edited.
</verification>

<success_criteria>
- Backend target structure is documented with TeamFlow-native names.
- Current backend files and runtime references have target mapping.
- Phase 20 can proceed in small, verifiable migration slices.
- No backend code is moved in this phase.
</success_criteria>
