---
phase: "19-refactor-map-safety-baseline"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - ".planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md"
autonomous: true
user_setup: []
requirements: ["STRUCT-02", "STRUCT-03"]
must_haves:
  truths:
    - "D-11: Define the full available baseline before code moves: backend tests, Alembic validation, frontend check, frontend build, Docker/runtime command review, and manual smoke checklist."
    - "D-12: If a command is blocked by local environment, document the exact command, failure reason, and next-best fallback."
    - "D-13: Manual smoke coverage must include login/session, task board load, AI task input, WebSocket chat connection, scheduler/notifications, /health, and current Svelte routes."
    - "D-15: The playbook must include protected behavior list, verification commands, sequencing notes, and shim policy."
  artifacts:
    - path: ".planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md"
      provides: "Pre-refactor command baseline and protected behavior inventory"
      contains: "## Protected Behavior Inventory"
    - path: ".planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md"
      provides: "Blocked command handling rules"
      contains: "failure reason"
  key_links: []
---

# Phase 19 Plan 01: Safety Baseline and Protected Behavior Inventory

<objective>
Create the pre-refactor safety baseline artifact so later code-moving phases know exactly which behavior, commands, and runtime targets must remain stable.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-refactor-map-safety-baseline/19-CONTEXT.md
@.planning/phases/19-refactor-map-safety-baseline/19-RESEARCH.md
@.planning/phases/19-refactor-map-safety-baseline/19-VALIDATION.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task id="19-01-01" type="execute">
  <name>Create the safety baseline artifact shell</name>
  <files>.planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md</files>
  <read_first>.planning/phases/19-refactor-map-safety-baseline/19-CONTEXT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>
Create `.planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md` with sections for:

- phase boundary and non-goals
- protected behavior inventory
- pre-refactor command baseline
- command result table with columns for command, purpose, result, failure reason, fallback, and notes
- manual smoke checklist
- Docker/runtime entrypoint review
- Alembic and migration safety notes
- temporary shim policy

This is a documentation artifact only. Do not edit application code.
  </action>
  <acceptance_criteria>
    - `19-SAFETY-BASELINE.md` exists
    - It includes `## Protected Behavior Inventory`
    - It includes `## Pre-Refactor Command Baseline`
    - It includes `## Manual Smoke Checklist`
    - It explicitly says Phase 19 does not move code
  </acceptance_criteria>
  <verify>`rtk proxy test -f .planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md` and `rtk proxy rg "## Protected Behavior Inventory|## Pre-Refactor Command Baseline|## Manual Smoke Checklist" .planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md`</verify>
  <done>The safety baseline file exists with the required sections and phase non-goal statement.</done>
</task>

<task id="19-01-02" type="execute">
  <name>Run or document baseline verification commands</name>
  <files>.planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md</files>
  <read_first>backend/tests, frontend/package.json, backend/alembic.ini, supervisord.conf, Dockerfile, docker-compose.yml</read_first>
  <action>
For each command below, run it if the local environment supports it. If it is blocked, record the exact command, failure reason, and next-best fallback in `19-SAFETY-BASELINE.md`.

- `rtk pytest backend/tests -q`
- `rtk proxy python -m compileall backend/app`
- `rtk proxy alembic -c backend/alembic.ini heads`
- from `frontend`: `rtk npm run check` or the Bun equivalent if available
- from `frontend`: `rtk npm run build` or the Bun equivalent if available
- frontend Playwright check scoped to existing tests if dependencies and browsers are present

For fallback rules, prefer non-mutating checks such as import greps, config review, or command availability checks. Do not install dependencies unless the user explicitly approves that outside this plan.
  </action>
  <acceptance_criteria>
    - Every listed command appears in `19-SAFETY-BASELINE.md`
    - Every blocked command has a failure reason
    - Every blocked command has a fallback or an explicit "no safe fallback" note
    - No command is silently omitted
  </acceptance_criteria>
  <verify>`rtk proxy rg "rtk pytest backend/tests -q|compileall backend/app|alembic -c backend/alembic.ini heads|npm run check|npm run build|playwright" .planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md`</verify>
  <done>All baseline commands are present with result, failure reason, and fallback fields completed where applicable.</done>
</task>

<task id="19-01-03" type="execute">
  <name>Inventory protected runtime and product behavior</name>
  <files>.planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md</files>
  <read_first>backend/app/main.py, backend/app/auth.py, backend/app/routers, backend/app/scheduler_jobs.py, backend/app/websocket/manager.py, frontend/src/routes, frontend/src/lib/api.ts, frontend/src/lib/websocket.ts, supervisord.conf, nginx.conf</read_first>
  <action>
Fill the protected behavior inventory with concrete current paths and behavior notes for:

- FastAPI startup/lifespan, scheduler start/shutdown, CORS, rate limiting, `/health`
- REST route prefixes and WebSocket `/ws/chat`
- auth/session behavior including cookie and bearer paths
- current Svelte route URLs
- WebSocket chat, DMs, presence, assistant streaming/cancel/reset
- scheduler and notification generation
- AI task input and AI breakdown/parse endpoints
- Docker/Azure/runtime entrypoints and uvicorn target
- Alembic env imports and migration history

Include enough detail that Phase 20/21/22 can verify no behavior changed after moves.
  </action>
  <acceptance_criteria>
    - `19-SAFETY-BASELINE.md` names `backend/app/main.py`
    - It names `/ws/chat`, `/health`, and the auth/session routes
    - It names current Svelte route URLs including `/invite/accept`
    - It names `supervisord.conf`, `Dockerfile`, `backend/Dockerfile`, `frontend/Dockerfile`, `docker-compose.yml`, and `nginx.conf`
    - It names `backend/alembic/env.py` and migration history as protected
  </acceptance_criteria>
  <verify>`rtk proxy rg "/api/auth|/ws/chat|/health|app.main:app|backend/alembic/env.py|/invite/accept" .planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md`</verify>
  <done>Protected behavior inventory names all critical API, route, scheduler, runtime, and migration surfaces.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | Protected behavior inventory | mitigate | Require concrete path and route references for auth, WebSocket, scheduler, runtime, and Alembic surfaces |
| T-19-02 | Repudiation | Blocked baseline commands | mitigate | Record command, result, failure reason, and fallback for every unavailable check |
| T-19-03 | Availability | Future refactor runtime startup | mitigate | Capture uvicorn, Docker, nginx, and compose entrypoints before any code moves |
</threat_model>

<verification>
- `rtk proxy rg "/api/auth|/ws/chat|/health|app.main:app|alembic" .planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md`
- `rtk proxy rg "failure reason|fallback|blocked" .planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md`
- Review that every baseline command is present with a result state.
</verification>

<success_criteria>
- `19-SAFETY-BASELINE.md` exists and is specific enough for downstream verification.
- All Phase 19 protected behavior categories from the context are represented.
- Baseline commands are either run or explicitly documented as blocked with fallback.
- No application code is moved or behavior is changed.
</success_criteria>
