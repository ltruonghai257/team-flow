---
phase: "19-refactor-map-safety-baseline"
plan: 3
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - ".planning/phases/19-refactor-map-safety-baseline/19-FRONTEND-MAP.md"
autonomous: true
user_setup: []
requirements: ["STRUCT-01", "STRUCT-03"]
must_haves:
  truths:
    - "D-03: Do not move the SvelteKit app to repo-root `src` during this milestone unless a later plan explicitly accepts Docker/build/runtime impact."
    - "D-07: Prioritize scalable frontend structure, not the smallest possible move."
    - "D-08: The map may include a full frontend structure pass when it improves maintainability."
    - "D-09: Current route URLs and visual behavior must remain stable; this is not a UI redesign."
    - "D-10: Split API clients and shared types by feature domain while keeping request/auth behavior centralized."
  artifacts:
    - path: ".planning/phases/19-refactor-map-safety-baseline/19-FRONTEND-MAP.md"
      provides: "Frontend target tree and old-to-new file map"
      contains: "## Frontend Target Structure"
    - path: ".planning/phases/19-refactor-map-safety-baseline/19-FRONTEND-MAP.md"
      provides: "Frontend migration slices for Phase 21"
      contains: "## Migration Slices"
  key_links: []
---

# Phase 19 Plan 03: Frontend Target Map

<objective>
Document the SvelteKit target structure, API/type split map, route boundary rules, and Phase 21 migration slices without changing frontend behavior or moving files.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-refactor-map-safety-baseline/19-CONTEXT.md
@.planning/phases/19-refactor-map-safety-baseline/19-RESEARCH.md
@.planning/phases/19-refactor-map-safety-baseline/19-VALIDATION.md
@.planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task id="19-03-01" type="execute">
  <name>Document frontend target structure</name>
  <files>.planning/phases/19-refactor-map-safety-baseline/19-FRONTEND-MAP.md</files>
  <read_first>frontend/src/lib, frontend/src/routes, frontend/package.json, frontend/vite.config.ts, frontend/svelte.config.js</read_first>
  <action>
Create `.planning/phases/19-refactor-map-safety-baseline/19-FRONTEND-MAP.md`.

Document an adapted Open WebUI-style target tree that keeps `frontend/src` as the SvelteKit root and includes:

- `src/lib/apis`
- `src/lib/components`
- `src/lib/stores`
- `src/lib/types`
- `src/lib/utils`
- WebSocket client placement
- route-local component boundaries
- app shell/global CSS/config files

State explicitly that route URLs and visual behavior must remain unchanged.
  </action>
  <acceptance_criteria>
    - `19-FRONTEND-MAP.md` contains `## Frontend Target Structure`
    - It keeps `frontend/src` as root
    - It includes `src/lib/apis`, `components`, `stores`, `types`, and `utils`
    - It states that this is not a UI redesign
  </acceptance_criteria>
  <verify>`rtk proxy rg "## Frontend Target Structure|frontend/src|src/lib/apis|src/lib/components|src/lib/stores|src/lib/types|src/lib/utils" .planning/phases/19-refactor-map-safety-baseline/19-FRONTEND-MAP.md`</verify>
  <done>Frontend target tree exists and covers every required SvelteKit shared-code group.</done>
</task>

<task id="19-03-02" type="execute">
  <name>Map current frontend files to target locations</name>
  <files>.planning/phases/19-refactor-map-safety-baseline/19-FRONTEND-MAP.md</files>
  <read_first>frontend/src/lib/api.ts, frontend/src/lib/stores, frontend/src/lib/components, frontend/src/routes, frontend/src/lib/websocket.ts, frontend/tests</read_first>
  <action>
Add a current-to-target map for:

- `frontend/src/lib/api.ts` into shared request module plus feature API modules
- exported API types into `frontend/src/lib/types/{domain}.ts`
- stores under `frontend/src/lib/stores`
- `frontend/src/lib/websocket.ts`
- `frontend/src/lib/utils.ts`
- shared components by feature group
- route files and route-local extraction candidates
- frontend tests and Playwright config
- package/build config files

For API splitting, document which shared behavior remains centralized: base URL, credentials, JSON handling, sub-team header behavior, and error handling.
  </action>
  <acceptance_criteria>
    - Map names `frontend/src/lib/api.ts`
    - Map includes feature API module targets under `frontend/src/lib/apis`
    - Map includes type module targets under `frontend/src/lib/types`
    - Map includes all current route URLs from ROADMAP Phase 21
    - Shared request/auth behavior is explicitly centralized
  </acceptance_criteria>
  <verify>`rtk proxy rg "frontend/src/lib/api.ts|frontend/src/lib/apis|frontend/src/lib/types|/tasks|/invite/accept|credentials" .planning/phases/19-refactor-map-safety-baseline/19-FRONTEND-MAP.md`</verify>
  <done>Frontend map includes current path, target path, route-boundary notes, and centralized request behavior.</done>
</task>

<task id="19-03-03" type="execute">
  <name>Define frontend migration slices for Phase 21</name>
  <files>.planning/phases/19-refactor-map-safety-baseline/19-FRONTEND-MAP.md</files>
  <read_first>.planning/phases/19-refactor-map-safety-baseline/19-FRONTEND-MAP.md, .planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md</read_first>
  <action>
Add `## Migration Slices` to `19-FRONTEND-MAP.md`.

Define small Phase 21 slices with dependencies and verification after each slice. Include:

- shared request wrapper extraction
- type extraction by domain
- feature API module extraction
- route/component import update waves
- route-local component extraction candidates where useful
- final Svelte check/build and visual smoke checklist

Each slice must preserve current routes and visual behavior.
  </action>
  <acceptance_criteria>
    - Migration slices are ordered
    - Each slice names files and verification commands
    - Route URLs are listed as protected
    - No slice introduces a UI redesign
  </acceptance_criteria>
  <verify>`rtk proxy rg "## Migration Slices|Phase 21|request wrapper|type extraction|route URLs|Svelte" .planning/phases/19-refactor-map-safety-baseline/19-FRONTEND-MAP.md`</verify>
  <done>Phase 21 frontend migration slices are ordered and preserve current routes and visual behavior.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Tampering | API request behavior | mitigate | Keep request/auth/sub-team behavior centralized and map feature APIs around it |
| T-19-08 | Availability | Svelte routes | mitigate | Require route URL list and route-local extraction boundaries before Phase 21 |
| T-19-09 | Integrity | Shared types | mitigate | Split types by domain only where they reduce duplication and avoid circular imports |
</threat_model>

<verification>
- `rtk proxy rg "frontend/src/lib/api.ts|frontend/src/lib/apis|/tasks|/invite/accept|credentials" .planning/phases/19-refactor-map-safety-baseline/19-FRONTEND-MAP.md`
- `rtk proxy rg "STRUCT-01|STRUCT-03|D-07|D-10" .planning/phases/19-refactor-map-safety-baseline/19-FRONTEND-MAP.md`
- Manually confirm no frontend source files were edited.
</verification>

<success_criteria>
- Frontend target structure is documented with scalable `src/lib` groups.
- Current frontend files and route boundaries have target mapping.
- Phase 21 can proceed in small, verifiable migration slices.
- No frontend code is moved or redesigned in this phase.
</success_criteria>
