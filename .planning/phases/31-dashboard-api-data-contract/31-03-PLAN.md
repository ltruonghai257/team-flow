---
phase: 31-dashboard-api-data-contract
plan: "03"
type: execute
wave: 3
depends_on:
  - "31-02"
files_modified:
  - backend/tests/test_dashboard.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
must_haves:
  truths:
    - "D-16: Tests cover member role (my_tasks + recent_activity present; team_health + kpi_summary absent), supervisor role (all 4 keys present), shape validation for each section, and my_tasks urgency sort."
    - "Tests use create_access_token({'sub': str(user.id)}) for JWT — not a .token attribute on User (no such attribute exists)."
    - "Tests create inline test data (users, tasks, standup posts) within each test function using db_session fixture — no reliance on external fixtures beyond conftest.py defaults."
    - "The existing placeholder test in test_dashboard.py (test_admin_all_teams) references admin_user.token which does not exist — replace the entire file content with the new tests."
  artifacts:
    - path: "backend/tests/test_dashboard.py"
      provides: "Role-conditional payload shape tests for member and supervisor, my_tasks sort test, recent_activity test"
      min_lines: 120
      contains: "test_member_dashboard"
  key_links:
    - from: "backend/tests/test_dashboard.py"
      to: "backend/app/routers/dashboard.py"
      via: "async HTTP client tests against GET /api/dashboard/"
      pattern: "/api/dashboard"
---

<objective>
Extend backend/tests/test_dashboard.py with pytest async tests covering all D-16 scenarios: role-conditional payload shape for member and supervisor roles, field presence validation, my_tasks urgency sort, and recent_activity scoping.

Purpose: Confirm API-01 through API-05 requirements are met with automated backend test coverage.
Output: test_dashboard.py with at least 4 test functions covering all D-16 scenarios.
</objective>

<execution_context>
@/Users/haila/Library/Mobile Documents/com~apple~CloudDocs/PrivateCode/team-flow/.windsurf/get-shit-done/workflows/execute-plan.md
@/Users/haila/Library/Mobile Documents/com~apple~CloudDocs/PrivateCode/team-flow/.windsurf/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/31-dashboard-api-data-contract/31-CONTEXT.md
@.planning/phases/31-dashboard-api-data-contract/31-RESEARCH.md
@backend/tests/conftest.py
@backend/tests/test_visibility.py
@backend/app/routers/dashboard.py
@backend/app/schemas/dashboard.py
@backend/app/models/updates.py
@backend/app/models/work.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace test_dashboard.py with D-16 role-conditional and shape tests</name>
  <files>backend/tests/test_dashboard.py</files>
  <read_first>backend/tests/test_dashboard.py, backend/tests/conftest.py, backend/tests/test_visibility.py, backend/app/schemas/dashboard.py, backend/app/routers/dashboard.py, backend/app/models/work.py, backend/app/models/updates.py, backend/app/models/users.py</read_first>
  <action>
Replace the entire content of `backend/tests/test_dashboard.py`. The existing file contains one broken placeholder test (`test_admin_all_teams`) that references `admin_user.token` (no such fixture). Replace it completely.

**Pattern to follow** (from test_visibility.py):
- Import `create_access_token` from `app.utils.auth`
- Create users inline in test functions using `db_session.add(User(...))` + `await db_session.flush()`
- Get token via `create_access_token({"sub": str(user.id)})`
- Make requests via `await async_client.get("/api/dashboard/", headers={"Cookie": f"access_token={token}"})`

**Required imports:**
```python
import pytest
import pytest_asyncio
from datetime import datetime, date, timedelta, timezone

from httpx import AsyncClient
from sqlalchemy.ext.asyncio import AsyncSession

from app.models import SubTeam, Task, TaskStatus, TaskPriority, User, UserRole
from app.models.updates import StandupPost
from app.utils.auth import create_access_token, hash_password
```

**Helper to create a user:**
```python
async def _make_user(db, *, email, username, role, sub_team_id=None):
    user = User(
        email=email,
        username=username,
        full_name=username.replace("_", " ").title(),
        hashed_password=hash_password("password"),
        role=role,
        sub_team_id=sub_team_id,
    )
    db.add(user)
    await db.flush()
    return user

def _token(user):
    return create_access_token({"sub": str(user.id)})
```

**Test 1: `test_member_dashboard_shape` — D-16 member role**

Create: sub_team, member_user (role=member, sub_team_id=sub_team.id)
Assert:
- GET /api/dashboard/ returns HTTP 200
- response JSON contains key `"my_tasks"`
- response JSON contains key `"recent_activity"`
- response JSON does NOT contain key `"team_health"` (absent, not null)
- response JSON does NOT contain key `"kpi_summary"` (absent, not null)
- `my_tasks` is a list
- `recent_activity` is a list

**Test 2: `test_supervisor_dashboard_shape` — D-16 supervisor role**

Create: sub_team, supervisor_user (role=supervisor, sub_team_id=sub_team.id)
Assert:
- GET /api/dashboard/ returns HTTP 200
- response JSON contains key `"my_tasks"`
- response JSON contains key `"team_health"`
- response JSON contains key `"kpi_summary"`
- response JSON contains key `"recent_activity"`
- `team_health` is a list
- `kpi_summary` is a dict
- `kpi_summary` has keys: `"avg_score"`, `"completion_rate"`, `"needs_attention_count"`

**Test 3: `test_my_tasks_urgency_sort` — D-16 my_tasks sort**

Create: sub_team, member_user, two tasks assigned to member_user:
- Task A: `due_date = (now - timedelta(days=1)).date()`, status=todo (overdue)
- Task B: `due_date = (now + timedelta(days=3)).date()`, status=todo (upcoming)

Assert:
- GET /api/dashboard/ returns HTTP 200
- response JSON `my_tasks` has length 2
- first item in `my_tasks` has `"is_overdue": true` (Task A comes first)
- second item in `my_tasks` has `"is_overdue": false` (Task B comes second)

Note on Task creation: tasks need at minimum `title`, `status`, `assignee_id`. `due_date` field type in the model is `date` — pass a `date` object, not `datetime`.

**Test 4: `test_my_tasks_excludes_done` — D-05 done exclusion**

Create: sub_team, member_user, two tasks assigned to member_user:
- Task A: status=todo (active)
- Task B: status=done (should be excluded)

Assert:
- GET /api/dashboard/ returns HTTP 200
- `my_tasks` has length 1 (only Task A)
- the single item title matches Task A's title

**Test 5: `test_recent_activity_shape` — D-13 to D-15**

Create: sub_team, member_user, one StandupPost (author_id=member_user.id, sub_team_id=sub_team.id, field_values={"Pending Tasks": "test"}, task_snapshot=[])

Assert:
- GET /api/dashboard/ returns HTTP 200
- `recent_activity` has length 1
- the activity item has keys: `"post_id"`, `"author_id"`, `"author_name"`, `"created_at"`, `"field_values"`
- `author_name` equals member_user's full_name

**Important implementation notes:**
1. Each test function must be decorated with `@pytest.mark.asyncio`
2. Each test must have `db_session: AsyncSession` and `async_client: AsyncClient` fixtures in its signature
3. After creating all objects with `db.add(...)` + `await db.flush()`, commit with `await db_session.commit()` before making HTTP requests (so the app's DB session sees the data)
4. The `StandupPost` model requires `field_values` (dict) and `task_snapshot` (list) — both are JSON columns
5. Task model fields: check `backend/app/models/work.py` for required vs optional columns before creating test tasks; at minimum provide `title`, `status`, `assignee_id`
  </action>
  <verify><automated>cd backend && rtk uv run pytest tests/test_dashboard.py -v</automated></verify>
  <acceptance_criteria>
    - `backend/tests/test_dashboard.py` contains `test_member_dashboard_shape`
    - `backend/tests/test_dashboard.py` contains `test_supervisor_dashboard_shape`
    - `backend/tests/test_dashboard.py` contains `test_my_tasks_urgency_sort`
    - `backend/tests/test_dashboard.py` contains `test_my_tasks_excludes_done`
    - `backend/tests/test_dashboard.py` contains `test_recent_activity_shape`
    - `cd backend && rtk uv run pytest tests/test_dashboard.py -v` exits 0 (all tests pass)
    - test for member role asserts `"team_health" not in data` and `"kpi_summary" not in data`
    - test for supervisor role asserts `"team_health" in data` and `"kpi_summary" in data`
    - urgency sort test asserts first task has `is_overdue: true`
  </acceptance_criteria>
  <done>All D-16 test scenarios pass. Role-conditional payload shape is verified by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test token to user identity | create_access_token must use {"sub": str(user.id)} to match the get_current_user dependency pattern |
| SQLite JSON columns in tests | StandupPost.field_values uses JSON().with_variant(JSONB(), "postgresql") — works in SQLite for tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06 | I | test_member role isolation | mitigate | test explicitly asserts "team_health" not in response — not just that it is null |
| T-31-07 | T | test data visibility | mitigate | flush+commit before HTTP requests so test data is visible to the app session |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd backend && rtk uv run pytest tests/test_dashboard.py -v` exits 0
- [ ] 5 tests present and all pass
- [ ] `test_member_dashboard_shape` asserts `"team_health" not in data`
- [ ] `test_supervisor_dashboard_shape` asserts `"team_health" in data` and `"kpi_summary" in data`
- [ ] `test_my_tasks_urgency_sort` asserts first item has `is_overdue: true`
- [ ] `test_my_tasks_excludes_done` asserts done task is excluded
</verification>

<success_criteria>
- All D-16 test scenarios implemented and passing
- Member role: my_tasks and recent_activity present; team_health and kpi_summary absent (key not in response)
- Supervisor role: all four keys present with correct shape
- my_tasks urgency sort: overdue tasks precede upcoming tasks
- Done tasks excluded from my_tasks
- recent_activity items have all required fields: post_id, author_id, author_name, created_at, field_values
- `cd backend && rtk uv run pytest tests/test_dashboard.py -v` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/31-dashboard-api-data-contract/31-03-SUMMARY.md`
</output>
