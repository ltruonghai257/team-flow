---
phase: "17-sprint-release-reminders"
plan: 5
type: execute
wave: 5
depends_on: ["17-01", "17-02", "17-03", "17-04"]
files_modified:
  - "backend/app/routers/notifications.py"
  - "backend/app/schemas.py"
  - "frontend/src/lib/stores/notifications.ts"
  - "frontend/src/lib/components/NotificationBell.svelte"
  - "frontend/src/routes/tasks/+page.svelte"
  - "frontend/src/routes/milestones/+page.svelte"
autonomous: true
requirements: ["REMIND-01", "REMIND-02"]
must_haves:
  truths:
    - "D-14: Members cannot acknowledge reminders beyond the existing notification dismiss action"
    - "D-27: Sprint reminder clicks navigate to /tasks with the relevant sprint selected when routing/state supports it; fallback is /tasks"
    - "D-28: Milestone reminder clicks navigate to /milestones, ideally focused or highlighted on the relevant milestone; fallback is /milestones"
    - "D-29: Use participant-vs-supervisor wording rather than identical copy for everyone"
    - "D-30: Participants get action wording such as Sprint X ends in 2 days. Review your remaining tasks"
    - "D-31: Supervisors and manager-chain recipients get awareness wording such as Sprint X ends in 2 days for members you supervise"
    - "D-32: If a user qualifies as both participant and supervisor, send one notification and prefer participant wording"
    - "D-33: Reminder notifications show both relative lead time and exact date"
---

<objective>
Finish notification behavior: generated reminder wording, bell routing, optional target highlighting, and final verification.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-sprint-release-reminders/17-CONTEXT.md
@.planning/phases/17-sprint-release-reminders/17-RESEARCH.md
@.planning/phases/17-sprint-release-reminders/17-VALIDATION.md
</context>

<tasks>

<task id="17-05-01" type="execute">
  <name>Ensure generated reminder copy carries role and date context</name>
  <read_first>backend/app/services/reminder_notifications.py, backend/app/schemas.py, backend/app/routers/notifications.py</read_first>
  <action>
Review generated `EventNotification.title_cache`, `start_at_cache`, `offset_minutes`, and any response fields used by the bell.

Ensure generated reminder copy:

- differs for participant vs supervisor recipients
- prefers participant wording when a user qualifies both ways
- includes relative lead time, such as "in 2 days"
- includes or exposes the exact sprint end/milestone due date through existing `start_at_cache`

Do not add a new acknowledgement flow. Existing dismiss behavior is sufficient.
  </action>
  <acceptance_criteria>
    - Generated sprint participant title includes action wording
    - Generated sprint supervisor title includes awareness wording
    - Generated milestone title includes due-date context
    - No new acknowledge endpoint is added
  </acceptance_criteria>
</task>

<task id="17-05-02" type="execute">
  <name>Extend frontend notification types and bell routing</name>
  <read_first>frontend/src/lib/stores/notifications.ts, frontend/src/lib/components/NotificationBell.svelte</read_first>
  <action>
Update frontend notification typing to include:

- `sprint_end`
- `milestone_due`
- `reminder_settings_proposal`

Update `NotificationBell.svelte` click routing:

- `schedule` -> `/schedule`
- `task` -> `/tasks`
- `sprint_end` -> `/tasks?sprint_id={event_ref_id}` if safe, otherwise `/tasks`
- `milestone_due` -> `/milestones?milestone_id={event_ref_id}` if safe, otherwise `/milestones`
- `reminder_settings_proposal` -> `/team`

Keep dismiss behavior unchanged.
  </action>
  <acceptance_criteria>
    - `frontend/src/lib/stores/notifications.ts` includes generated event types
    - `NotificationBell.svelte` routes `sprint_end`
    - `NotificationBell.svelte` routes `milestone_due`
    - `NotificationBell.svelte` routes `reminder_settings_proposal`
    - Dismiss action remains the only acknowledgement behavior
  </acceptance_criteria>
</task>

<task id="17-05-03" type="execute">
  <name>Add optional sprint and milestone target handling</name>
  <read_first>frontend/src/routes/tasks/+page.svelte, frontend/src/routes/milestones/+page.svelte</read_first>
  <action>
If the tasks and milestones pages can consume query parameters with small, surgical changes:

- make `/tasks?sprint_id={id}` preselect or filter to the sprint when route state supports it
- make `/milestones?milestone_id={id}` focus, highlight, or scroll to the milestone when practical

If the page structure does not support this without broad refactoring, keep the fallback routes from Task 17-05-02 and record the limitation in the summary.
  </action>
  <acceptance_criteria>
    - Either tasks page consumes `sprint_id` or summary records fallback to `/tasks`
    - Either milestones page consumes `milestone_id` or summary records fallback to `/milestones`
  </acceptance_criteria>
</task>

<task id="17-05-04" type="execute">
  <name>Run final focused verification</name>
  <read_first>.planning/phases/17-sprint-release-reminders/17-VALIDATION.md</read_first>
  <action>
Run:

```bash
python -m compileall backend/app
cd backend && pytest tests/test_notifications.py tests/test_sub_teams.py tests/test_sprints.py
cd frontend && bun run check
```

If any command cannot run due to missing local dependencies or broken pre-existing fixtures, record the exact blocker and any narrower command that did pass.
  </action>
  <acceptance_criteria>
    - Backend compile exits 0
    - Focused backend tests exit 0 or blockers are recorded exactly
    - Frontend check exits 0 or blocker is recorded exactly
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-13 | Information disclosure | Bell routing | mitigate | Route only to pages already authorized by backend APIs |
| T-17-14 | Repudiation | Reminder acknowledgement | accept | Existing dismiss action remains the only member action and preserves no extra business state |
| T-17-15 | Spoofing | Notification copy | mitigate | Backend service decides participant/supervisor wording from authoritative recipient resolution |
</threat_model>

<verification>
- `python -m compileall backend/app`
- `cd backend && pytest tests/test_notifications.py tests/test_sub_teams.py tests/test_sprints.py`
- `cd frontend && bun run check`
</verification>

<success_criteria>
- Sprint reminders click through to tasks or a documented safe fallback.
- Milestone reminders click through to milestones or a documented safe fallback.
- Generated reminders show role-appropriate copy, relative lead time, and exact date.
- Existing notification dismiss remains the only acknowledgement behavior.
</success_criteria>
