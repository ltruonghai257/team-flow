---
phase: "17-sprint-release-reminders"
plan: 4
type: execute
wave: 4
depends_on: ["17-01"]
files_modified:
  - "frontend/src/lib/api.ts"
  - "frontend/src/routes/team/+page.svelte"
autonomous: true
requirements: ["REMIND-01", "REMIND-02"]
must_haves:
  truths:
    - "D-04: Reminder settings live on /team inside the sub-team management/settings area"
    - "D-05: Admins edit reminder settings for the currently active sub-team from the global switcher"
    - "D-06: Supervisors work within their own sub-team context"
    - "D-07: Members can see the team's reminder lead time read-only, but cannot change it"
    - "D-08: Admins can apply reminder setting changes directly for the active sub-team"
    - "D-09: Supervisors can propose changes to lead time and sprint/milestone reminder toggles"
    - "D-10: Supervisor-proposed changes do not take effect until an admin approves them"
    - "D-12: Review and approval happen on /team near the sub-team reminder settings"
    - "D-13: No dedicated approvals page is required in Phase 17"
---

<objective>
Add the `/team` frontend controls for viewing, editing, proposing, and approving reminder settings.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-sprint-release-reminders/17-CONTEXT.md
@.planning/phases/17-sprint-release-reminders/17-RESEARCH.md
</context>

<tasks>

<task id="17-04-01" type="execute">
  <name>Add frontend API helpers for reminder settings</name>
  <read_first>frontend/src/lib/api.ts</read_first>
  <action>
Update `frontend/src/lib/api.ts` with typed helper methods for:

- loading current reminder settings
- admin direct update
- supervisor proposal create
- admin pending proposal list
- admin proposal approve/reject

Use existing `request()` so admin active sub-team context continues to flow through `X-SubTeam-ID`.
  </action>
  <acceptance_criteria>
    - `frontend/src/lib/api.ts` contains `reminderSettings` or equivalent API object
    - Helpers call `/sub-teams/reminder-settings/current`
    - Helpers call `/sub-teams/reminder-settings/proposals`
  </acceptance_criteria>
</task>

<task id="17-04-02" type="execute">
  <name>Add reminder settings state to /team</name>
  <read_first>frontend/src/routes/team/+page.svelte, frontend/src/lib/stores/auth.ts</read_first>
  <action>
Update `frontend/src/routes/team/+page.svelte`:

- Load reminder settings on mount for all authenticated users who can access `/team`.
- For members, render read-only lead time and enabled/disabled status.
- For supervisors, render controls that submit a proposal instead of directly saving settings.
- For admins, render editable controls that patch current active sub-team settings directly.
- Keep controls in the existing team/sub-team management surface; do not create a new page.

Use compact, utilitarian controls: numeric input for lead time and toggles/checkboxes for sprint and milestone reminders.
  </action>
  <acceptance_criteria>
    - `/team` displays reminder lead time
    - `/team` displays separate sprint and milestone toggles/statuses
    - Supervisor save action calls proposal API
    - Admin save action calls direct update API
    - Member view has no editable save action
  </acceptance_criteria>
</task>

<task id="17-04-03" type="execute">
  <name>Add admin proposal review UI on /team</name>
  <read_first>frontend/src/routes/team/+page.svelte</read_first>
  <action>
On `/team`, add an admin-only proposal review area near the reminder settings controls:

- Load pending proposals.
- Show proposed values and current status.
- Provide approve and reject buttons.
- On approve/reject, refresh current settings and proposal list.

Do not add a dedicated approvals page.
  </action>
  <acceptance_criteria>
    - `/team` contains admin-only pending proposal list
    - UI calls review API with approve/reject action
    - Approved proposal refreshes displayed settings
  </acceptance_criteria>
</task>

<task id="17-04-04" type="execute">
  <name>Verify frontend team settings UI</name>
  <read_first>frontend/src/lib/api.ts, frontend/src/routes/team/+page.svelte</read_first>
  <action>
Run:

```bash
cd frontend && bun run check
```

If Bun dependencies are not installed, record the exact blocker in the summary.
  </action>
  <acceptance_criteria>
    - `cd frontend && bun run check` exits 0 or blocker is recorded exactly
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-10 | Spoofing | Frontend role controls | mitigate | UI hides invalid actions but backend remains source of truth |
| T-17-11 | Tampering | Supervisor proposal UI | mitigate | Supervisor form uses proposal endpoint only; no direct settings PATCH |
| T-17-12 | Information disclosure | Admin active sub-team state | mitigate | Reuse existing API request helper with `X-SubTeam-ID` |
</threat_model>

<verification>
- `cd frontend && bun run check`
- Manual smoke: member read-only, supervisor proposal, admin direct update, admin approval
</verification>

<success_criteria>
- `/team` exposes reminder settings in the existing sub-team/settings area.
- Admins can apply settings directly.
- Supervisors can propose settings changes.
- Members can read the lead time and toggles without changing them.
</success_criteria>
