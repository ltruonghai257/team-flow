---
phase: 13-multi-team-hierarchy-timeline-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models.py
  - backend/app/schemas.py
  - backend/alembic/versions/xxx_add_sub_team.py
autonomous: true
requirements: [TEAM-01, TEAM-02]
user_setup: []

must_haves:
  truths:
    - SubTeam table exists in database with id, name, supervisor_id, created_at columns
    - User table has nullable sub_team_id FK column
    - Project table has nullable sub_team_id FK column
    - TeamInvite table has nullable sub_team_id FK column
    - Default sub-team named "Default Team" exists with no supervisor
    - All existing users belong to default sub-team
    - All existing projects belong to default sub-team
    - All existing invites belong to default sub-team
    - FK constraints enforce referential integrity
  artifacts:
    - path: backend/app/models.py
      provides: SubTeam ORM model, User.sub_team relationship, Project.sub_team relationship
      contains: class SubTeam, sub_team_id Column on User and Project
    - path: backend/app/schemas.py
      provides: Pydantic schemas for SubTeam CRUD
      exports: SubTeamCreate, SubTeamUpdate, SubTeamOut
    - path: backend/alembic/versions/xxx_add_sub_team.py
      provides: Database migration script
      contains: op.create_table('sub_teams'), op.add_column('sub_team_id')
  key_links:
    - from: backend/app/models.py
      to: backend/alembic/versions/xxx_add_sub_team.py
      via: migration script references model structure
      pattern: op.create_table.*sub_teams
    - from: backend/alembic/versions/xxx_add_sub_team.py
      to: database
      via: alembic upgrade command
      pattern: alembic upgrade head
---

<objective>
Create SubTeam model and migrate existing data to default sub-team (per D-01, D-02, D-04)

Purpose: Establish the foundational data model for multi-team hierarchy before implementing scoping logic
Output: Working SubTeam model with all users/projects migrated to "Default Team"
</objective>

<execution_context>
@/Users/haila/Library/Mobile Documents/com~apple~CloudDocs/PrivateCode/team-flow/.windsurf/get-shit-done/workflows/execute-plan.md
@/Users/haila/Library/Mobile Documents/com~apple~CloudDocs/PrivateCode/team-flow/.windsurf/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-multi-team-hierarchy-timeline-visibility/13-CONTEXT.md
@.planning/phases/13-multi-team-hierarchy-timeline-visibility/13-RESEARCH.md

From backend/app/models.py:
```python
class User(Base):
    __tablename__ = "users"
    id = Column(Integer, primary_key=True, index=True)
    email = Column(String, unique=True, index=True, nullable=False)
    role = Column(Enum(UserRole), default=UserRole.member, nullable=False)
    # ... other fields

class Project(Base):
    __tablename__ = "projects"
    id = Column(Integer, primary_key=True, index=True)
    name = Column(String, nullable=False)
    # ... other fields
```

From backend/app/schemas.py:
```python
class UserBase(BaseModel):
    email: str
    role: UserRole

class ProjectBase(BaseModel):
    name: str
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SubTeam model to models.py</name>
  <files>backend/app/models.py</files>
  <action>Add SubTeam ORM model with:
  - Table name: "sub_teams"
  - Columns: id (Integer, primary_key, index=True), name (String, nullable=False), supervisor_id (Integer, ForeignKey("users.id"), nullable=True), created_at (DateTime, default=datetime.utcnow)
  - Relationships: supervisor = relationship("User", foreign_keys=[supervisor_id]), members = relationship("User", back_populates="sub_team")
  
  Add sub_team_id column to User model:
  - Column(Integer, ForeignKey("sub_teams.id"), nullable=True)
  - Add relationship: sub_team = relationship("SubTeam", back_populates="members")
  
  Add sub_team_id column to Project model:
  - Column(Integer, ForeignKey("sub_teams.id"), nullable=True)
  - Add relationship: sub_team = relationship("SubTeam")
  
  Add sub_team_id column to TeamInvite model:
  - Column(Integer, ForeignKey("sub_teams.id"), nullable=True)
  - Add relationship: sub_team = relationship("SubTeam")
  
  Do NOT make sub_team_id non-nullable yet - per D-04, supervisor_id is nullable during migration.
</action>
  <verify>File compiles without errors, no import issues</verify>
  <done>SubTeam model defined, FK columns added to User/Project/TeamInvite, relationships configured</done>
</task>

<task type="auto">
  <name>Task 2: Add SubTeam Pydantic schemas</name>
  <files>backend/app/schemas.py</files>
  <action>Add Pydantic schemas for SubTeam CRUD:
  - SubTeamBase: name (str)
  - SubTeamCreate(SubTeamBase): name (str), supervisor_id (Optional[int] = None)
  - SubTeamUpdate: name (Optional[str] = None), supervisor_id (Optional[int] = None)
  - SubTeamOut: id (int), name (str), supervisor_id (Optional[int]), created_at (datetime), members_count (Optional[int] = None)
  
  Update User schemas to include sub_team_id:
  - UserBase: add sub_team_id (Optional[int] = None)
  - UserOut: add sub_team_id (Optional[int])
  
  Update Project schemas to include sub_team_id:
  - ProjectBase: add sub_team_id (Optional[int] = None)
  - ProjectOut: add sub_team_id (Optional[int])
  
  Update TeamInvite schemas to include sub_team_id:
  - TeamInviteCreate: add sub_team_id (Optional[int] = None)
  - TeamInviteOut: add sub_team_id (Optional[int])
</action>
  <verify>File compiles without errors, schema validation passes</verify>
  <done>SubTeam schemas defined, existing schemas updated with sub_team_id fields</done>
</task>

<task type="auto">
  <name>Task 3: Create Alembic migration for SubTeam</name>
  <files>backend/alembic/versions/xxx_add_sub_team.py</files>
  <action>Create new Alembic migration file with revision ID (use alembic revision --autogenerate -m "add_sub_team" to generate base, then modify manually):
  
  In upgrade():
  1. Create sub_teams table with columns: id, name, supervisor_id (FK to users.id), created_at
  2. Add nullable sub_team_id column to users table
  3. Add nullable sub_team_id column to projects table
  4. Add nullable sub_team_id column to team_invites table
  5. Insert default sub-team: INSERT INTO sub_teams (name, supervisor_id, created_at) VALUES ('Default Team', NULL, NOW())
  6. Get default sub-team ID: SELECT MAX(id) FROM sub_teams
  7. Backfill users: UPDATE users SET sub_team_id = {default_id} WHERE sub_team_id IS NULL
  8. Backfill projects: UPDATE projects SET sub_team_id = {default_id} WHERE sub_team_id IS NULL
  9. Backfill team_invites: UPDATE team_invites SET sub_team_id = {default_id} WHERE sub_team_id IS NULL
  10. Create FK constraint: fk_users_sub_team (users.sub_team_id -> sub_teams.id)
  11. Create FK constraint: fk_projects_sub_team (projects.sub_team_id -> sub_teams.id)
  12. Create FK constraint: fk_invites_sub_team (team_invites.sub_team_id -> sub_teams.id)
  
  In downgrade():
  1. Drop FK constraints (reverse order: invites, projects, users)
  2. Drop sub_team_id columns (users, projects, team_invites)
  3. Drop sub_teams table
  
  Per D-01/D-02: All existing users and projects migrate to "Default Team" with no supervisor pre-assigned.
</action>
  <verify>Migration file syntax is valid, SQL statements are correct</verify>
  <done>Migration script created with reversible upgrade/downgrade, default sub-team backfill logic included</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database migration → Database | SQL execution with data modification (backfill) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | Alembic migration | mitigate | Use transactional migration (Alembic default), verify backfill count matches expected row count before commit |
| T-13-02 | Tampering | SubTeam model FK | mitigate | FK constraints enforced at database level, nullable during migration prevents blocking |
| T-13-03 | Information Disclosure | User.sub_team_id | accept | Sub-team membership is not PII, low-value target, no additional mitigation needed |
</threat_model>

<verification>
- Migration script runs without errors: `alembic upgrade head`
- Default sub-team exists in database: verify via `SELECT * FROM sub_teams WHERE name = 'Default Team'`
- All users have sub_team_id set: `SELECT COUNT(*) FROM users WHERE sub_team_id IS NULL` returns 0
- All projects have sub_team_id set: `SELECT COUNT(*) FROM projects WHERE sub_team_id IS NULL` returns 0
- FK constraints are enforced: attempt to insert user with invalid sub_team_id fails
</verification>

<success_criteria>
- SubTeam model defined with all required columns and relationships
- All existing tables (User, Project, TeamInvite) have sub_team_id FK columns
- Default sub-team "Default Team" exists with no supervisor
- All existing data migrated to default sub-team
- FK constraints enforce referential integrity
- Migration is reversible (downgrade works)
</success_criteria>

<output>
After completion, create `.planning/phases/13-multi-team-hierarchy-timeline-visibility/13-01-SUMMARY.md`
</output>
