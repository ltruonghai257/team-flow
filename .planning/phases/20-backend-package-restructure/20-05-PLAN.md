---
phase: "20-backend-package-restructure"
plan: 5
type: execute
wave: 5
depends_on: ["20-01", "20-02", "20-03", "20-04"]
files_modified:
  - ".planning/phases/20-backend-package-restructure/20-BACKEND-MIGRATION.md"
  - "backend/app/"
  - "backend/alembic/env.py"
  - "backend/tests/test_package_structure.py"
autonomous: true
user_setup: []
requirements: ["BACK-01", "BACK-02", "BACK-03", "BACK-04", "BACK-05"]
must_haves:
  truths:
    - "D-12 - Planner decides shim surfaces per module based on dependency mapping and verification risk."
    - "D-14 - Document shims with short inline comments plus a backend migration guide."
    - "D-15 - Remove low-risk shims before Phase 20 ends if verification shows they are unnecessary."
    - "D-16 - Keep high-risk compatibility surfaces such as `app.main`, `app.models`, and `app.schemas` through Phase 22 runtime/regression verification unless planning finds a safer alternative."
    - "D-18 - Phase 20 completion requires backend tests, Alembic validation, and uvicorn `/health` startup smoke."
    - "D-19 - Run focused verification after risky migration slices and the full verification floor at the end."
    - "D-20 - Include a concise backend migration guide with old-to-new paths, compatibility shims, and verification commands."
    - "D-21 - Phase 20 smoke should cover `/health` plus router registration/import checks; broader user-flow smoke remains Phase 22."
    - "D-22 - If an exact verification command is blocked, document the command, blocker, and next-best fallback."
  artifacts:
    - path: ".planning/phases/20-backend-package-restructure/20-BACKEND-MIGRATION.md"
      provides: "Backend old-to-new path map, shim inventory, and verification record"
      contains: "## Compatibility Shims"
    - path: "backend/tests/test_package_structure.py"
      provides: "Final compatibility and canonical import checks"
      contains: "app.schemas"
  key_links:
    - ".planning/phases/20-backend-package-restructure/20-CONTEXT.md#verification-floor"
    - ".planning/phases/20-backend-package-restructure/20-VALIDATION.md"
---

# Phase 20 Plan 05: Migration Guide and Verification Floor

<objective>
Finalize the backend restructure by documenting old-to-new paths and shims, removing safe low-risk delegates, and running the Phase 20 backend verification floor.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-backend-package-restructure/20-CONTEXT.md
@.planning/phases/20-backend-package-restructure/20-RESEARCH.md
@.planning/phases/20-backend-package-restructure/20-VALIDATION.md
@.planning/phases/20-backend-package-restructure/20-01-PLAN.md
@.planning/phases/20-backend-package-restructure/20-02-PLAN.md
@.planning/phases/20-backend-package-restructure/20-03-PLAN.md
@.planning/phases/20-backend-package-restructure/20-04-PLAN.md
</context>

<tasks>

<task id="20-05-01" type="execute">
  <name>Create backend migration guide</name>
  <files>.planning/phases/20-backend-package-restructure/20-BACKEND-MIGRATION.md</files>
  <read_first>.planning/phases/20-backend-package-restructure/20-CONTEXT.md, backend/app, backend/alembic/env.py, backend/tests/test_package_structure.py</read_first>
  <action>
Create `.planning/phases/20-backend-package-restructure/20-BACKEND-MIGRATION.md` with:

- phase boundary and non-goals
- target package tree summary
- old-to-new backend path map
- compatibility shim inventory
- owner/removal condition/target removal phase for every remaining shim
- list of low-risk shims removed in Phase 20
- Alembic metadata/import notes
- runtime target notes for `app.api.main:app` and `app.main:app`
- verification command results table with command, result, blocker, fallback, and notes
- Phase 22 handoff notes for runtime/Docker/Azure verification
  </action>
  <acceptance_criteria>
    - Migration guide exists
    - It contains `## Old-to-New Path Map`
    - It contains `## Compatibility Shims`
    - It contains `## Verification Results`
    - It names `app.main`, `app.models`, and `app.schemas`
    - Every remaining shim has owner, removal condition, and target removal phase
  </acceptance_criteria>
  <verify>`rtk proxy rg "## Old-to-New Path Map|## Compatibility Shims|## Verification Results|app.main|app.models|app.schemas|removal condition" .planning/phases/20-backend-package-restructure/20-BACKEND-MIGRATION.md`</verify>
  <done>Migration guide documents path map, shims, verification, and Phase 22 handoff.</done>
</task>

<task id="20-05-02" type="execute">
  <name>Remove only safe low-risk shims and preserve high-risk surfaces</name>
  <files>backend/app/</files>
  <read_first>backend/app, backend/tests/test_package_structure.py, .planning/phases/20-backend-package-restructure/20-BACKEND-MIGRATION.md</read_first>
  <action>
Review remaining compatibility delegates.

Rules:

- Keep high-risk compatibility surfaces through Phase 22: `app.main`, `app.models`, and `app.schemas`.
- Keep any delegate needed by Alembic, runtime, scripts, or tests until Phase 22 verifies integrated runtime behavior.
- Remove only delegates that are clearly unused by code, tests, runtime references, and migration imports.
- Record every removed shim and every kept shim in the migration guide.

Do not remove a shim just to make the tree look cleaner if verification does not prove it is safe.
  </action>
  <acceptance_criteria>
    - `from app.main import app` still works
    - `from app.models import User` still works
    - `from app.schemas import UserOut` still works
    - Migration guide lists kept and removed shims
  </acceptance_criteria>
  <verify>`cd backend && rtk proxy python -c "from app.main import app; from app.models import User; from app.schemas import UserOut; assert app and User and UserOut"`</verify>
  <done>Only safe low-risk shims are removed; high-risk compatibility remains protected.</done>
</task>

<task id="20-05-03" type="execute">
  <name>Run final backend tests and import checks</name>
  <files>.planning/phases/20-backend-package-restructure/20-BACKEND-MIGRATION.md</files>
  <read_first>backend/tests, backend/app, .planning/phases/20-backend-package-restructure/20-VALIDATION.md</read_first>
  <action>
Run and record the final backend verification commands:

- `rtk proxy python -m compileall backend/app`
- `rtk pytest backend/tests -q`
- canonical/compatibility import smoke for app, models, schemas, config, database, scheduler, and socket manager
- router registration/import check for `/health`, `/ws/chat`, and representative `/api/*` routes

If a command is blocked by missing dependencies or environment configuration, record exact command, blocker, and fallback in `20-BACKEND-MIGRATION.md`.
  </action>
  <acceptance_criteria>
    - Compile check passes or blocker/fallback is recorded
    - Backend tests pass or blocker/fallback is recorded
    - Import smoke passes
    - Router registration check passes
    - Results are recorded in migration guide
  </acceptance_criteria>
  <verify>`rtk proxy python -m compileall backend/app` and `rtk pytest backend/tests -q`</verify>
  <done>Final backend regression and import checks are green or precisely documented with fallbacks.</done>
</task>

<task id="20-05-04" type="execute">
  <name>Run Alembic validation and `/health` startup smoke</name>
  <files>.planning/phases/20-backend-package-restructure/20-BACKEND-MIGRATION.md</files>
  <read_first>backend/alembic.ini, backend/alembic/env.py, backend/app/api/main.py, backend/app/main.py</read_first>
  <action>
Run or document final runtime/migration checks:

- Alembic heads check from `backend`
- Alembic metadata import check if DB upgrade cannot run locally
- uvicorn startup smoke for `/health` using the compatibility target `app.main:app`
- optional canonical target smoke for `app.api.main:app`

For uvicorn smoke, use a short-lived process, request `/health`, confirm `{"status":"ok"}`, then stop the process. If process startup is blocked by missing env/DB/dependencies, record blocker and run router registration import fallback.
  </action>
  <acceptance_criteria>
    - Alembic check passes or blocker/fallback is recorded
    - `/health` smoke passes for `app.main:app` or blocker/fallback is recorded
    - Canonical `app.api.main:app` import is validated
    - Migration guide records command results
  </acceptance_criteria>
  <verify>`cd backend && rtk proxy alembic -c alembic.ini heads` and `cd backend && rtk proxy python -c "from app.api.main import app; from app.main import app as compat_app; assert app is compat_app"`</verify>
  <done>Alembic and startup smoke are validated or documented with exact blockers and fallbacks.</done>
</task>

<task id="20-05-05" type="execute">
  <name>Final traceability check against Phase 20 decisions</name>
  <files>.planning/phases/20-backend-package-restructure/20-BACKEND-MIGRATION.md</files>
  <read_first>.planning/phases/20-backend-package-restructure/20-CONTEXT.md, .planning/phases/20-backend-package-restructure/20-BACKEND-MIGRATION.md, backend/app</read_first>
  <action>
Review Phase 20 CONTEXT decisions D-01 through D-22 and confirm each is represented by code, compatibility coverage, migration guide notes, or explicit planner discretion.

Add a `## Decision Traceability` section to the migration guide mapping D-01 through D-22 to:

- package tree
- app target
- model package
- schema package
- shim inventory
- test/import coverage
- verification results
- Phase 22 handoff
  </action>
  <acceptance_criteria>
    - Migration guide contains `## Decision Traceability`
    - D-01 through D-22 are each named
    - Any decision left to discretion has a short outcome note
    - No Phase 20 decision is silently dropped
  </acceptance_criteria>
  <verify>`rtk proxy rg "## Decision Traceability|D-01|D-22" .planning/phases/20-backend-package-restructure/20-BACKEND-MIGRATION.md`</verify>
  <done>Phase 20 migration guide proves decisions were translated into implementation or handoff notes.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-16 | Repudiation | Verification results | mitigate | Record command, result, blocker, fallback, and notes in migration guide |
| T-20-17 | Availability | Runtime startup | mitigate | Validate `app.main:app` and canonical app imports; smoke `/health` where possible |
| T-20-18 | Availability | Alembic migration path | mitigate | Run Alembic heads/metadata checks and document blockers/fallbacks |
| T-20-19 | Maintainability | Temporary shims | mitigate | Require owner, removal condition, and target removal phase for each remaining shim |
| T-20-20 | Integrity | Discuss-phase decision coverage | mitigate | Add D-01 through D-22 traceability to migration guide |
</threat_model>

<verification>
- `rtk proxy python -m compileall backend/app`
- `rtk pytest backend/tests -q`
- `cd backend && rtk proxy alembic -c alembic.ini heads`
- `cd backend && rtk proxy python -c "from app.api.main import app; from app.main import app as compat_app; assert app is compat_app"`
- Uvicorn `/health` smoke for `app.main:app`, or documented blocker plus router registration fallback
- `rtk proxy rg "## Compatibility Shims|## Verification Results|## Decision Traceability|D-22" .planning/phases/20-backend-package-restructure/20-BACKEND-MIGRATION.md`
</verification>

<success_criteria>
- Backend migration guide documents old-to-new paths, shims, verification, and Phase 22 handoff.
- High-risk compatibility surfaces remain valid.
- Low-risk shims are removed only when proven safe.
- Backend tests, Alembic validation, and `/health` startup smoke pass or have exact blocker/fallback notes.
- All Phase 20 context decisions are traceable.
</success_criteria>
