---
phase: "14-sprint-model"
plan: 4
type: execute
wave: 4
depends_on: ["14-03"]
files_modified:
  - "frontend/src/lib/components/tasks/KanbanBoard.svelte"
  - "frontend/src/routes/tasks/+page.svelte"
  - "frontend/src/lib/components/tasks/AiTaskInput.svelte"
autonomous: true
requirements: ["SPRINT-03", "SPRINT-04"]
must_haves:
  truths:
    - "Kanban board displays a Backlog column for unassigned tasks"
    - "Dragging a task to Backlog removes its sprint assignment"
    - "Tasks page allows filtering by sprint"
  artifacts:
    - path: "frontend/src/lib/components/tasks/KanbanBoard.svelte"
      provides: "Backlog droppable zone"
      contains: "function handleTaskMove"
    - path: "frontend/src/routes/tasks/+page.svelte"
      provides: "Sprint selector integration"
      contains: "<select"
  key_links:
    - from: "frontend/src/lib/components/tasks/KanbanBoard.svelte"
      to: "frontend/src/routes/tasks/+page.svelte"
      via: "task move event dispatcher"
      pattern: "dispatch('taskMove'"
---

<objective>
Integrate Sprint interactions into the Kanban Board and Task page.

Purpose: Bring the Sprint model to life in the user interface, enabling users to view, assign, and drag-and-drop tasks between the Backlog and active Sprints.
Output: Updated Kanban board with Backlog column, task page with sprint selector.
</objective>

<execution_context>
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-sprint-model/14-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update KanbanBoard with Backlog Column</name>
  <files>frontend/src/lib/components/tasks/KanbanBoard.svelte</files>
  <read_first>frontend/src/lib/components/tasks/KanbanBoard.svelte</read_first>
  <action>
1. Modify `KanbanBoard.svelte` to accept an `activeSprintId` prop (number | null) and a `backlogTasks` prop (array).
2. Render a fixed "Backlog" column on the far-left of the board (before the status columns). It uses `svelte-dnd-action` droppable zone, tracking items from `backlogTasks`.
3. Add a new `createEventDispatcher` called `taskMove`.
4. Update drop logic: 
   - If a task is dropped into the Backlog column, emit `dispatch('taskMove', { id: taskId, sprint_id: null, status: currentStatus })`.
   - If a task is dropped into a status column, emit `dispatch('taskMove', { id: taskId, sprint_id: activeSprintId, status: targetStatus })`.
5. Remove the old `onStatusChange` callback usage in favor of the new `taskMove` event listener.
  </action>
  <acceptance_criteria>
    - `grep "dispatch('taskMove'" frontend/src/lib/components/tasks/KanbanBoard.svelte` must return matches for both backlog drops and status drops
    - `grep "Backlog" frontend/src/lib/components/tasks/KanbanBoard.svelte` must return a match showing the column title
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Integrate Sprints into Tasks Page</name>
  <files>frontend/src/routes/tasks/+page.svelte, frontend/src/lib/components/tasks/AiTaskInput.svelte</files>
  <read_first>frontend/src/routes/tasks/+page.svelte, frontend/src/lib/components/tasks/KanbanBoard.svelte</read_first>
  <action>
1. In `tasks/+page.svelte`, fetch `sprints` from `api.sprints.list({ project_id })`.
2. Add a `<select>` dropdown to choose an active sprint from the fetched list. Bind this to `selectedSprintId`.
3. Fetch tasks for the selected sprint (`api.tasks.list({ sprint_id: selectedSprintId })`) and fetch backlog tasks (`api.tasks.list({ unassigned: true, project_id })`). Pass these as `tasks` and `backlogTasks` to `KanbanBoard`.
4. Handle the `on:taskMove` event from `KanbanBoard` by calling `api.tasks.update(e.detail.id, { status: e.detail.status, sprint_id: e.detail.sprint_id })` and refreshing task lists.
5. Provide a "Close Sprint" button that triggers the `SprintCloseModal` (from 14-03).
6. In `AiTaskInput.svelte` (if used on this page), pass `selectedSprintId` into its context or payload so new AI-created tasks land in the active sprint if one is selected.
  </action>
  <acceptance_criteria>
    - `grep "sprint_id: e.detail.sprint_id" frontend/src/routes/tasks/+page.svelte` must return a match
    - `grep "<select" frontend/src/routes/tasks/+page.svelte` must return a match corresponding to the sprint selector
    - `grep "backlogTasks" frontend/src/routes/tasks/+page.svelte` must return a match
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI | Client initiates data mutations and component rendering, but state integrity is handled backend-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Tampering | Kanban Board | mitigate | Backend ensures user has write access to the tasks being dragged across sprints |
</threat_model>

<verification>
- Changing the sprint selector updates the displayed tasks.
- Dragging a task into the backlog updates its `sprint_id` to null on the backend.
</verification>

<success_criteria>
- The sprint board filters correctly.
- The backlog column integrates fluidly into the Kanban view.
- Sprint tasks can be created and manipulated through drag and drop.
</success_criteria>

<output>
After completion, create `.planning/phases/14-sprint-model/14-04-SUMMARY.md`
</output>
