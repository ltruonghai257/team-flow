---
phase: "14-sprint-model"
plan: 2
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - "backend/app/schemas.py"
  - "backend/app/routers/sprints.py"
  - "backend/app/routers/tasks.py"
  - "backend/app/main.py"
autonomous: true
requirements: ["SPRINT-01", "SPRINT-03"]
must_haves:
  truths:
    - "Sprint API endpoints exist for CRUD and bulk close"
    - "Task API supports sprint_id filtering and assignment"
  artifacts:
    - path: "backend/app/routers/sprints.py"
      provides: "Sprint CRUD and close endpoints"
      contains: "@router.post(\"/{sprint_id}/close\")"
    - path: "backend/app/schemas.py"
      provides: "Sprint validation schemas"
      contains: "class SprintCreate(BaseModel):"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/routers/sprints.py"
      via: "app.include_router(sprints.router)"
      pattern: "sprints.router"
---

<objective>
Implement the backend API endpoints and validation schemas for the Sprint Model.

Purpose: Allow the frontend to create, read, update, and close sprints, and to filter/assign tasks to sprints.
Output: New `sprints.py` router, updated `schemas.py`, updated `tasks.py` filtering, and updated `main.py`.
</objective>

<execution_context>
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-sprint-model/14-CONTEXT.md
@.planning/phases/14-sprint-model/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Sprint schemas and update Task schemas</name>
  <files>backend/app/schemas.py</files>
  <read_first>backend/app/schemas.py, backend/app/models.py</read_first>
  <action>
1. Import `SprintStatus` from `app.models`.
2. Add the following Sprint schemas (using Pydantic `BaseModel`):
   - `SprintBase`: `name: str`, `start_date: Optional[datetime]`, `end_date: Optional[datetime]`, `status: SprintStatus`, `milestone_id: int`
   - `SprintCreate`: inherits `SprintBase`, adds `_normalize_dates = field_validator("start_date", "end_date", mode="after")(_to_naive_utc)`
   - `SprintUpdate`: `name: Optional[str]`, `start_date: Optional[datetime]`, `end_date: Optional[datetime]`, `status: Optional[SprintStatus]`, with date normalization.
   - `SprintOut`: inherits `SprintBase`, adds `id: int`, `created_at: datetime`.
   - `SprintClosePayload`: `task_mapping: Dict[int, Optional[int]]` (task_id -> new_sprint_id or None for Backlog).
3. Update `TaskBase` or `TaskCreate`, `TaskUpdate`, and `TaskOut` to include `sprint_id: Optional[int] = None`.
  </action>
  <acceptance_criteria>
    - `grep "class SprintCreate(BaseModel):" backend/app/schemas.py` must return a match
    - `grep "task_mapping: Dict\[int, Optional\[int\]\]" backend/app/schemas.py` must return a match
    - `grep "sprint_id: Optional\[int\]" backend/app/schemas.py` must return matches in Task schemas
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create Sprints router and update main</name>
  <files>backend/app/routers/sprints.py, backend/app/main.py</files>
  <read_first>backend/app/routers/milestones.py, backend/app/main.py</read_first>
  <action>
1. Create `backend/app/routers/sprints.py`. Pattern it after `milestones.py`.
2. Implement endpoints:
   - `GET /` (list sprints, filter by `project_id` and `milestone_id`, scoped to `get_sub_team`)
   - `POST /` (create sprint)
   - `GET /{sprint_id}` (get sprint)
   - `PATCH /{sprint_id}` (update sprint)
   - `DELETE /{sprint_id}` (delete sprint)
   - `POST /{sprint_id}/close` (takes `SprintClosePayload`, sets sprint status to closed, and bulk updates the `sprint_id` of tasks in `task_mapping`).
3. Update `backend/app/main.py` to import `sprints` from `app.routers` and add `app.include_router(sprints.router, prefix="/api/sprints", tags=["sprints"])`.
  </action>
  <acceptance_criteria>
    - `grep "@router.post(\"/{sprint_id}/close\")" backend/app/routers/sprints.py` must return a match
    - `grep "app.include_router(sprints.router" backend/app/main.py` must return a match
    - `sprints.py` must use `get_sub_team` context for query authorization
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update Tasks router to handle Sprint filtering</name>
  <files>backend/app/routers/tasks.py</files>
  <read_first>backend/app/routers/tasks.py</read_first>
  <action>
1. In `backend/app/routers/tasks.py`, update `list_tasks` endpoint signature to accept `sprint_id: Optional[int] = None` and `unassigned: bool = False`.
2. In the query building logic inside `list_tasks`:
   - If `sprint_id` is provided, append `.where(Task.sprint_id == sprint_id)`.
   - If `unassigned` is True, append `.where(Task.sprint_id == None)`.
3. The existing patch endpoint `update_task` should naturally handle `sprint_id` since it uses `payload.model_dump(exclude_unset=True)`. Ensure it does not block `sprint_id` updates.
  </action>
  <acceptance_criteria>
    - `grep "sprint_id: Optional\[int\] = None" backend/app/routers/tasks.py` must return a match in list_tasks
    - `grep "unassigned: bool = False" backend/app/routers/tasks.py` must return a match
    - `grep "Task.sprint_id == None" backend/app/routers/tasks.py` must return a match
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Router | Validates incoming payloads to ensure correct typing and format |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Information Disclosure | Sprints Router | mitigate | Ensure `get_sub_team` is used to limit sprints to the user's active sub_team |
| T-14-03 | Denial of Service | Sprint Close | mitigate | Schema limits `task_mapping` dictionary size inherently by JSON limits, or add max_length if required |
</threat_model>

<verification>
- `pytest tests/` passes successfully for task schemas.
- `GET /api/sprints/` correctly requires authentication and returns sprints.
</verification>

<success_criteria>
- Sprints CRUD is fully functional via API.
- Bulk update of tasks during Sprint close works successfully.
- Task API properly filters by sprint and handles backlog (unassigned) queries.
</success_criteria>

<output>
After completion, create `.planning/phases/14-sprint-model/14-02-SUMMARY.md`
</output>
