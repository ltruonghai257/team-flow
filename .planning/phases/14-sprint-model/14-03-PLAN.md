---
phase: "14-sprint-model"
plan: 3
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - "frontend/src/lib/api.ts"
  - "frontend/src/lib/components/sprints/SprintForm.svelte"
  - "frontend/src/lib/components/sprints/SprintCloseModal.svelte"
autonomous: true
requirements: ["SPRINT-01", "SPRINT-03"]
must_haves:
  truths:
    - "Frontend API client includes sprint methods"
    - "Sprint forms handle creation and display warnings for overlapping dates"
    - "Sprint Close modal provides task reassignment UI"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "Sprint API integration"
      contains: "export const sprints = {"
    - path: "frontend/src/lib/components/sprints/SprintCloseModal.svelte"
      provides: "Sprint close bulk reassignment UI"
      contains: "function handleSubmit()"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "backend"
      via: "api/sprints"
      pattern: "/sprints"
---

<objective>
Implement frontend API integration and create core Sprint UI components.

Purpose: Allow the frontend application to interact with Sprint APIs, and provide the user interfaces needed to create/edit sprints and close them.
Output: Updated `api.ts`, and two new reusable Svelte components (`SprintForm` and `SprintCloseModal`).
</objective>

<execution_context>
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-sprint-model/14-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Sprints to Frontend API client</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>frontend/src/lib/api.ts</read_first>
  <action>
1. Add a new `sprints` object mirroring the `milestones` pattern:
   - `list: (params?: Record<string, any>) => request('/sprints/' + buildQueryString(params))`
   - `get: (id: number) => request(\`/sprints/${id}\`)`
   - `create: (data: object) => request('/sprints/', { method: 'POST', body: JSON.stringify(data) })`
   - `update: (id: number, data: object) => request(\`/sprints/${id}\`, { method: 'PATCH', body: JSON.stringify(data) })`
   - `delete: (id: number) => request(\`/sprints/${id}\`, { method: 'DELETE' })`
   - `close: (id: number, data: object) => request(\`/sprints/${id}/close\`, { method: 'POST', body: JSON.stringify(data) })`
   (Use existing `request` wrapper or query string utility if available).
2. Ensure the `tasks.list` function (or equivalent) in `api.ts` accepts `sprint_id` and `unassigned` if query parameters are hardcoded, otherwise update to accept an extensible `params` object.
  </action>
  <acceptance_criteria>
    - `grep "export const sprints = {" frontend/src/lib/api.ts` must return a match
    - `grep "close:" frontend/src/lib/api.ts` inside sprints block must return a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create SprintForm component with date warnings</name>
  <files>frontend/src/lib/components/sprints/SprintForm.svelte</files>
  <read_first>frontend/src/lib/components/milestones/MilestoneForm.svelte</read_first>
  <action>
1. Create a `SprintForm.svelte` component (based on the layout of other forms like `MilestoneForm` or `ProjectForm` if available).
2. Props: `sprint` (optional, for editing), `milestoneId`, `milestones` (array of milestone dates), `existingSprints` (array).
3. Fields: `name`, `start_date`, `end_date`, `status` (select).
4. Logic: Calculate date overlaps. If `start_date` or `end_date` fall outside the milestone's bounds, or overlap with `existingSprints`, display a non-blocking warning message (e.g. text in yellow or red).
5. Submit event should trigger the respective `api.sprints.create` or `api.sprints.update` method.
  </action>
  <acceptance_criteria>
    - File `frontend/src/lib/components/sprints/SprintForm.svelte` exists and contains `<form>`
    - Contains logic to check overlapping dates (`new Date(start_date) < new Date(milestone.start_date)`) and display warning
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create SprintCloseModal component</name>
  <files>frontend/src/lib/components/sprints/SprintCloseModal.svelte</files>
  <read_first>frontend/src/lib/components/sprints/SprintForm.svelte</read_first>
  <action>
1. Create `SprintCloseModal.svelte` component.
2. Props: `sprint` (object), `incompleteTasks` (array), `availableSprints` (array of other active/planning sprints), `show` (boolean bindable).
3. UI: 
   - A modal overlay.
   - List each task from `incompleteTasks` with its title and a `<select>` dropdown.
   - Dropdown options: `Backlog` (value `null`), and each sprint in `availableSprints` (value `sprint.id`). Default to Backlog.
4. Submission: Constructs a `task_mapping: { [taskId]: selectedSprintId }` dictionary. Calls `api.sprints.close(sprint.id, { task_mapping })` and closes the modal, triggering a refresh event on success.
  </action>
  <acceptance_criteria>
    - File `frontend/src/lib/components/sprints/SprintCloseModal.svelte` exists
    - `grep "task_mapping" frontend/src/lib/components/sprints/SprintCloseModal.svelte` must return a match
    - Lists tasks with a `<select>` for assigning to Backlog or Sprints
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI | User inputs are processed by UI components before API transmission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Spoofing | Sprint Close Modal | mitigate | User can only map tasks they have permission to see, backed by API sub-team checks |
</threat_model>

<verification>
- `api.ts` correctly exposes sprints endpoints
- Components compile via SvelteKit without errors (`npm run build` locally passes)
</verification>

<success_criteria>
- Sprints can be interacted with via the frontend API layer
- Form handles creation and overlap warning gracefully
- Sprint close modal logic produces a correct dictionary mapping
</success_criteria>

<output>
After completion, create `.planning/phases/14-sprint-model/14-03-SUMMARY.md`
</output>
