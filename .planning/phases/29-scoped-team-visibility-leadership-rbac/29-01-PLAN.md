---
phase: 29-scoped-team-visibility-leadership-rbac
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/enums.py
  - backend/app/models/users.py
  - backend/app/schemas/users.py
  - backend/app/schemas/teams.py
  - backend/app/utils/auth.py
  - backend/app/services/visibility.py
  - backend/app/services/__init__.py
  - backend/alembic/versions/<new_phase_29_role_scope_migration>.py
  - backend/tests/test_visibility.py
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04
  - VIS-07
user_setup: []
must_haves:
  truths:
    - "D-01 D-02 D-03 D-04: the active role model is exactly `manager`, `supervisor`, `assistant_manager`, and `member`; remove `admin` from active code paths rather than preserving old-version compatibility."
    - "D-05 D-06 D-07: `manager` is the top role with org-wide visibility and admin-like management authority."
    - "D-08 D-09 D-10 D-11 D-12: supervisors and assistant managers share the same Phase 29 visibility model; they see scoped members plus peer leaders, while members remain sub-team-only."
    - "D-16 D-17 D-18: backend visibility helpers are the source of truth; frontend filtering will mirror them later but cannot be relied on for security."
    - "VIS-07: because the application is unreleased, implement a clean role/scope schema update without production backfill or dual-role compatibility paths."
  artifacts:
    - path: "backend/app/models/enums.py"
      provides: "UserRole enum with exactly manager, supervisor, assistant_manager, member"
      min_lines: 8
      contains: "assistant_manager"
    - path: "backend/app/services/visibility.py"
      provides: "Shared visibility predicates and scoped query helpers for all later routers"
      min_lines: 100
      contains: "visible"
    - path: "backend/app/utils/auth.py"
      provides: "Role guard dependencies updated for manager and leadership roles"
      min_lines: 80
      contains: "require_manager"
    - path: "backend/tests/test_visibility.py"
      provides: "Role/scope regression tests for member, supervisor, assistant manager, and manager visibility"
      min_lines: 120
      contains: "test_"
  key_links:
    - from: "backend/app/utils/auth.py"
      to: "backend/app/services/visibility.py"
      via: "auth dependencies call shared role/scope helpers"
      pattern: "visibility"
    - from: "backend/tests/test_visibility.py"
      to: "backend/app/services/visibility.py"
      via: "tests exercise visible user/team helpers before router rollout"
      pattern: "assistant_manager"
---

<objective>
Build the shared role and visibility foundation for Phase 29 before updating every product surface.

Purpose: Replace the unreleased `admin` role shape with the locked manager/supervisor/assistant-manager/member model and create one reusable backend source of truth for scoped visibility.
Output: updated role enum/schema/auth helpers, shared visibility service, clean schema migration if needed, and focused backend visibility tests.
</objective>

<execution_context>
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-scoped-team-visibility-leadership-rbac/29-CONTEXT.md
@.planning/phases/29-scoped-team-visibility-leadership-rbac/29-RESEARCH.md
@.planning/phases/29-scoped-team-visibility-leadership-rbac/29-VALIDATION.md
@backend/app/models/enums.py
@backend/app/models/users.py
@backend/app/schemas/users.py
@backend/app/schemas/teams.py
@backend/app/utils/auth.py
@backend/tests/conftest.py
@backend/tests/test_sub_teams.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the active backend role enum and schema types</name>
  <files>backend/app/models/enums.py, backend/app/models/users.py, backend/app/schemas/users.py, backend/app/schemas/teams.py, backend/alembic/versions/&lt;new_phase_29_role_scope_migration&gt;.py</files>
  <read_first>backend/app/models/enums.py, backend/app/models/users.py, backend/app/schemas/users.py, backend/app/schemas/teams.py, backend/alembic/versions, .planning/phases/29-scoped-team-visibility-leadership-rbac/29-CONTEXT.md</read_first>
  <action>Update `UserRole` so the active values are exactly `manager`, `supervisor`, `assistant_manager`, and `member`. Remove `admin` as an active role value from backend schemas and examples. If database enum shape requires an Alembic migration, create a clean pre-release schema migration that establishes the new enum values without dual-role compatibility logic or production backfill ceremony. Keep existing `User.sub_team_id`, `SubTeam.supervisor_id`, and `TeamInvite.sub_team_id` fields available for Phase 29 scope rules unless implementation finds a simpler clean replacement.</action>
  <verify><automated>cd backend && rtk uv run pytest tests/test_visibility.py -q</automated></verify>
  <acceptance_criteria>
    - `backend/app/models/enums.py` defines `UserRole.manager`, `UserRole.supervisor`, `UserRole.assistant_manager`, and `UserRole.member`
    - no active backend schema still offers `UserRole.admin`
    - any migration created is clean and does not preserve old-version compatibility branches
    - tests can import the new enum values
  </acceptance_criteria>
  <done>The backend type system has the Phase 29 role vocabulary and no active `admin` role path.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared visibility helpers for role and scope decisions</name>
  <files>backend/app/services/visibility.py, backend/app/services/__init__.py</files>
  <read_first>backend/app/services/knowledge_sessions.py, backend/app/utils/auth.py, backend/app/models/users.py, .planning/phases/29-scoped-team-visibility-leadership-rbac/29-CONTEXT.md</read_first>
  <action>Create `backend/app/services/visibility.py` with shared helpers for manager checks, leadership checks, member checks, visible sub-team IDs, visible user predicates, peer-leader selection, and scoped SQLAlchemy filters that later routers can reuse. Encode the locked model: managers see all; members see their own sub-team; supervisors and assistant managers see scoped members plus peer leaders in the same parent/team scope. Keep helper names boring and explicit so later plans can apply them consistently.</action>
  <verify><automated>cd backend && rtk uv run pytest tests/test_visibility.py -q</automated></verify>
  <acceptance_criteria>
    - visibility helpers distinguish `manager`, `supervisor`, `assistant_manager`, and `member`
    - helpers expose a reusable way to filter visible users and sub-team data
    - peer leader behavior includes supervisors and assistant managers in the same allowed scope
    - managers can intentionally request all-team visibility
  </acceptance_criteria>
  <done>Later routers have one backend source of truth for Phase 29 visibility decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Update auth dependencies to use the shared role model</name>
  <files>backend/app/utils/auth.py</files>
  <read_first>backend/app/utils/auth.py, backend/app/services/visibility.py, backend/app/models/users.py, backend/tests/conftest.py</read_first>
  <action>Replace old `require_admin`, `require_supervisor`, and `require_supervisor_or_admin` semantics with Phase 29-aware dependencies such as `require_manager`, `require_leader_or_manager`, and compatibility-free call sites that can be updated by later plans. Update `get_sub_team` or add a new explicit scope dependency so managers can choose all teams or a selected sub-team, members default to their sub-team, and supervisors/assistant managers resolve their allowed scope through visibility helpers.</action>
  <verify><automated>cd backend && rtk uv run pytest tests/test_visibility.py -q</automated></verify>
  <acceptance_criteria>
    - `backend/app/utils/auth.py` contains manager and leader guard dependencies
    - old admin-only language is removed from dependency behavior
    - member scope resolution remains implicit and sub-team-only
    - manager all-team behavior remains explicit and testable
  </acceptance_criteria>
  <done>Backend route dependencies can enforce the new role model without re-implementing role checks.</done>
</task>

<task type="auto">
  <name>Task 4: Lock the foundation with backend visibility tests</name>
  <files>backend/tests/test_visibility.py</files>
  <read_first>backend/tests/conftest.py, backend/tests/test_sub_teams.py, backend/app/services/visibility.py, backend/app/utils/auth.py</read_first>
  <action>Create `backend/tests/test_visibility.py` with fixtures for at least two sub-teams, one manager, one supervisor, one assistant manager, peer leaders, and members. Test that members see only their own sub-team users/data, supervisors and assistant managers see scoped members plus peer leaders, managers see everyone, and obsolete `admin` assumptions are absent from active role checks.</action>
  <verify><automated>cd backend && rtk uv run pytest tests/test_visibility.py -q</automated></verify>
  <acceptance_criteria>
    - tests cover all four active roles
    - tests cover peer leader visibility
    - tests cover manager all-team visibility
    - tests cover member sub-team-only visibility
    - `cd backend && rtk uv run pytest tests/test_visibility.py -q` exits 0
  </acceptance_criteria>
  <done>The Phase 29 role/scope foundation is covered before broad router changes begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/auth token to backend role helpers | Untrusted requests claim identity through auth cookies or bearer tokens before route guards decide access |
| current user to scoped SQL queries | Role and scope helpers decide which rows can be read or modified |
| schema migration to active role enum | Role vocabulary changes affect every authenticated route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | E | `backend/app/models/enums.py`, `backend/app/utils/auth.py` | mitigate | Remove `admin` from active code paths and replace privileged checks with manager/leader dependencies covered by tests |
| T-29-02 | I | `backend/app/services/visibility.py` | mitigate | Centralize visible-user and visible-sub-team logic, then test all four roles and peer-leader cases before router rollout |
| T-29-03 | T | role enum migration | mitigate | Use a clean pre-release enum/schema update and avoid dual-role compatibility branches that could preserve stale privilege assumptions |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd backend && rtk uv run pytest tests/test_visibility.py -q`
- [ ] `backend/app/models/enums.py` contains `assistant_manager`
- [ ] `backend/app/utils/auth.py` contains manager/leader guard dependencies
- [ ] `backend/app/services/visibility.py` exists and is imported by auth/tests
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Active backend role vocabulary is exactly manager/supervisor/assistant_manager/member
- Shared visibility helpers encode member, leader, peer-leader, and manager scope behavior
- No old-version `admin` compatibility path remains in the foundation
</success_criteria>

<output>
After completion, create `.planning/phases/29-scoped-team-visibility-leadership-rbac/29-01-SUMMARY.md`
</output>
