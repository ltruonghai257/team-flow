---
phase: 29-scoped-team-visibility-leadership-rbac
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01-PLAN.md
files_modified:
  - backend/app/routers/users.py
  - backend/app/routers/sub_teams.py
  - backend/app/routers/invites.py
  - backend/app/schemas/users.py
  - backend/app/schemas/teams.py
  - backend/app/services/visibility.py
  - backend/tests/test_visibility.py
  - backend/tests/test_sub_teams.py
autonomous: true
requirements:
  - VIS-02
  - VIS-03
  - VIS-04
  - VIS-06
  - VIS-07
user_setup: []
must_haves:
  truths:
    - "D-05 D-06 D-07: managers can manage users, roles, scopes, teams, invites, and privileged surfaces across all teams."
    - "D-13: only managers can assign leadership roles: `manager`, `supervisor`, and `assistant_manager`."
    - "D-14: supervisors and assistant managers can invite or manage `member` users only inside their allowed scope."
    - "D-15: invite and team-management flows must encode role and scope data without adding matrix management or many-to-many reporting."
    - "D-18: out-of-scope user/team/invite records should be filtered or return 404/403; do not reveal hidden team/user existence through disabled data."
  artifacts:
    - path: "backend/app/routers/invites.py"
      provides: "Manager-only leadership assignment and scoped member invite/direct-add behavior"
      min_lines: 120
      contains: "assistant_manager"
    - path: "backend/app/routers/users.py"
      provides: "Scoped user list/detail/update behavior for the four-role model"
      min_lines: 80
      contains: "visible"
    - path: "backend/app/routers/sub_teams.py"
      provides: "Scoped team management behavior using Phase 29 visibility helpers"
      min_lines: 120
      contains: "manager"
    - path: "backend/tests/test_visibility.py"
      provides: "Regression coverage for team/invite/user management scope"
      min_lines: 200
      contains: "invite"
  key_links:
    - from: "backend/app/routers/invites.py"
      to: "backend/app/services/visibility.py"
      via: "role assignment and invite scope checks"
      pattern: "can_assign"
    - from: "backend/app/routers/users.py"
      to: "backend/app/services/visibility.py"
      via: "visible user lookup for list/detail/update"
      pattern: "visible"
---

<objective>
Update user, team, and invite management so the new leadership role model can be assigned and enforced safely.

Purpose: Make VIS-06 real without allowing supervisors or assistant managers to grant leadership privileges outside their scope.
Output: scoped user/team/invite endpoints, role/scope request and response support, and backend tests for manager-only leadership assignment.
</objective>

<execution_context>
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-scoped-team-visibility-leadership-rbac/29-CONTEXT.md
@.planning/phases/29-scoped-team-visibility-leadership-rbac/29-RESEARCH.md
@.planning/phases/29-scoped-team-visibility-leadership-rbac/29-VALIDATION.md
@.planning/phases/29-scoped-team-visibility-leadership-rbac/29-01-PLAN.md
@backend/app/services/visibility.py
@backend/app/utils/auth.py
@backend/app/routers/users.py
@backend/app/routers/sub_teams.py
@backend/app/routers/invites.py
@backend/app/schemas/users.py
@backend/app/schemas/teams.py
@backend/tests/test_visibility.py
@backend/tests/test_sub_teams.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope user list, detail, and role update endpoints</name>
  <files>backend/app/routers/users.py, backend/app/schemas/users.py, backend/tests/test_visibility.py</files>
  <read_first>backend/app/routers/users.py, backend/app/schemas/users.py, backend/app/services/visibility.py, backend/tests/test_visibility.py</read_first>
  <action>Update user endpoints so list, detail, update, and role/scope changes all go through Phase 29 visibility helpers. Managers can see and manage everyone. Supervisors and assistant managers can see scoped members plus peer leaders, but cannot assign leadership roles. Members can see only users in their own sub-team and can only update themselves where existing behavior permits.</action>
  <verify><automated>cd backend && rtk uv run pytest tests/test_visibility.py -q</automated></verify>
  <acceptance_criteria>
    - user list results match each role's allowed visibility
    - out-of-scope user detail/update requests return 404 or 403
    - only managers can assign leadership roles
    - tests cover member, supervisor, assistant manager, and manager behavior
  </acceptance_criteria>
  <done>User management obeys the Phase 29 scope model.</done>
</task>

<task type="auto">
  <name>Task 2: Scope sub-team listing and management</name>
  <files>backend/app/routers/sub_teams.py, backend/app/schemas/teams.py, backend/tests/test_sub_teams.py, backend/tests/test_visibility.py</files>
  <read_first>backend/app/routers/sub_teams.py, backend/app/schemas/teams.py, backend/app/services/visibility.py, backend/tests/test_sub_teams.py</read_first>
  <action>Update sub-team endpoints so managers can list/create/update/delete across all teams, while supervisors and assistant managers see only allowed sub-team scope and cannot create broad management structures. Preserve reminder settings behavior but update manager/supervisor/assistant-manager permission checks and proposal review language to the new role model.</action>
  <verify><automated>cd backend && rtk uv run pytest tests/test_visibility.py tests/test_sub_teams.py -q</automated></verify>
  <acceptance_criteria>
    - managers can list all sub-teams
    - supervisors and assistant managers do not see out-of-scope sub-teams
    - reminder settings proposal review uses manager authority instead of admin
    - existing reminder settings tests are updated to new roles and pass
  </acceptance_criteria>
  <done>Sub-team and reminder settings management use the new leadership model.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce invite and direct-add role/scope rules</name>
  <files>backend/app/routers/invites.py, backend/app/schemas/teams.py, backend/tests/test_visibility.py</files>
  <read_first>backend/app/routers/invites.py, backend/app/schemas/teams.py, backend/app/services/visibility.py, backend/tests/test_visibility.py</read_first>
  <action>Update invite and direct-add flows so managers can invite or assign any valid role/scope, while supervisors and assistant managers can only invite/direct-add `member` users inside their allowed scope. Scope pending invite listing and cancellation so leaders cannot see or cancel out-of-scope invites. Ensure invite acceptance assigns the invite's stored role and sub-team/scope cleanly.</action>
  <verify><automated>cd backend && rtk uv run pytest tests/test_visibility.py -q</automated></verify>
  <acceptance_criteria>
    - managers can invite manager/supervisor/assistant_manager/member roles
    - supervisors and assistant managers cannot invite or direct-add leadership roles
    - pending invite list is scoped
    - out-of-scope invite cancellation is blocked
  </acceptance_criteria>
  <done>Invite and direct-add flows encode the Phase 29 role/scope model.</done>
</task>

<task type="auto">
  <name>Task 4: Expand management regression tests</name>
  <files>backend/tests/test_visibility.py, backend/tests/test_sub_teams.py</files>
  <read_first>backend/tests/test_visibility.py, backend/tests/test_sub_teams.py, backend/tests/conftest.py</read_first>
  <action>Add or update tests for manager-only leadership assignment, supervisor/assistant-manager member-only management, scoped pending invites, scoped sub-team listing, and rejection of out-of-scope user/team mutation. Keep tests focused enough to run quickly during execution.</action>
  <verify><automated>cd backend && rtk uv run pytest tests/test_visibility.py tests/test_sub_teams.py -q</automated></verify>
  <acceptance_criteria>
    - management tests cover manager, supervisor, assistant manager, and member paths
    - tests prove leadership assignment is manager-only
    - tests prove scoped leaders cannot manage users/invites outside their scope
    - `cd backend && rtk uv run pytest tests/test_visibility.py tests/test_sub_teams.py -q` exits 0
  </acceptance_criteria>
  <done>Team and invite management behavior is locked by regression tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| leader browser to user/team/invite APIs | Untrusted users attempt to assign roles or inspect teams/invites |
| invite token to accepted user record | Invite acceptance turns stored role and scope into a real account |
| scoped leader to hidden user/team rows | Supervisors and assistant managers may try to mutate out-of-scope records by ID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | E | `backend/app/routers/invites.py` | mitigate | Managers only can assign leadership roles; scoped leaders can assign only members in their scope, with tests for blocked elevation |
| T-29-05 | I | `backend/app/routers/users.py`, `backend/app/routers/sub_teams.py` | mitigate | Apply shared visibility helpers to list and detail endpoints so out-of-scope records are filtered or rejected |
| T-29-06 | T | invite cancellation and direct-add | mitigate | Scope pending invite lookup/cancellation and direct-add targets before mutating records |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd backend && rtk uv run pytest tests/test_visibility.py tests/test_sub_teams.py -q`
- [ ] managers can assign leadership roles in tests
- [ ] supervisors and assistant managers cannot assign leadership roles in tests
- [ ] pending invites and sub-team lists are scoped in tests
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Team, user, and invite management can represent the new leadership scopes
- Leadership role assignment is manager-only
- Supervisors and assistant managers can manage only in-scope members
</success_criteria>

<output>
After completion, create `.planning/phases/29-scoped-team-visibility-leadership-rbac/29-02-SUMMARY.md`
</output>
