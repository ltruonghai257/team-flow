---
phase: 32-dashboard-frontend-redesign
plan: "02"
type: execute
wave: 2
depends_on:
  - 32-01-PLAN.md
files_modified:
  - frontend/src/routes/+page.svelte
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - TASKS-01
  - TASKS-02
  - TASKS-03
  - HEALTH-01
  - HEALTH-02
  - HEALTH-03
  - KPI-01
  - KPI-02
  - KPI-03
  - FEED-01
  - FEED-02
  - FEED-03
must_haves:
  truths:
    - "Member layout: My Tasks + Activity Feed only — no KPI strip, no Team Health panel."
    - "Supervisor/manager layout adds KPI strip above and Team Health panel below the two-column row."
    - "Role gate uses {#if $isManagerOrLeader} from $lib/stores/auth — not a prop, not derived locally."
    - "Overdue row tint: bg-red-950/40. Due-soon row tint: bg-yellow-950/40. Applied as class on the <a> row, not as inline style."
    - "At-risk team health card border: border-red-500/50 when member.status === 'red'."
    - "Status dot mapping: green → bg-green-400, yellow → bg-yellow-400, red → bg-red-400 (matches actual API values)."
    - "completion_rate is 0.0–1.0 — displayed as Math.round(kpi_summary.completion_rate * 100) + '%'."
    - "kpi_summary avg_score color: >= 80 → text-green-400, 60-79 → text-yellow-400, < 60 → text-red-400."
    - "All panels use the .card CSS class. No inline background colors for panels."
    - "All dashboard content is inline in +page.svelte — no new component files."
  artifacts:
    - path: "frontend/src/routes/+page.svelte"
      provides: "Full dashboard rebuild with 4 sections and role-conditional layout"
      min_lines: 120
      contains: "isManagerOrLeader"
    - path: "frontend/src/routes/+page.svelte"
      provides: "My Tasks section with overdue/due-soon tints"
      min_lines: 120
      contains: "bg-red-950/40"
    - path: "frontend/src/routes/+page.svelte"
      provides: "Team Health section with status dot and at-risk border"
      min_lines: 120
      contains: "border-red-500/50"
  key_links:
    - from: "frontend/src/routes/+page.svelte"
      to: "frontend/src/lib/apis/dashboard.ts"
      via: "import { dashboard } from '$lib/apis'; calls dashboard.get()"
      pattern: "dashboard.get()"
    - from: "frontend/src/routes/+page.svelte"
      to: "frontend/src/lib/stores/auth.ts"
      via: "import { isManagerOrLeader } from '$lib/stores/auth'"
      pattern: "isManagerOrLeader"
---

<objective>
Rebuild frontend/src/routes/+page.svelte into the professional role-aware dashboard with four sections: KPI Summary Strip, My Tasks, Activity Feed, and Team Health Panel.

Purpose: Replace the old stat-card dashboard with a layout that renders role-conditional sections from the Phase 31 DashboardPayload. Members see My Tasks + Activity Feed. Supervisors/managers additionally see KPI Summary Strip (top) and Team Health Panel (bottom). All visual decisions are locked in 32-CONTEXT.md and 32-UI-SPEC.md.
Output: Fully rebuilt frontend/src/routes/+page.svelte consuming dashboard.get() with role-conditional rendering.
</objective>

<execution_context>
@/Users/haila/Library/Mobile Documents/com~apple~CloudDocs/PrivateCode/team-flow/.windsurf/get-shit-done/workflows/execute-plan.md
@/Users/haila/Library/Mobile Documents/com~apple~CloudDocs/PrivateCode/team-flow/.windsurf/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-dashboard-frontend-redesign/32-CONTEXT.md
@.planning/phases/32-dashboard-frontend-redesign/32-UI-SPEC.md
@.planning/phases/32-dashboard-frontend-redesign/32-RESEARCH.md
@frontend/src/routes/+page.svelte
@frontend/src/lib/apis/dashboard.ts
@frontend/src/lib/stores/auth.ts
@frontend/src/lib/utils.ts
@frontend/src/lib/components/performance/KpiScoreCard.svelte
@frontend/src/lib/components/updates/StandupCard.svelte
@frontend/src/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild +page.svelte — script block with imports, types, reactive helpers</name>
  <files>frontend/src/routes/+page.svelte</files>
  <read_first>frontend/src/routes/+page.svelte, frontend/src/lib/apis/dashboard.ts, frontend/src/lib/stores/auth.ts, frontend/src/lib/utils.ts, .planning/phases/32-dashboard-frontend-redesign/32-CONTEXT.md</read_first>
  <action>
Replace the entire `<script lang="ts">` block of `frontend/src/routes/+page.svelte` with the following. This replaces all existing imports and variables:

```svelte
<script lang="ts">
	import { onMount } from 'svelte';
	import { dashboard } from '$lib/apis';
	import type { DashboardPayload } from '$lib/apis/dashboard';
	import { timeAgo, priorityColors, statusColors, statusLabels, initials } from '$lib/utils';
	import { isManagerOrLeader } from '$lib/stores/auth';
	import { CheckSquare, Clock, AlertTriangle, Users, TrendingUp } from 'lucide-svelte';

	let stats: DashboardPayload | null = null;
	let loading = true;

	onMount(async () => {
		try {
			stats = await dashboard.get();
		} finally {
			loading = false;
		}
	});

	function activityPreview(fv: Record<string, string>): string {
		const first = Object.values(fv).find((v) => v?.trim());
		return first ? first.substring(0, 120) : '';
	}

	function kpiScoreColor(score: number): string {
		if (score >= 80) return 'text-green-400';
		if (score >= 60) return 'text-yellow-400';
		return 'text-red-400';
	}

	const statusDotClass: Record<string, string> = {
		green: 'bg-green-400',
		yellow: 'bg-yellow-400',
		red: 'bg-red-400'
	};
</script>
```

Verify the script block imports `isManagerOrLeader` (not `isManager` or derived locally), calls `dashboard.get()` (not `dashboard.stats()`), and imports `initials` and `timeAgo` from `$lib/utils`.
  </action>
  <verify><automated>grep -n "isManagerOrLeader\|dashboard.get\|activityPreview\|kpiScoreColor" frontend/src/routes/+page.svelte</automated></verify>
  <acceptance_criteria>
    - `frontend/src/routes/+page.svelte` contains `import { isManagerOrLeader } from '$lib/stores/auth'`
    - `frontend/src/routes/+page.svelte` contains `dashboard.get()`
    - `frontend/src/routes/+page.svelte` contains `import type { DashboardPayload } from '$lib/apis/dashboard'`
    - `frontend/src/routes/+page.svelte` contains `function activityPreview`
    - `frontend/src/routes/+page.svelte` contains `function kpiScoreColor`
    - `frontend/src/routes/+page.svelte` contains `statusDotClass`
    - `frontend/src/routes/+page.svelte` does NOT contain `dashboard.stats()`
  </acceptance_criteria>
  <done>Script block updated with all imports, types, and reactive helpers for the new dashboard.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild +page.svelte — full HTML template (page header, loading state, role-conditional layout)</name>
  <files>frontend/src/routes/+page.svelte</files>
  <read_first>frontend/src/routes/+page.svelte, .planning/phases/32-dashboard-frontend-redesign/32-UI-SPEC.md, .planning/phases/32-dashboard-frontend-redesign/32-CONTEXT.md, frontend/src/lib/components/performance/KpiScoreCard.svelte, frontend/src/lib/components/updates/StandupCard.svelte</read_first>
  <action>
Replace everything after the closing `</script>` tag in `frontend/src/routes/+page.svelte` with the complete template below. This is a full replacement of the HTML portion:

```svelte
<svelte:head><title>Dashboard · TeamFlow</title></svelte:head>

<div class="p-4 md:p-6 max-w-7xl mx-auto">
	<div class="mb-6">
		<h1 class="text-2xl font-bold text-white">Dashboard</h1>
		<p class="text-gray-400 text-sm mt-1">Overview of your projects and team</p>
	</div>

	{#if loading}
		<div class="flex items-center justify-center py-20">
			<div class="w-6 h-6 border-2 border-primary-500 border-t-transparent rounded-full animate-spin"></div>
		</div>
	{:else if stats}
		{#if $isManagerOrLeader && stats.kpi_summary}
			<!-- KPI Summary Strip (supervisor / assistant_manager / manager only — D-12, D-13) -->
			<div class="grid grid-cols-1 sm:grid-cols-3 gap-4 mb-6">
				<!-- Avg Score -->
				<div class="card">
					<div class="flex items-center justify-between mb-3">
						<span class="text-gray-400 text-sm">Avg Score</span>
						<TrendingUp class="text-primary-400" size={18} />
					</div>
					<p class="text-2xl font-semibold {kpiScoreColor(stats.kpi_summary.avg_score)}">{stats.kpi_summary.avg_score}</p>
					<p class="text-xs text-gray-500 mt-1">out of 100</p>
				</div>
				<!-- Completion Rate -->
				<div class="card">
					<div class="flex items-center justify-between mb-3">
						<span class="text-gray-400 text-sm">Completion Rate</span>
						<CheckSquare class="text-primary-400" size={18} />
					</div>
					<p class="text-2xl font-semibold text-white">{Math.round(stats.kpi_summary.completion_rate * 100)}%</p>
					<p class="text-xs text-gray-500 mt-1">last 30 days</p>
				</div>
				<!-- Needs Attention — links to /performance (KPI-03) -->
				<a href="/performance" class="card block hover:border-gray-600 transition-colors">
					<div class="flex items-center justify-between mb-3">
						<span class="text-gray-400 text-sm">Needs Attention</span>
						<AlertTriangle class="text-yellow-400" size={18} />
					</div>
					<p class="text-2xl font-semibold {stats.kpi_summary.needs_attention_count > 0 ? 'text-yellow-400' : 'text-white'}">{stats.kpi_summary.needs_attention_count}</p>
					<p class="text-xs text-gray-500 mt-1">members below 70</p>
				</a>
			</div>
		{/if}

		<!-- Two-column row: My Tasks (left) + Activity Feed (right) — all roles (D-01, D-02) -->
		<div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-6">
			<!-- My Tasks Panel (D-04 to D-07) -->
			<div class="card">
				<div class="flex items-center gap-2 mb-4">
					<CheckSquare class="text-primary-400" size={18} />
					<h2 class="font-semibold text-white">My Tasks</h2>
				</div>
				{#if stats.my_tasks.length === 0}
					<p class="text-gray-500 text-sm py-4 text-center">No tasks assigned. <a href="/tasks" class="text-primary-400 hover:underline">View all tasks →</a></p>
				{:else}
					<div class="space-y-1">
						{#each stats.my_tasks as t}
							<a
								href="/tasks"
								class="flex items-center gap-3 p-2.5 rounded-lg hover:bg-gray-800 transition-colors {t.is_overdue ? 'bg-red-950/40' : t.is_due_soon ? 'bg-yellow-950/40' : ''}"
							>
								<div class="flex-1 min-w-0">
									<p class="text-sm text-gray-200 truncate">{t.title}</p>
									{#if t.project_name}
										<p class="text-xs text-gray-500">{t.project_name}</p>
									{/if}
								</div>
								<div class="flex items-center gap-2 flex-shrink-0">
									{#if t.priority}
										<span class="badge {priorityColors[t.priority]}">{t.priority}</span>
									{/if}
									<span class="badge {statusColors[t.status] ?? 'bg-gray-700 text-gray-300'}">{statusLabels[t.status] ?? t.status}</span>
								</div>
							</a>
						{/each}
					</div>
				{/if}
			</div>

			<!-- Activity Feed Panel (D-14 to D-16) -->
			<div class="card">
				<div class="flex items-center gap-2 mb-4">
					<Clock class="text-primary-400" size={18} />
					<h2 class="font-semibold text-white">Recent Activity</h2>
				</div>
				{#if stats.recent_activity.length === 0}
					<p class="text-gray-500 text-sm py-4 text-center">No recent updates. <a href="/updates" class="text-primary-400 hover:underline">View all updates →</a></p>
				{:else}
					<div>
						{#each stats.recent_activity as activity}
							<div class="flex items-start gap-3 py-2 border-b border-gray-800 last:border-0">
								<div class="w-8 h-8 rounded-full bg-gray-700 flex items-center justify-center text-xs font-semibold text-gray-300 flex-shrink-0">
									{activity.author_name.charAt(0).toUpperCase()}
								</div>
								<div class="flex-1 min-w-0">
									<p class="text-sm font-semibold text-gray-200">{activity.author_name}</p>
									<p class="text-xs text-gray-500">{timeAgo(activity.created_at)}</p>
									{#if activityPreview(activity.field_values)}
										<p class="text-sm text-gray-400 mt-1 truncate">{activityPreview(activity.field_values)}</p>
									{/if}
								</div>
							</div>
						{/each}
					</div>
					<div class="flex justify-end mt-3 pt-3 border-t border-gray-800">
						<a href="/updates" class="text-xs text-primary-400 hover:underline">View all updates →</a>
					</div>
				{/if}
			</div>
		</div>

		{#if $isManagerOrLeader && stats.team_health}
			<!-- Team Health Panel (supervisor / assistant_manager / manager only — D-08 to D-11) -->
			<div class="card">
				<div class="flex items-center gap-2 mb-4">
					<Users class="text-primary-400" size={18} />
					<h2 class="font-semibold text-white">Team Health</h2>
				</div>
				{#if stats.team_health.length === 0}
					<p class="text-gray-500 text-sm py-4 text-center">No team members visible</p>
				{:else}
					<div class="grid grid-cols-2 md:grid-cols-3 gap-3">
						{#each stats.team_health as member}
							<div class="bg-gray-800 rounded-lg p-3 border {member.status === 'red' ? 'border-red-500/50' : 'border-gray-700'}">
								<div class="flex items-center gap-2">
									{#if member.avatar_url}
										<img src={member.avatar_url} alt={member.full_name} class="w-10 h-10 rounded-full object-cover flex-shrink-0" />
									{:else}
										<div class="w-10 h-10 rounded-full bg-primary-700 flex items-center justify-center text-white font-semibold text-sm flex-shrink-0">
											{initials(member.full_name)}
										</div>
									{/if}
									<div class="flex-1 min-w-0">
										<p class="text-sm text-gray-200 truncate">{member.full_name}</p>
										<div class="flex items-center gap-1 mt-0.5">
											<span class="w-2 h-2 rounded-full {statusDotClass[member.status]} flex-shrink-0"></span>
											<span class="text-xs text-gray-500">{member.active_tasks} active · {member.overdue_tasks} overdue</span>
										</div>
									</div>
								</div>
							</div>
						{/each}
					</div>
					<div class="flex justify-end mt-3 pt-3 border-t border-gray-800">
						<a href="/performance" class="text-xs text-primary-400 hover:underline">View full performance →</a>
					</div>
				{/if}
			</div>
		{/if}
	{/if}
</div>
```

Verify the complete file has: KPI strip gated by `$isManagerOrLeader && stats.kpi_summary`, two-column panel row for all roles, Team Health panel gated by `$isManagerOrLeader && stats.team_health`, and all empty states with CTA links.
  </action>
  <verify><automated>grep -c "isManagerOrLeader\|bg-red-950/40\|bg-yellow-950/40\|border-red-500/50\|statusDotClass\|View full performance\|View all updates\|dashboard.get" frontend/src/routes/+page.svelte</automated></verify>
  <acceptance_criteria>
    - `frontend/src/routes/+page.svelte` contains `$isManagerOrLeader && stats.kpi_summary` (KPI strip gate)
    - `frontend/src/routes/+page.svelte` contains `$isManagerOrLeader && stats.team_health` (Team Health gate)
    - `frontend/src/routes/+page.svelte` contains `bg-red-950/40` (overdue row tint — D-05)
    - `frontend/src/routes/+page.svelte` contains `bg-yellow-950/40` (due-soon row tint — D-06)
    - `frontend/src/routes/+page.svelte` contains `border-red-500/50` (at-risk member card — D-09)
    - `frontend/src/routes/+page.svelte` contains `statusDotClass[member.status]` (status dot — D-08)
    - `frontend/src/routes/+page.svelte` contains `Math.round(stats.kpi_summary.completion_rate * 100)` (% conversion — D-12)
    - `frontend/src/routes/+page.svelte` contains `kpiScoreColor(stats.kpi_summary.avg_score)` (color-coded avg score — D-12)
    - `frontend/src/routes/+page.svelte` contains `href="/performance"` (KPI-03, HEALTH-03)
    - `frontend/src/routes/+page.svelte` contains `href="/updates"` (FEED-03)
    - `frontend/src/routes/+page.svelte` contains `href="/tasks"` (TASKS-03)
    - `frontend/src/routes/+page.svelte` contains `grid grid-cols-1 lg:grid-cols-2 gap-6` (two-column row — D-01, D-02)
    - `frontend/src/routes/+page.svelte` contains `grid grid-cols-1 sm:grid-cols-3 gap-4` (KPI strip — D-13)
    - `frontend/src/routes/+page.svelte` contains `grid grid-cols-2 md:grid-cols-3 gap-3` (Team Health grid — D-08)
    - `frontend/src/routes/+page.svelte` contains `View full performance →`
    - `frontend/src/routes/+page.svelte` contains `View all updates →`
    - `frontend/src/routes/+page.svelte` contains `timeAgo(activity.created_at)` (D-14)
    - `frontend/src/routes/+page.svelte` contains `initials(member.full_name)` (D-08)
  </acceptance_criteria>
  <done>+page.svelte fully rebuilt with all four sections, role-conditional layout, and all visual rules from 32-CONTEXT.md and 32-UI-SPEC.md.</done>
</task>

<task type="auto">
  <name>Task 3: TypeScript type-check and verify no broken imports</name>
  <files>frontend/src/routes/+page.svelte, frontend/src/lib/apis/dashboard.ts</files>
  <read_first>frontend/src/routes/+page.svelte, frontend/src/lib/apis/dashboard.ts, frontend/src/lib/apis/index.ts</read_first>
  <action>
Run the SvelteKit type checker to confirm there are no TypeScript errors introduced by the rebuild:

```bash
cd frontend && rtk bun run check
```

If `bun run check` reports errors related to `dashboard.stats` still being referenced somewhere (e.g., in index.ts re-exports), locate the reference and update it to `dashboard.get`. The `frontend/src/lib/apis/index.ts` re-exports `dashboard` — verify it still works after the rename.

If `bun run check` reports `Property 'stats' does not exist` errors, find the remaining callers and update them to `get`.

No other type errors are expected from this change. Do NOT fix pre-existing type errors that are unrelated to the dashboard rebuild.
  </action>
  <verify><automated>cd frontend && rtk bun run check 2>&1 | grep -i "error\|stats" | head -20</automated></verify>
  <acceptance_criteria>
    - `bun run check` exits 0 OR any remaining errors are pre-existing and unrelated to dashboard.ts / +page.svelte
    - No errors containing `stats` or `DashboardStats` in frontend/src/routes/+page.svelte
    - No errors containing `Property 'stats' does not exist on type`
    - `frontend/src/lib/apis/index.ts` still exports `dashboard` (re-export is unchanged)
  </acceptance_criteria>
  <done>TypeScript type-check passes. No import errors from the dashboard.ts rename or +page.svelte rebuild.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Role gate | $isManagerOrLeader derived from JWT-stored role — cannot be elevated client-side; sensitive sections gated at backend too (team_health/kpi_summary absent from payload for member role) |
| activity field_values | User-supplied text rendered as plain text via {activityPreview()} — no {@html}, no DOMPurify needed |
| avatar_url | Rendered via <img src={member.avatar_url}> — URL comes from backend, not user input; CSP covers external images |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03 | S | Role-conditional rendering | mitigate | Backend omits team_health/kpi_summary from payload for member role — frontend gate is defence-in-depth |
| T-32-04 | I | field_values XSS | mitigate | activityPreview() returns plain string; rendered as text node (not {@html}) — no injection vector |
| T-32-05 | I | avatar_url rendering | accept | URL from trusted backend; no user-controlled redirect; CSP handles external domains |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `grep -n "dashboard.get\|isManagerOrLeader\|bg-red-950/40\|bg-yellow-950/40\|border-red-500/50" frontend/src/routes/+page.svelte` — all present
- [ ] `grep -n "dashboard.stats" frontend/src/routes/+page.svelte` — empty (no old call)
- [ ] `grep "Math.round.*completion_rate.*100" frontend/src/routes/+page.svelte` — present
- [ ] `grep "kpiScoreColor" frontend/src/routes/+page.svelte` — present
- [ ] `grep "statusDotClass\[member.status\]" frontend/src/routes/+page.svelte` — present
- [ ] `grep "href=\"/performance\"" frontend/src/routes/+page.svelte` — present (HEALTH-03, KPI-03)
- [ ] `grep "href=\"/updates\"" frontend/src/routes/+page.svelte` — present (FEED-03)
- [ ] `grep "href=\"/tasks\"" frontend/src/routes/+page.svelte` — present (TASKS-03)
- [ ] `cd frontend && rtk bun run check` — exits 0 or only pre-existing errors
</verification>

<success_criteria>
- Member role: sees My Tasks panel + Activity Feed only; KPI strip and Team Health panel absent from DOM
- Supervisor/manager role: sees KPI strip (top) → My Tasks + Activity Feed (two-column) → Team Health Panel (bottom)
- Overdue tasks: bg-red-950/40 row tint; due-soon tasks: bg-yellow-950/40 row tint
- Team health at-risk members (status === 'red'): border-red-500/50 card border
- KPI avg_score: color-coded text-green-400 / text-yellow-400 / text-red-400 per threshold
- completion_rate displayed as percentage (Math.round × 100 + '%')
- Activity feed: author name + timeAgo() timestamp + field_values preview (120 chars)
- All empty states have CTA anchor links to /tasks, /updates, /performance
- Mobile: grid collapses to single column via grid-cols-1 sm:grid-cols-3 and grid-cols-1 lg:grid-cols-2
- TypeScript type-check passes
- All 15 DASH/TASKS/HEALTH/KPI/FEED requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/32-dashboard-frontend-redesign/32-02-SUMMARY.md`
</output>
