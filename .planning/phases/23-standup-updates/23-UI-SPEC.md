---
phase: 23
slug: standup-updates
status: approved
shadcn_initialized: false
preset: none
created: 2026-04-28
reviewed_at: 2026-04-28
---

# Phase 23 — UI Design Contract: Standup Updates

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — manual TailwindCSS with global utility classes in `frontend/src/app.css` |
| Preset | not applicable |
| Component library | none (Svelte components only) |
| Icon library | lucide-svelte (tree-shaken per-component) |
| Font | Inter, system-ui, sans-serif (via `tailwind.config.js` `fontFamily.sans`) |

No shadcn gate applies: `components.json` not found; project uses hand-authored Tailwind utility classes.

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Tailwind Class | Value | Usage |
|-------|---------------|-------|-------|
| xs | `gap-1` / `p-1` | 4px | Icon gaps, badge inner padding |
| sm | `gap-2` / `p-2` | 8px | Button icon gap, inline element spacing |
| md | `gap-4` / `p-4` | 16px | Default element spacing, form field gap |
| lg | `gap-6` / `p-6` | 24px | Section padding, card internal sections |
| xl | `gap-8` | 32px | Layout gaps between major blocks |
| 2xl | `py-12` | 48px | Empty state vertical padding |
| 3xl | `py-16` | 64px | Loading spinner vertical centering |

Exceptions:
- Card component uses `p-5` (20px) per existing `.card` definition — do not change.
- Inline delete/edit button row uses `gap-2` (8px) for tightly grouped action buttons.
- Touch targets: all icon-only buttons must be minimum 36×36px via `p-2` on a block element.

---

## Typography

| Role | Size | Tailwind Class | Weight | Line Height | Usage |
|------|------|---------------|--------|-------------|-------|
| Meta / Caption | 12px | `text-xs` | 400 | 1.5 | Timestamps, template field labels (uppercase), task row metadata, action button text |
| Body | 14px | `text-sm font-normal` | 400 (`font-normal`) | 1.5 | Card field values, feed metadata, snapshot task titles |
| Label | 14px | `text-sm font-semibold` | 600 (`font-semibold`) | 1.5 | Form field labels, card author name, section headers |
| Heading | 24px | `text-2xl font-semibold` | 600 (`font-semibold`) | 1.2 | Page title "Updates" |
| Sub-heading | 20px | `text-xl font-semibold` | 600 (`font-semibold`) | 1.2 | Card post date/timestamp (primary identifier on card) |

All text uses `font-sans` (Inter) from the inherited body style. Distinct sizes in use: 12, 14, 20, 24 (4 sizes within limit). Exactly 2 weights in use: 400 (normal) and 600 (semibold).

---

## Color

| Role | Tailwind Token | Hex | Usage |
|------|---------------|-----|-------|
| Dominant (60%) | `bg-gray-950` | #030712 | Page background (inherited from layout) |
| Secondary (30%) | `bg-gray-900 border border-gray-800` | #111827 / #1f2937 | `.card` component — standup post cards, form panel |
| Accent (10%) | `primary-500` / `primary-600` | #6366f1 / #4f46e5 | Reserved for: Submit button, focus rings, expand-toggle chevron (active) |
| Destructive | `red-600` hover `red-700` | #dc2626 / #b91c1c | Delete confirmation "Yes" button only |

Accent reserved for: Submit standup button, focused `.input` ring (`focus:ring-primary-500`), expand/collapse toggle active state chevron color.

Semantic state colors:
- Task status "todo": `text-gray-400`
- Task status "in_progress": `text-blue-400`
- Task status "done": `text-green-400`
- Task status "blocked": `text-red-400`
- Priority "high": `text-red-400`
- Priority "medium": `text-amber-400`
- Priority "low": `text-gray-500`
- Author own-post indicator (edit/delete controls visible): no color change — controls appear on hover/focus of the card action area

---

## Component Inventory

### New Files

| File | Type | Description |
|------|------|-------------|
| `frontend/src/routes/updates/+page.svelte` | Route page | Main /updates page: form panel above feed |
| `frontend/src/lib/components/updates/StandupForm.svelte` | Component | Standup submission form — template-driven labeled textareas |
| `frontend/src/lib/components/updates/StandupCard.svelte` | Component | Single standup post card — read, edit-inline, delete-inline, snapshot expand/collapse |
| `frontend/src/lib/components/updates/SnapshotPanel.svelte` | Component | Collapsed/expanded task snapshot panel inside StandupCard |
| `frontend/src/lib/apis/updates.ts` | API module | CRUD for standup posts; template fetch |
| `frontend/src/lib/stores/updates.ts` | Store | Reactive post list, cursor state, filter state |

### Modified Files

| File | Change |
|------|--------|
| `frontend/src/routes/+layout.svelte` | Add `{ href: '/updates', label: 'Updates', icon: MessageSquare }` to `navItems` array (after Tasks, before Milestones) |

### Icon Imports for This Phase

```
MessageSquare  — nav item and page header
ChevronDown    — snapshot expand toggle (collapsed state)
ChevronUp      — snapshot expand toggle (expanded state)
Pencil         — Edit action button
Trash2         — Delete action button
X              — Cancel edit action
Check          — Save edit action (alternative to text button)
User           — Author avatar placeholder
Filter         — Filter bar icon
RefreshCw      — Load more / refresh
```

---

## Interaction Contract

### 1. Page Layout

```
/updates
├── Page header row
│   ├── Left: h1 "Updates" + subtitle "Team standup feed"
│   └── Right: (no CTA button — form is always accessible on page)
├── StandupForm panel (collapsible, collapsed by default on load)
│   ├── Collapse toggle: "Post a standup" chevron row — bg-gray-900 border border-gray-800 rounded-xl
│   └── Expanded: labeled textareas for each template field + Submit button
├── Filter bar (always visible when feed has content)
│   ├── Author select: "All members" default
│   └── Date input: type="date", "All dates" when empty
└── Post feed (StandupCard list, reverse-chronological)
    └── "Load more" button (when more pages available)
```

Page padding: `p-4 md:p-6`, max-width: `max-w-4xl mx-auto`.

### 2. StandupForm — Submit Flow

States: `collapsed` | `expanded` | `submitting` | `submitted`

- **Collapsed state:** Single row `button` with `ChevronDown` icon + label "Post a standup update". Class: `w-full flex items-center justify-between px-5 py-4 bg-gray-900 border border-gray-800 rounded-xl text-sm font-semibold text-gray-300 hover:bg-gray-800 transition-colors`.
- **Expanded state:** Full form inside `bg-gray-900 border border-gray-800 rounded-xl p-5`. Template fields rendered from API response as `<label class="label">` + `<textarea class="input resize-none h-20">`. Field order matches template order from API.
- **Submit button:** `<button class="btn-primary">Post Update</button>` — right-aligned at bottom of form.
- **Submitting state:** Button shows spinner (`w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin`) + text "Posting...". Button `disabled`.
- **On success:** Toast `toast.success('Standup posted')`. Form resets to blank. Panel collapses. New card prepended to feed top.
- **On error:** Toast `toast.error('Failed to post update. Try again.')`. Form stays open with user's text preserved.
- **Empty submit guard:** If all template fields are empty string (whitespace-trimmed), Submit button is `disabled` and shows `opacity-50 cursor-not-allowed`.

### 3. StandupCard — Read State

Card class: `card hover:border-gray-700 transition-colors` (inherits `.card` = `bg-gray-900 border border-gray-800 rounded-xl p-5`).

Card layout:
```
[Avatar placeholder 32×32 rounded-full bg-gray-700]  [Author name text-sm font-semibold text-gray-200]
                                                       [Timestamp text-xs text-gray-500]
─────────────────────────────────────────────────────────────────────────────
[Template field label text-xs font-semibold text-gray-500 uppercase tracking-wide]
[Field value text-sm text-gray-300]
... (repeat for each field that has a non-empty value)
─────────────────────────────────────────────────────────────────────────────
[SnapshotPanel: "Task snapshot" toggle row]
─────────────────────────────────────────────────────────────────────────────
[Edit post button]  [Delete post button]          ← visible only when post.author_id === current_user.id
```

Empty template fields (blank values): omit the field row entirely — do not render label + empty value.

Author avatar: `<div class="w-8 h-8 rounded-full bg-gray-700 flex items-center justify-center text-xs font-semibold text-gray-300">` with first letter of display name. No real avatar images.

Timestamp format: `"Mon Apr 28, 2026 · 09:41"` using `format(date, 'EEE MMM d, yyyy · HH:mm')` from `date-fns`.

Edit post and Delete post buttons: `<button class="btn-secondary text-xs px-3 py-2">`. Only rendered when `post.author_id === currentUser.id`.

### 4. SnapshotPanel — Expand/Collapse

Toggle row class: `flex items-center justify-between w-full text-xs font-semibold text-gray-500 hover:text-gray-300 transition-colors py-2 border-t border-gray-800 mt-3`.

Toggle label: "Task snapshot ({count} tasks)" where count = snapshot array length.

- **Collapsed (default):** `<ChevronDown size={14} />` on right side of toggle row. Snapshot content hidden (`hidden` class or Svelte `{#if}`).
- **Expanded:** `<ChevronUp size={14} />`. Snapshot content visible.

Snapshot content (when expanded): `<div class="mt-2 space-y-2">` containing one row per task:

```
<div class="flex items-center gap-3 text-xs py-1">
  <span class="w-2 h-2 rounded-full {statusDotColor}"></span>
  <span class="flex-1 text-gray-300">{task.title}</span>
  <span class="text-gray-500">{task.status}</span>
  <span class="badge {priorityColors[task.priority]}">{task.priority}</span>
  <span class="text-gray-600">{task.due_date ? format(task.due_date, 'MMM d') : '—'}</span>
</div>
```

Status dot colors: todo `bg-gray-500`, in_progress `bg-blue-400`, done `bg-green-400`, blocked `bg-red-400`.

If snapshot is empty array: render `<p class="text-xs text-gray-600 py-1">No tasks assigned at time of post.</p>`.

### 5. StandupCard — Edit Inline (D-10, D-11)

Trigger: user clicks "Edit post" button on their own card.

The entire card interior (all field text rows) is replaced in-place by the edit form. Card wrapper stays the same (`card`). The task snapshot panel is hidden during edit (snapshot is not re-frozen per D-11).

Edit form layout:
```
[Template field label text-xs font-semibold text-gray-500 uppercase tracking-wide]
[<textarea class="input resize-none h-20"> — pre-filled with current value]
... (one textarea per template field, including empty ones)

[row: <button class="btn-secondary text-xs px-3 py-2">Discard changes</button>
      <button class="btn-primary text-sm">Save changes</button>]
```

- **Discard changes:** Reverts card to read state. No API call. No confirmation.
- **Save changes:** `disabled` + spinner while saving. On success: `toast.success('Update saved')`, card switches to read state with new values. On error: `toast.error('Failed to save. Try again.')`, edit form stays open with user's edits preserved.

### 6. StandupCard — Delete Inline (D-12)

Trigger: user clicks "Delete post" button on their own card.

The Delete post button is replaced in-place by:
```html
<div class="flex items-center gap-2">
  <span class="text-xs text-gray-400">Delete this post?</span>
  <button class="btn-danger text-xs px-3 py-2">Yes, delete</button>
  <button class="btn-secondary text-xs px-3 py-2">Keep post</button>
</div>
```

No modal. No browser `confirm()`. No overlay. The card remains fully visible.

- **Yes, delete:** Calls DELETE endpoint. On success: card removed from feed with `toast.success('Post deleted')`. On error: `toast.error('Failed to delete. Try again.')`, confirmation row stays visible.
- **Keep post:** Returns to normal card read state (Delete post button reappears).

### 7. Feed Filters (UPD-06)

Filter bar rendered above the post feed, below the StandupForm panel.

```html
<div class="flex items-center gap-3 mb-4 flex-wrap">
  <select class="input w-auto text-sm" bind:value={filterAuthorId}>
    <option value={null}>All members</option>
    {#each teamMembers as m}
      <option value={m.id}>{m.full_name}</option>
    {/each}
  </select>
  <input type="date" class="input w-auto text-sm" bind:value={filterDate} />
  {#if filterAuthorId || filterDate}
    <button class="btn-secondary text-xs px-3 py-2" on:click={clearFilters}>
      Clear filters
    </button>
  {/if}
</div>
```

Filter behavior: changing either select or date input triggers a fresh API fetch (not client-side filter). Existing cursor is reset on filter change. "Load more" button re-appears if new result set has more pages.

### 8. Load More (D-06)

Rendered below the post list when `nextCursor !== null`:

```html
<div class="flex justify-center mt-6">
  <button class="btn-secondary" on:click={loadMore} disabled={loadingMore}>
    {#if loadingMore}
      <span class="w-4 h-4 border-2 border-gray-400 border-t-transparent rounded-full animate-spin"></span>
      Loading...
    {:else}
      Load more
    {/if}
  </button>
</div>
```

New cards appended below existing cards (not prepend). Scroll position preserved.

### 9. Loading State

Full-page initial load spinner (matches existing pattern from schedule page):
```html
<div class="flex items-center justify-center py-16">
  <div class="w-6 h-6 border-2 border-primary-500 border-t-transparent rounded-full animate-spin"></div>
</div>
```

### 10. Supervisor Template Management

The supervisor template customization UI (UPD-03) is scoped to a separate settings section. For Phase 23 the template admin surface is a `<details>` or collapsible section below the main form on `/updates`, visible only when `$isSupervisor === true`. It is not a separate route.

Template editor layout:
```
[Section header: "Configure standup template" — text-sm font-semibold text-gray-400 — supervisor only]
[Field list: each field row has text input for field name + Remove button (Trash2 icon, btn-secondary)]
[Add field row: text input + "Add field" btn-secondary button]
[Save template: btn-primary "Save template"]
```

Template changes take effect for subsequent form renders only — existing posts unaffected (D-03).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Page title | Updates |
| Page subtitle | Team standup feed |
| Page `<title>` | Updates · TeamFlow |
| Form collapse toggle (closed) | Post a standup update |
| Form collapse toggle (open) | Hide form |
| Default template field 1 label | Pending Tasks |
| Default template field 2 label | Future Tasks |
| Default template field 3 label | Blockers |
| Default template field 4 label | Need Help From |
| Default template field 5 label | Critical Timeline |
| Default template field 6 label | Release Date |
| Submit button (idle) | Post Update |
| Submit button (submitting) | Posting... |
| Submit button (disabled — all fields empty) | Post Update (disabled, `opacity-50`) |
| Edit button | Edit post |
| Delete button | Delete post |
| Save edit button | Save changes |
| Cancel edit button | Discard changes |
| Delete confirmation label | Delete this post? |
| Delete confirm button | Yes, delete |
| Delete cancel button | Keep post |
| Snapshot toggle (collapsed) | Task snapshot ({n} tasks) |
| Snapshot toggle (expanded) | Task snapshot ({n} tasks) |
| Snapshot empty | No tasks assigned at time of post. |
| Feed empty state heading | No standup posts yet |
| Feed empty state body | Be the first to post a standup update for your team. |
| Feed empty state (filtered, no results) | No posts match the selected filters. |
| Filter author select default | All members |
| Filter date input placeholder | (browser native date placeholder) |
| Clear filters button | Clear filters |
| Load more button (idle) | Load more |
| Load more button (loading) | Loading... |
| Toast: post success | Standup posted |
| Toast: post error | Failed to post update. Try again. |
| Toast: edit success | Update saved |
| Toast: edit error | Failed to save. Try again. |
| Toast: delete success | Post deleted |
| Toast: delete error | Failed to delete. Try again. |
| Template save success | Template saved |
| Template save error | Failed to save template. Try again. |
| Supervisor template section label | Configure standup template |
| Add field button | Add field |
| Save template button | Save template |
| Remove field button | (icon only: Trash2, `aria-label="Remove field"` `title="Remove field"`) |
| Nav item label | Updates |

---

## Visual Hierarchy

### Page Priority Order (top → bottom)
1. Page header (h1 "Updates" + subtitle)
2. StandupForm panel (collapsed by default — user initiates when ready)
3. Filter bar (always visible, low visual weight)
4. Post feed — cards in reverse-chronological order
5. "Load more" button

### Card Priority Order (within each card)
1. Author name + timestamp (who posted, when)
2. Template field values (the standup content)
3. Task snapshot toggle (secondary, collapsed)
4. Edit/Delete controls (lowest priority — only for card author, no visual prominence when not hovered)

### Density
Cards are `p-5` (20px) with `space-y-4` between cards. Template fields within a card use `space-y-3`. This matches `.card` padding from app.css.

### Marked + DOMPurify rendering
Template field text values are plain text — they are NOT rendered as markdown in Phase 23. Render as `<p class="text-sm text-gray-300 whitespace-pre-wrap">`. The `marked` + `DOMPurify` packages are installed in Phase 23 for use by the KnowledgeSession and WeeklyBoard features in Phases 24-25, but Phase 23 does NOT use `{@html}` blocks. This avoids any XSS surface on standup fields which accept free-form text.

Exception: if a future decision adds markdown support to standup fields, the mandatory pattern is:
```svelte
{@html DOMPurify.sanitize(marked.parse(fieldValue))}
```

---

## Registry Safety

| Registry | Packages Used | Safety Gate |
|----------|--------------|-------------|
| npm (official) | `marked` — HTML rendering from markdown | Standard npm install; no shadcn registry involved; source is public at github.com/markedjs/marked. No vetting gate required. |
| npm (official) | `dompurify` — XSS sanitization | Standard npm install; widely audited library. No vetting gate required. |
| shadcn | none | not applicable — project does not use shadcn |

Note: `marked` and `DOMPurify` are npm packages, not shadcn registry blocks. The registry vetting gate (shadcn view command) does not apply. Both packages are installed in Phase 23 per STATE.md Architecture Decisions (Milestone v2.2), though their first `{@html}` usage occurs in Phase 25.

Install command: `cd frontend && bun add marked dompurify && bun add -d @types/dompurify`

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
