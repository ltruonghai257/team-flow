---
phase: 30-phase-18-status-transition-follow-up-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/statuses.py
  - backend/tests/test_status_sets.py
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "D-01 D-02 D-03 D-04: _require_status_write_scope allows manager, supervisor, and assistant_manager; only member is blocked. The old UserRole.admin sub-team guard is removed entirely."
    - "D-06: Any Phase 29 role-reference breaks found during the audit are fixed inline."
    - "The RBAC fix uses is_member() from services/visibility.py — consistent with every other Phase 29 router update."
  artifacts:
    - path: "backend/app/routers/statuses.py"
      provides: "_require_status_write_scope with UserRole.admin removed and is_member() used instead"
      min_lines: 5
      contains: "is_member"
    - path: "backend/tests/test_status_sets.py"
      provides: "Extended RBAC test covering manager and assistant_manager write access"
      min_lines: 300
      contains: "assistant_manager"
  key_links:
    - from: "backend/app/routers/statuses.py"
      to: "backend/app/services/visibility.py"
      via: "_require_status_write_scope calls is_member() from the shared visibility service"
      pattern: "is_member"
---

<objective>
Fix the confirmed runtime `AttributeError` in `_require_status_write_scope`, harden the backend test coverage to include the full Phase 29 role set, and confirm no other Phase 29 side effects remain in the status/transition code paths.

Purpose: `UserRole.admin` no longer exists after Phase 29 — any write to the status-set or transition endpoints triggers `AttributeError` at runtime. The fix is surgical: replace the old admin branch with the `is_member()` helper from `services/visibility.py` and drop the now-meaningless sub-team guard for manager.
Output: patched `statuses.py`, extended `test_status_sets.py` covering `manager` and `assistant_manager`, and a clean `pytest` run on the status and task test suites.
</objective>

<context>
@.planning/phases/30-phase-18-status-transition-follow-up-hardening/30-CONTEXT.md
@.planning/phases/30-phase-18-status-transition-follow-up-hardening/30-RESEARCH.md
@.planning/phases/18-status-transition-graph/18-SUMMARY.md
@.planning/phases/29-scoped-team-visibility-leadership-rbac/29-CONTEXT.md
@backend/app/routers/statuses.py
@backend/app/services/visibility.py
@backend/app/models/enums.py
@backend/tests/test_status_sets.py
@backend/tests/test_tasks.py
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix _require_status_write_scope — replace UserRole.admin with is_member()</name>
  <files>backend/app/routers/statuses.py</files>
  <read_first>backend/app/routers/statuses.py, backend/app/services/visibility.py, backend/app/models/enums.py, .planning/phases/30-phase-18-status-transition-follow-up-hardening/30-CONTEXT.md</read_first>
  <action>
In `backend/app/routers/statuses.py`:

1. Add import at the top of the file:
   ```python
   from app.services.visibility import is_member
   ```

2. Replace `_require_status_write_scope` (lines 141–148) with:
   ```python
   def _require_status_write_scope(current_user: User, sub_team: Optional[SubTeam]) -> None:
       if is_member(current_user):
           raise HTTPException(status_code=403, detail="Manager, supervisor, or assistant manager access required")
   ```

   The `sub_team` parameter is kept in the signature (8 call sites pass it; removing it would require touching all callers) but is no longer used in the function body. This is intentional — per D-02, manager has org-wide visibility and the old sub-team guard is removed entirely. Per D-03, assistant_manager and supervisor both get write access.

3. Check whether `UserRole` is still referenced elsewhere in `statuses.py` after the change. If `UserRole` is no longer imported directly, remove it from the import block. If still used (e.g., in models imported transitively), leave it.
  </action>
  <verify>
    <automated>cd backend && rtk uv run pytest tests/test_status_sets.py tests/test_tasks.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/routers/statuses.py` contains `from app.services.visibility import is_member`
    - `backend/app/routers/statuses.py` does NOT contain `UserRole.admin`
    - `_require_status_write_scope` body contains `is_member(current_user)`
    - `cd backend && rtk uv run pytest tests/test_status_sets.py tests/test_tasks.py -q` exits 0
  </acceptance_criteria>
  <done>The AttributeError at runtime is eliminated. All non-member leadership roles (manager, supervisor, assistant_manager) can write status sets and transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Extend RBAC test coverage to include manager and assistant_manager write access</name>
  <files>backend/tests/test_status_sets.py</files>
  <read_first>backend/tests/test_status_sets.py, backend/tests/conftest.py, backend/app/models/enums.py, backend/app/services/visibility.py, .planning/phases/30-phase-18-status-transition-follow-up-hardening/30-CONTEXT.md</read_first>
  <action>
In `backend/tests/test_status_sets.py`, extend the existing `test_transition_replace_validates_auth_pairs_and_empty_payload` test (or add a new focused test `test_transition_write_scope_allows_manager_and_assistant_manager`) that:

1. Creates two additional users using the existing `_create_user` helper:
   - `manager_user`: `role=UserRole.manager`, `sub_team_id=sub_team.id`
   - `assistant_manager_user`: `role=UserRole.assistant_manager`, `sub_team_id=sub_team.id`

2. Verifies that both `manager` and `assistant_manager` can POST to `/api/status-sets/{id}/transitions` and receive HTTP 200 (not 403).

3. Re-verifies that `member` still receives HTTP 403.

4. For `manager` specifically: the test should NOT need to set `sub_team.supervisor_id` — per D-02, manager has org-wide write access without sub-team context enforcement. Confirm the 200 response is returned even when `sub_team.supervisor_id` is None.

The test should follow the same fixture pattern as the existing test: create status set with `_create_status_set(db_session, suffix="_rbac")`, login via `_login()`, set headers, POST transitions, assert response code.
  </action>
  <verify>
    <automated>cd backend && rtk uv run pytest tests/test_status_sets.py -q -k "scope or rbac or auth"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_status_sets.py` contains `UserRole.assistant_manager`
    - `backend/tests/test_status_sets.py` contains `UserRole.manager`
    - a test asserts that `assistant_manager` POST to transitions returns HTTP 200
    - a test asserts that `manager` POST to transitions returns HTTP 200
    - a test asserts that `member` POST to transitions returns HTTP 403
    - `cd backend && rtk uv run pytest tests/test_status_sets.py -q` exits 0
  </acceptance_criteria>
  <done>Backend RBAC coverage for the Phase 29 role model is complete for the transition write scope path.</done>
</task>

<task type="auto">
  <name>Task 3: Regression sweep — confirm no other admin role references remain in status/task paths</name>
  <files>backend/app/routers/statuses.py, backend/app/routers/tasks.py, backend/tests/test_status_sets.py, backend/tests/test_tasks.py</files>
  <read_first>backend/app/routers/statuses.py, backend/app/routers/tasks.py, backend/tests/test_status_sets.py, backend/tests/test_tasks.py</read_first>
  <action>
Perform a targeted audit of the four files for Phase 29 side effects:

1. `backend/app/routers/statuses.py` — grep for `admin`. After Task 1 the only occurrence should be zero. If any remain, fix them.
2. `backend/app/routers/tasks.py` — grep for `admin`. Research confirmed 0 matches; verify this is still true and no `_require_status_write_scope` call in tasks.py passes unexpected arguments.
3. `backend/tests/test_status_sets.py` — grep for `admin`. After Task 2 should be zero. If any remain, update the fixture to use `UserRole.supervisor` or `UserRole.manager` as appropriate.
4. `backend/tests/test_tasks.py` — grep for `admin`. Research confirmed 0 matches; verify.

If this task finds no issues (expected outcome), document the clean result in a comment at the top of the summary. If issues are found, fix them inline per D-06.
  </action>
  <verify>
    <automated>cd backend && rtk uv run pytest tests/test_status_sets.py tests/test_tasks.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "UserRole.admin" backend/app/routers/statuses.py backend/app/routers/tasks.py backend/tests/test_status_sets.py backend/tests/test_tasks.py` returns 0 matches
    - `cd backend && rtk uv run pytest tests/test_status_sets.py tests/test_tasks.py -q` exits 0
  </acceptance_criteria>
  <done>Status and task code paths have zero Phase 29 admin-role remnants. The RBAC hardening is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client to `_require_status_write_scope` | Authenticated user claims a role; guard must reject members without trusting client-supplied role assertions |
| Test fixtures to live role enum | Tests construct users with explicit `UserRole.*` values — stale enum references silently skip the intended test path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | E | `_require_status_write_scope` in `statuses.py` | mitigate | Replace `UserRole.admin` with `is_member()` from shared visibility service; cover manager + assistant_manager write access in tests |
| T-30-02 | I | `test_status_sets.py` fixture gap | mitigate | Add manager and assistant_manager test users to confirm the guard allows all Phase 29 leadership roles |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `grep "UserRole.admin" backend/app/routers/statuses.py` → no matches
- [ ] `grep "is_member" backend/app/routers/statuses.py` → at least 1 match
- [ ] `grep "assistant_manager" backend/tests/test_status_sets.py` → at least 1 match
- [ ] `cd backend && rtk uv run pytest tests/test_status_sets.py tests/test_tasks.py -q` → exits 0
</verification>

<success_criteria>
- All 3 tasks completed
- All verification checks pass
- `_require_status_write_scope` uses `is_member()` from `services/visibility.py`
- No `UserRole.admin` reference in `statuses.py`, `tasks.py`, `test_status_sets.py`, or `test_tasks.py`
- Backend tests cover manager, supervisor, assistant_manager (write allowed) and member (write denied) for transition endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/30-phase-18-status-transition-follow-up-hardening/30-01-SUMMARY.md`
</output>
