---
phase: "15-custom-kanban-statuses"
plan: "15-05"
title: "End-to-End Verification and Phase 16 Readiness"
type: "execute"
wave: 5
depends_on: ["15-01", "15-02", "15-03", "15-04"]
files_modified:
  - "backend/tests/test_status_sets.py"
  - "backend/tests/test_tasks.py"
  - "frontend/tests/mobile/kanban-scroll.spec.ts"
  - ".planning/phases/15-custom-kanban-statuses/15-VALIDATION.md"
autonomous: true
requirements: ["STATUS-01", "STATUS-02", "STATUS-03", "STATUS-04"]
---

# Plan 15-05 — End-to-End Verification and Phase 16 Readiness

<objective>
Verify Phase 15 satisfies all requirements, preserves migration safety, and exposes `custom_status_id`/`is_done` semantics for Phase 16 KPI work.
</objective>

<must_haves>
- STATUS-01 through STATUS-04 all pass.
- D-01 through D-18 are represented in code or explicitly preserved as transition behavior.
- Phase 16 must be able to use `CustomStatus.is_done`, not `TaskStatus.done`.
</must_haves>

<truths>
- D-01: Team-wide default status sets are scoped per sub-team, not globally across the whole organization.
- D-02: Supervisors/admins manage the default status set for their active or implicit sub-team, following the Phase 13 sub-team scoping model.
- D-03: Admin access should respect the global sub-team context switcher when editing defaults; supervisors operate only within their assigned sub-team.
- D-04: Projects may define their own status set; projects without custom statuses inherit the sub-team default set.
- D-05: Statuses need a stable canonical key/slug so project-specific statuses can be mapped back to default statuses safely.
- D-06: When switching a project from custom statuses back to the sub-team default set, the system should auto-map matching slugs and require an explicit fallback only for unmatched statuses.
- D-07: Multiple statuses in a set may have `is_done = true`.
- D-08: Moving a task into any `is_done` status sets `completed_at`; moving it from any done status back to a non-done status clears `completed_at`.
- D-09: Phase 16 KPI and burndown work must use `custom_status.is_done`, not `TaskStatus.done` or status slug comparisons.
- D-10: Status management should be available from both `/projects` and `/tasks`.
- D-11: `/projects` should provide the full project-context settings surface for project overrides and access to sub-team defaults.
- D-12: `/tasks` should provide direct Kanban column management where supervisors/admins can see the impact of status changes on the board.
- D-13: Board-context editing must handle mixed project views carefully; project-specific settings should only apply when the board has a clear project context.
- D-14: Deleting a status with assigned tasks should offer two safe paths: move affected tasks to a replacement status and delete, or archive the status.
- D-15: Archived statuses remain attached to existing tasks but are hidden from new task creation/status selection.
- D-16: Status reordering is immediate and shared: drag-and-drop reorder saves instantly and all users see the updated order.
- D-17: AI task parsing and quick task creation stay on the legacy enum values during the dual-write period.
- D-18: The backend maps legacy AI/status input (`todo`, `in_progress`, `review`, `done`, `blocked`) to the appropriate DB-backed status for the task's project/sub-team context.
</truths>

<tasks>

<task id="15-05-01" type="execute">
  <name>Verify requirements coverage in code</name>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/15-custom-kanban-statuses/15-CONTEXT.md, backend/app/models.py, backend/app/routers/statuses.py, backend/app/routers/tasks.py, frontend/src/routes/tasks/+page.svelte, frontend/src/routes/projects/+page.svelte</read_first>
  <action>
Audit code against:

- STATUS-01: supervisor/admin can create statuses with name/color/order and reorder them; `is_done` exists; assigned statuses cannot be unsafely deleted.
- STATUS-02: projects can define override set; inherited default exists; revert maps matching slugs and requires fallback for unmatched statuses.
- STATUS-03: migration creates legacy enum statuses as DB records and backfills tasks.
- STATUS-04: task completion uses `CustomStatus.is_done` and exposes data for KPI joins.

Record any gap directly in the execution summary. Do not mark the plan complete with unaddressed STATUS gaps.
  </action>
  <acceptance_criteria>
    - `backend/app/models.py` contains `class CustomStatus`
    - `backend/app/models.py` contains `is_done`
    - `backend/app/routers/statuses.py` contains `reorder`
    - `backend/app/routers/statuses.py` contains `fallback_mappings`
    - `backend/app/routers/tasks.py` contains `custom_status_id`
    - `frontend/src/lib/components/tasks/KanbanBoard.svelte` contains `export let statuses`
  </acceptance_criteria>
</task>

<task id="15-05-02" type="execute">
  <name>Verify migration safety manually or through tests</name>
  <read_first>backend/alembic/versions, backend/app/models.py, backend/tests/test_status_sets.py, .planning/phases/15-custom-kanban-statuses/15-VALIDATION.md</read_first>
  <action>
If a local DB is available, run Alembic upgrade and verify:

```sql
select count(*) from tasks;
select count(*) from tasks where custom_status_id is null;
select count(*) from custom_statuses where slug in ('todo','in_progress','review','done','blocked');
select count(*) from custom_statuses where slug = 'done' and is_done = true;
```

Expected:

- task count is unchanged before/after migration
- `tasks where custom_status_id is null` is `0`, unless documented orphan cases are intentionally handled
- seeded legacy status count is at least five per default set
- `done` seeded statuses have `is_done = true`

If no local DB is available, confirm the migration code contains equivalent backfill logic and record DB verification as manual UAT.
  </action>
  <acceptance_criteria>
    - Migration file contains `custom_status_id`
    - Migration file contains `legacy_status`
    - Migration file contains `is_done`
    - Execution summary records either DB query outputs or exact reason DB verification was manual-only
  </acceptance_criteria>
</task>

<task id="15-05-03" type="execute">
  <name>Run full automated checks</name>
  <read_first>backend/tests/test_status_sets.py, backend/tests/test_tasks.py, frontend/package.json, backend/app/routers/tasks.py, frontend/src/routes/tasks/+page.svelte</read_first>
  <action>
Run:

```bash
python -m compileall backend/app
cd backend && pytest
cd ../frontend && bun run check
```

If existing unrelated tests fail, isolate Phase 15 tests:

```bash
cd backend && pytest tests/test_status_sets.py tests/test_tasks.py
```

Record exact failing test names and whether failures are Phase 15 regressions or pre-existing unrelated failures.
  </action>
  <acceptance_criteria>
    - `python -m compileall backend/app` exits 0
    - `cd frontend && bun run check` exits 0
    - `cd backend && pytest` exits 0, or the execution summary classifies exact non-Phase-15 failures
  </acceptance_criteria>
</task>

<task id="15-05-04" type="execute">
  <name>Manual UAT for supervisor status workflows</name>
  <read_first>.planning/phases/15-custom-kanban-statuses/15-UI-SPEC.md, frontend/src/routes/tasks/+page.svelte, frontend/src/routes/projects/+page.svelte</read_first>
  <action>
Perform or document manual verification steps:

1. Login as supervisor/admin.
2. Open `/tasks`.
3. Click `Manage Statuses`.
4. Create a status with name, color, and `Marks tasks complete`.
5. Reorder statuses; refresh; verify order persists.
6. Drag a task into an `is_done` status; verify it appears completed.
7. Drag it back to a non-done status; verify completion styling clears.
8. Open `/projects`, click `Statuses`.
9. Create project override.
10. Add or rename a project status so fallback is required.
11. Revert to defaults and verify unmatched fallback is required.
12. Archive an assigned status and verify it remains visible on existing task but hidden from new selectors.
  </action>
  <acceptance_criteria>
    - Execution summary contains `Manual UAT: PASS` or a list of exact failed UAT steps
    - Execution summary mentions `/tasks Manage Statuses`
    - Execution summary mentions `/projects Statuses`
    - Execution summary mentions project override revert fallback
  </acceptance_criteria>
</task>

<task id="15-05-05" type="execute">
  <name>Update validation sign-off and Phase 16 readiness note</name>
  <read_first>.planning/phases/15-custom-kanban-statuses/15-VALIDATION.md, .planning/phases/16-advanced-kpi-dashboard/16-01-PLAN.md, backend/app/models.py, backend/app/routers/tasks.py</read_first>
  <action>
Update `15-VALIDATION.md` task statuses to reflect completed automated/manual checks.

Confirm Phase 16 prerequisite:

- `Task.custom_status_id` exists.
- `CustomStatus.is_done` exists.
- Task update logic uses `CustomStatus.is_done`.
- Existing `Task.status` remains for dual-write compatibility.

Record this in execution summary as:

`Phase 16 readiness: custom_status_id and is_done are available for KPI joins.`
  </action>
  <acceptance_criteria>
    - `.planning/phases/15-custom-kanban-statuses/15-VALIDATION.md` remains present
    - Execution summary contains `Phase 16 readiness: custom_status_id and is_done are available for KPI joins.`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
- T-15-01 Data loss: migration must preserve row counts and completed timestamps.
- T-15-05 Completion regression: `is_done` must be the source of truth.
- T-15-08 UX safety: delete/revert flows must require explicit choices.
- T-15-10 Phase 16 blocker: KPI work cannot start unless `is_done` join path exists.
</threat_model>

<verification>
- Run backend compile, backend tests, frontend check.
- Complete manual UAT or record exact blockers.
- Verify Phase 16 join path in models.
</verification>

<success_criteria>
- STATUS-01, STATUS-02, STATUS-03, and STATUS-04 are demonstrably covered.
- UI-SPEC contract is implemented.
- Validation strategy is updated.
- Phase 16 can proceed without retrofitting completion semantics.
</success_criteria>
