---
phase: 28-milestone-planning-decisions
plan: 04
type: execute
wave: 4
depends_on:
  - "28-03"
files_modified:
  - frontend/tests/milestones.spec.ts
  - backend/tests/test_milestones.py
autonomous: true
requirements:
  - ML-01
  - ML-02
  - ML-03
  - ML-04
user_setup: []
must_haves:
  truths:
    - "D-17 D-18 D-19 D-20 D-21: automated browser coverage proves the four-lane milestone command view, risk-first ordering, expanded active or risky cards, summary metrics row, and mobile lane model all render correctly."
    - "D-06 D-07 D-08 D-09 D-15 D-16: browser coverage proves linked tasks and decisions appear in milestone detail while task navigation still hands off to the existing task flow."
    - "Phase 28 release verification runs the backend milestone tests, frontend type/build checks, and focused Playwright milestone spec together before the phase is declared ready."
  artifacts:
    - path: "frontend/tests/milestones.spec.ts"
      provides: "Targeted Playwright regression coverage for milestone command-view behavior"
      min_lines: 80
      contains: "milestones"
    - path: "backend/tests/test_milestones.py"
      provides: "Final backend assertions for milestone command-view and decision contract"
      min_lines: 120
      contains: "command_view"
  key_links:
    - from: "frontend/tests/milestones.spec.ts"
      to: "frontend/src/routes/milestones/+page.svelte"
      via: "lane rendering, expansion, and decision/task interaction assertions"
      pattern: "Milestones"
    - from: "frontend/tests/milestones.spec.ts"
      to: "frontend/src/lib/components/milestones/MilestoneCard.svelte"
      via: "expanded task and decision rendering assertions"
      pattern: "decision"
---

<objective>
Lock the Phase 28 command view with targeted regression coverage and explicit release verification.

Purpose: Catch milestone-planning regressions before Phase 29 builds on the same planning surface and access rules.
Output: focused Playwright coverage, final backend assertions, and an exact release-check sequence.
</objective>

<execution_context>
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-milestone-planning-decisions/28-CONTEXT.md
@.planning/phases/28-milestone-planning-decisions/28-UI-SPEC.md
@.planning/phases/28-milestone-planning-decisions/28-03-SUMMARY.md
@frontend/playwright.config.ts
@frontend/tests/timeline-gantt.spec.ts
@frontend/src/routes/milestones/+page.svelte
@backend/tests/test_milestones.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focused Playwright coverage for the milestone command view</name>
  <files>frontend/tests/milestones.spec.ts</files>
  <read_first>frontend/tests/timeline-gantt.spec.ts, frontend/playwright.config.ts, frontend/src/routes/milestones/+page.svelte, .planning/phases/28-milestone-planning-decisions/28-UI-SPEC.md</read_first>
  <action>Create `frontend/tests/milestones.spec.ts` as a focused browser regression for Phase 28. Mock the command-view API and decision CRUD responses so the spec can assert the exact lane labels `Planned`, `Committed`, `Active`, and `Completed`; summary metrics visibility; default expansion for active or risky milestones; task grouping visibility inside expanded cards; task link affordances; and decision count versus full-detail behavior. Keep the spec narrow and deterministic instead of depending on unrelated app state.</action>
  <verify><automated>cd frontend && bun x playwright test tests/milestones.spec.ts --workers=1</automated></verify>
  <acceptance_criteria>
    - `frontend/tests/milestones.spec.ts` exists
    - the spec asserts all four planning-state lanes
    - the spec checks expanded milestone detail includes both tasks and decisions
    - `cd frontend && bun x playwright test tests/milestones.spec.ts --workers=1` exits 0
  </acceptance_criteria>
  <done>Phase 28 has a dedicated browser-level safety net for the milestone command view.</done>
</task>

<task type="auto">
  <name>Task 2: Run the Phase 28 release checks and final visual confirmation</name>
  <files>frontend/tests/milestones.spec.ts, backend/tests/test_milestones.py</files>
  <read_first>backend/tests/test_milestones.py, frontend/package.json, frontend/tests/milestones.spec.ts, .planning/phases/28-milestone-planning-decisions/28-VALIDATION.md</read_first>
  <action>Run the explicit Phase 28 release sequence in order: `cd backend && rtk uv run pytest tests/test_milestones.py -q`, `cd frontend && bun run check`, `cd frontend && bun run build`, and `cd frontend && bun x playwright test tests/milestones.spec.ts --workers=1`. After automated checks pass, run the milestone page locally and visually confirm the summary row, lane ordering, risk overlays, expanded active or risky cards, task link handoff, decision counts, and mobile stacked-lane behavior. Keep the manual verification scoped to the milestone command view instead of broadening into unrelated screens.</action>
  <verify><automated>cd backend && rtk uv run pytest tests/test_milestones.py -q && cd ../frontend && bun run check && bun run build && bun x playwright test tests/milestones.spec.ts --workers=1</automated></verify>
  <acceptance_criteria>
    - `cd backend && rtk uv run pytest tests/test_milestones.py -q` succeeds
    - `cd frontend && bun run check` succeeds
    - `cd frontend && bun run build` succeeds
    - `cd frontend && bun x playwright test tests/milestones.spec.ts --workers=1` succeeds
  </acceptance_criteria>
  <done>The milestone command view is covered by focused automated checks and a final visual pass before release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release verification to shipped UX | Automated and manual checks decide whether milestone planning behavior is safe to build on in later phases |
| mocked browser tests to real API behavior | Browser tests use fixtures and must still match the real backend contract |
| mobile and desktop layout variants | The same planning semantics must survive different viewport structures without hiding data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-10 | R | release verification workflow | mitigate | Run the exact backend, type, build, and Playwright commands in a fixed order and record them in the plan so verification is repeatable |
| T-28-11 | T | Playwright mocked milestone fixtures | mitigate | Keep mocks aligned with the real command-view schema and assert the exact lane and decision fields the backend returns |
| T-28-12 | I | mobile milestone presentation | mitigate | Include explicit mobile visual confirmation that stacked collapsible lanes do not hide task or decision context |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd backend && rtk uv run pytest tests/test_milestones.py -q`
- [ ] `cd frontend && bun run check`
- [ ] `cd frontend && bun run build`
- [ ] `cd frontend && bun x playwright test tests/milestones.spec.ts --workers=1`
- [ ] visually confirm on desktop and mobile: summary metrics row, four lane labels, risk-first ordering, default expansion for active or risky milestones, task-link handoff, decision counts on collapsed cards, and full task plus decision detail on expanded cards
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 28 has focused regression coverage for the milestone command view
- Release verification is explicit enough for later phases to trust this planning surface
</success_criteria>

<output>
After completion, create `.planning/phases/28-milestone-planning-decisions/28-04-SUMMARY.md`
</output>
