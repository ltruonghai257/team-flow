---
phase: 28-milestone-planning-decisions
plan: 03
type: execute
wave: 3
depends_on:
  - "28-02"
files_modified:
  - frontend/src/routes/milestones/+page.svelte
  - frontend/src/lib/apis/milestones.ts
  - frontend/src/lib/components/milestones/MilestoneLane.svelte
  - frontend/src/lib/components/milestones/MilestoneCard.svelte
  - frontend/src/lib/components/milestones/MilestoneDecisionForm.svelte
autonomous: true
requirements:
  - ML-01
  - ML-02
  - ML-03
  - ML-04
user_setup: []
must_haves:
  truths:
    - "D-06 D-07 D-08 D-09 D-10: linked tasks appear inside expandable milestone cards, collapsed cards still show task rollups, expanded tasks are grouped by status then due date, task rows open the existing `/tasks?task_id=` flow, and only already-linked tasks are shown."
    - "D-17 D-18 D-19 D-20 D-21: `/milestones` renders the exact four lanes `Planned`, `Committed`, `Active`, and `Completed`; milestones are ordered by risk first then nearest due date inside each lane; active and risky cards expand by default; the page shows a compact summary metrics row; and mobile keeps the same lane model as stacked collapsible sections."
    - "D-04 D-15 D-16: risk remains an overlay signal and decision CRUD lives inside expanded milestone detail instead of a separate workflow dashboard."
  artifacts:
    - path: "frontend/src/routes/milestones/+page.svelte"
      provides: "Milestone command-view page state, data loading, lane orchestration, and modal wiring"
      min_lines: 120
      contains: "commandView"
    - path: "frontend/src/lib/components/milestones/MilestoneCard.svelte"
      provides: "Expandable milestone card with task rollups and decision detail"
      min_lines: 120
      contains: "Proposed"
    - path: "frontend/src/lib/components/milestones/MilestoneLane.svelte"
      provides: "Desktop lane and mobile collapsible-section wrapper"
      min_lines: 60
      contains: "Committed"
    - path: "frontend/src/lib/components/milestones/MilestoneDecisionForm.svelte"
      provides: "Decision create/edit UI used inside expanded milestone cards"
      min_lines: 60
      contains: "status"
  key_links:
    - from: "frontend/src/routes/milestones/+page.svelte"
      to: "frontend/src/lib/apis/milestones.ts"
      via: "command-view data loading and decision CRUD actions"
      pattern: "commandView"
    - from: "frontend/src/lib/components/milestones/MilestoneCard.svelte"
      to: "frontend/src/routes/tasks/+page.svelte"
      via: "task detail link behavior through task_id query params"
      pattern: "task_id"
---

<objective>
Rebuild `/milestones` into the Phase 28 command view using the derived contract and UI specification.

Purpose: Make planning state, linked execution work, and milestone decisions visible together without leaving the existing milestone route.
Output: summary metrics row, four planning-state lanes, expandable milestone cards, linked-task detail, and embedded decision CRUD UI.
</objective>

<execution_context>
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-milestone-planning-decisions/28-CONTEXT.md
@.planning/phases/28-milestone-planning-decisions/28-RESEARCH.md
@.planning/phases/28-milestone-planning-decisions/28-UI-SPEC.md
@.planning/phases/28-milestone-planning-decisions/28-02-SUMMARY.md
@frontend/src/routes/milestones/+page.svelte
@frontend/src/lib/apis/milestones.ts
@frontend/src/routes/tasks/+page.svelte
@frontend/src/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the flat milestone list with summary metrics and lane orchestration</name>
  <files>frontend/src/routes/milestones/+page.svelte, frontend/src/lib/apis/milestones.ts, frontend/src/lib/components/milestones/MilestoneLane.svelte</files>
  <read_first>frontend/src/routes/milestones/+page.svelte, frontend/src/lib/apis/milestones.ts, frontend/src/app.css, .planning/phases/28-milestone-planning-decisions/28-UI-SPEC.md, .planning/phases/28-milestone-planning-decisions/28-CONTEXT.md</read_first>
  <action>Refactor `frontend/src/routes/milestones/+page.svelte` so it loads the dedicated command-view endpoint, preserves the existing milestone create/edit modal flow, and renders a summary metrics row plus four lanes labeled exactly `Planned`, `Committed`, `Active`, and `Completed`. Add `frontend/src/lib/components/milestones/MilestoneLane.svelte` to render each lane consistently on desktop and as collapsible stacked sections on mobile. Keep `milestone_id` route highlighting and scroll-into-view behavior intact.</action>
  <verify><automated>cd frontend && bun run check</automated></verify>
  <acceptance_criteria>
    - `frontend/src/routes/milestones/+page.svelte` contains the lane labels `Planned`, `Committed`, `Active`, and `Completed`
    - the page reads from `commandView` instead of only the flat `list` helper
    - route highlight logic for `milestone_id` still exists
    - `cd frontend && bun run check` exits 0
  </acceptance_criteria>
  <done>The milestone page is structured as a command view with summary metrics and planning-state lanes.</done>
</task>

<task type="auto">
  <name>Task 2: Build expandable milestone cards with linked task detail</name>
  <files>frontend/src/lib/components/milestones/MilestoneCard.svelte, frontend/src/routes/milestones/+page.svelte</files>
  <read_first>frontend/src/lib/components/timeline/TimelineGantt.svelte, frontend/src/routes/tasks/+page.svelte, frontend/src/routes/milestones/+page.svelte, .planning/phases/28-milestone-planning-decisions/28-UI-SPEC.md</read_first>
  <action>Create `frontend/src/lib/components/milestones/MilestoneCard.svelte` and use it from the milestone page. The collapsed header must show title, project name, planning-state badge, risk overlay badge, due-date or date span, task progress summary, and decision counts by status. Expanded cards must reveal only the milestone's already-linked tasks, grouped by status first then due date, with each task row linking into `/tasks?task_id={id}`. Do not add inline task edit controls or task-status mutation in the card.</action>
  <verify><automated>cd frontend && bun run check && bun run build</automated></verify>
  <acceptance_criteria>
    - `frontend/src/lib/components/milestones/MilestoneCard.svelte` exists
    - expanded task rows contain `task_id=` link behavior
    - task groups are rendered by status section before due-date ordering inside each section
    - active or risky cards default to expanded state in code
  </acceptance_criteria>
  <done>Milestone cards expose linked execution work in context without duplicating the task-edit surface.</done>
</task>

<task type="auto">
  <name>Task 3: Add embedded decision CRUD UI inside expanded milestone detail</name>
  <files>frontend/src/lib/components/milestones/MilestoneDecisionForm.svelte, frontend/src/lib/components/milestones/MilestoneCard.svelte, frontend/src/routes/milestones/+page.svelte</files>
  <read_first>frontend/src/lib/components/milestones/MilestoneCard.svelte, frontend/src/routes/milestones/+page.svelte, frontend/src/lib/apis/milestones.ts, .planning/phases/28-milestone-planning-decisions/28-UI-SPEC.md</read_first>
  <action>Create `frontend/src/lib/components/milestones/MilestoneDecisionForm.svelte` for create/edit interactions and wire it into expanded milestone cards. The decision list must show title, status, note, created date, and optional linked task affordance, and the collapsed card must continue to show decision counts only. Support create, edit, and delete within the expanded detail surface, using the exact statuses `proposed`, `approved`, `rejected`, and `superseded`, while keeping the page free of approval workflow or sign-off concepts.</action>
  <verify><automated>cd frontend && bun run check && bun run build</automated></verify>
  <acceptance_criteria>
    - `frontend/src/lib/components/milestones/MilestoneDecisionForm.svelte` exists
    - the UI uses the exact decision statuses `proposed`, `approved`, `rejected`, and `superseded`
    - collapsed cards show decision counts without rendering full notes
    - delete decision uses a destructive confirmation path
  </acceptance_criteria>
  <done>The milestone command view shows and manages structured decisions inside the relevant milestone detail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to command-view API | High-signal milestone, task, and decision data is rendered on one page and must stay within allowed scope |
| browser to decision CRUD | User-entered decision title/note text crosses into mutation routes |
| milestone card to task route handoff | Task links pass identifiers into the existing `/tasks` detail flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07 | I | `/milestones` command view | mitigate | Render only the backend-scoped command-view payload and do not merge in unrelated client-side task datasets |
| T-28-08 | T | decision create/edit UI | mitigate | Send only schema-backed fields (`title`, `status`, `note`, optional `task_id`) and rely on backend validation from Plans 01-02 for scope enforcement |
| T-28-09 | S | task-link handoff to `/tasks?task_id=` | mitigate | Reuse the existing route/query behavior already enforced in the task screen rather than adding a parallel inline task editor with new auth paths |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && bun run check`
- [ ] `cd frontend && bun run build`
- [ ] `/milestones` renders the exact lanes `Planned`, `Committed`, `Active`, and `Completed`
- [ ] task rows in expanded milestone cards link into `/tasks?task_id=`
- [ ] decision CRUD exists only inside expanded milestone detail
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The milestone page is a scan-first command view rather than a flat list
- Linked tasks and decisions are visible together without route churn
</success_criteria>

<output>
After completion, create `.planning/phases/28-milestone-planning-decisions/28-03-SUMMARY.md`
</output>
