---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["backend/app/config.py", "backend/requirements.txt", "backend/app/main.py", "backend/app/routers/auth.py", "backend/app/routers/ai.py"]
autonomous: true
must_haves:
  truths:
    - "Production environment rejects default SECRET_KEY"
    - "CORS origins are dynamically loaded from environment"
    - "Auth and AI endpoints are rate-limited"
  artifacts:
    - path: "backend/app/config.py"
      provides: "Validation logic for SECRET_KEY and ALLOWED_ORIGINS"
    - path: "backend/app/main.py"
      provides: "slowapi initialization and CORS middleware update"
  key_links:
    - from: "backend/app/main.py"
      to: "slowapi"
      via: "limiter state and exception handler"
      pattern: "SlowAPIMiddleware|limiter"
---

<objective>
Implement configuration hardening and rate limiting.
Purpose: Secure the app startup configuration (SECRET_KEY, CORS) and protect critical endpoints.
</objective>

<context>
@.planning/phases/01-production-hardening/01-CONTEXT.md
@.planning/phases/01-production-hardening/01-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Update config.py for SECRET_KEY and ALLOWED_ORIGINS</name>
  <files>backend/app/config.py</files>
  <read_first>backend/app/config.py</read_first>
  <action>Add `ENVIRONMENT: str = "development"` and `ALLOWED_ORIGINS: str = "http://localhost:5173,http://localhost:4173,http://localhost:3000"` to `Settings`. Add a `@field_validator("SECRET_KEY")` that raises `ValueError` if `info.data.get("ENVIRONMENT") == "production"` and the value is `"change-me-in-production"`. Add a `@property` `cors_origins` that splits `ALLOWED_ORIGINS` by `,` and strips whitespace.</action>
  <acceptance_criteria>
    - `backend/app/config.py` contains `ENVIRONMENT` and `ALLOWED_ORIGINS` fields
    - `backend/app/config.py` contains `@field_validator("SECRET_KEY")` logic checking for production
    - `backend/app/config.py` contains `cors_origins` property parsing the string
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Setup slowapi and update CORS in main.py</name>
  <files>backend/requirements.txt, backend/app/main.py</files>
  <read_first>backend/requirements.txt, backend/app/main.py, backend/app/config.py</read_first>
  <action>Add `slowapi` to `backend/requirements.txt`. In `backend/app/main.py`, setup `slowapi` Limiter with `get_remote_address`. Add `app.state.limiter = limiter` and `app.add_exception_handler(RateLimitExceeded, _rate_limit_exceeded_handler)`. Update `CORSMiddleware` setup to use `allow_origins=settings.cors_origins`.</action>
  <acceptance_criteria>
    - `backend/requirements.txt` contains `slowapi`
    - `backend/app/main.py` instantiates `Limiter` and configures `app.state.limiter`
    - `backend/app/main.py` uses `settings.cors_origins` for CORS
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Apply rate limiting to Auth and AI endpoints</name>
  <files>backend/app/routers/auth.py, backend/app/routers/ai.py</files>
  <read_first>backend/app/routers/auth.py, backend/app/routers/ai.py, backend/app/main.py</read_first>
  <action>Import `limiter` from `app.main` and `Request` from `fastapi`. Apply `@limiter.limit("10/minute")` to POST `/login` in `auth.py`. Apply `@limiter.limit("30/minute")` to all routes in `ai.py`. Add `request: Request` parameter to all decorated endpoint signatures if missing.</action>
  <acceptance_criteria>
    - `auth.py` contains `@limiter.limit("10/minute")` on login
    - `ai.py` contains `@limiter.limit("30/minute")`
    - Decorated functions accept `request: Request`
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] Pytest passes for rate limiting and config
- [ ] `grep -q 'limiter.limit' backend/app/routers/auth.py` succeeds
</verification>
