---
phase: "01"
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["backend/alembic.ini", "backend/alembic/env.py", "backend/app/main.py"]
autonomous: true
must_haves:
  truths:
    - "Schema runs via Alembic migration runner on startup"
  artifacts:
    - path: "backend/alembic/env.py"
      provides: "Alembic env configuration"
      contains: "target_metadata"
    - path: "backend/app/main.py"
      provides: "Alembic upgrade head in lifespan"
      contains: "asyncio.to_thread"
  key_links:
    - from: "backend/app/main.py"
      to: "alembic.command"
      via: "upgrade head"
      pattern: "command\\.upgrade"
---

<objective>
Setup Alembic migrations and run them programmatically on startup.
Purpose: Replace `create_all` with a robust migration strategy for production DB changes.
</objective>

<context>
@.planning/phases/01-production-hardening/01-CONTEXT.md
@.planning/phases/01-production-hardening/01-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Initialize Alembic with async template</name>
  <files>backend/alembic.ini, backend/alembic/env.py</files>
  <read_first>backend/alembic/env.py</read_first>
  <action>Run `alembic init -t async alembic` from `backend` directory. Edit `backend/alembic/env.py` to add `from app.database import Base` and `target_metadata = Base.metadata`. Ensure it uses `settings.DATABASE_URL` via `from app.config import settings` and `config.set_main_option("sqlalchemy.url", settings.DATABASE_URL)`.</action>
  <acceptance_criteria>
    - `backend/alembic/env.py` exists
    - `backend/alembic/env.py` contains `target_metadata = Base.metadata`
    - `backend/alembic/env.py` configures `sqlalchemy.url` dynamically
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Generate initial migration</name>
  <files>backend/alembic/versions/</files>
  <read_first>backend/alembic/env.py</read_first>
  <action>Run `alembic revision --autogenerate -m "initial migration"` inside `backend` directory.</action>
  <acceptance_criteria>
    - `backend/alembic/versions/` directory contains a python migration script
    - `grep -q "def upgrade" backend/alembic/versions/*.py` succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Replace create_all with alembic in lifespan</name>
  <files>backend/app/main.py, backend/app/database.py</files>
  <read_first>backend/app/main.py, backend/app/database.py</read_first>
  <action>In `backend/app/main.py`, remove `async with engine.begin() as conn: await conn.run_sync(Base.metadata.create_all)`. Replace it with a synchronous migration runner function using `alembic.command.upgrade(alembic_cfg, "head")`, and run it asynchronously in the lifespan using `await asyncio.to_thread(run_migrations)`. If migration fails, log error and `sys.exit(1)`.</action>
  <acceptance_criteria>
    - `backend/app/main.py` contains `import asyncio` and `from alembic import command`
    - `backend/app/main.py` contains `sys.exit(1)` on error
    - `create_all` is fully removed
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `grep -q 'alembic upgrade' backend/app/main.py` succeeds
- [ ] `grep -q 'create_all' backend/app/main.py` fails
</verification>
