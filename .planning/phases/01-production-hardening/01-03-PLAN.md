---
phase: "01"
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: ["backend/app/auth.py", "backend/app/routers/tasks.py", "backend/app/routers/ai.py", "backend/app/routers/dashboard.py", "backend/app/routers/notifications.py", "backend/app/routers/websocket.py", "backend/app/models.py"]
autonomous: true
must_haves:
  truths:
    - "No datetime.utcnow() is used in the codebase"
  artifacts:
    - path: "backend/app/models.py"
      provides: "Native UTC timestamps for new rows"
      contains: "datetime.now(timezone.utc).replace(tzinfo=None)"
  key_links: []
---

<objective>
Replace `datetime.utcnow()` to prevent future deprecation breaks.
Purpose: Use `datetime.now(timezone.utc).replace(tzinfo=None)` throughout to maintain naive UTC timestamps without schema changes.
</objective>

<context>
@.planning/phases/01-production-hardening/01-CONTEXT.md
@.planning/phases/01-production-hardening/01-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Replace utcnow in models.py</name>
  <files>backend/app/models.py</files>
  <read_first>backend/app/models.py</read_first>
  <action>Find `datetime.utcnow` and replace it with a lambda returning `datetime.now(timezone.utc).replace(tzinfo=None)`. Import `from datetime import datetime, timezone`.</action>
  <acceptance_criteria>
    - `backend/app/models.py` imports `timezone`
    - `backend/app/models.py` contains `datetime.now(timezone.utc).replace(tzinfo=None)`
    - `backend/app/models.py` does not contain `datetime.utcnow()`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Replace utcnow in auth and routers</name>
  <files>backend/app/auth.py, backend/app/routers/tasks.py, backend/app/routers/ai.py, backend/app/routers/dashboard.py, backend/app/routers/notifications.py, backend/app/routers/websocket.py</files>
  <read_first>backend/app/auth.py, backend/app/routers/tasks.py, backend/app/routers/ai.py, backend/app/routers/dashboard.py, backend/app/routers/notifications.py, backend/app/routers/websocket.py</read_first>
  <action>Globally find and replace `datetime.utcnow()` with `datetime.now(timezone.utc).replace(tzinfo=None)`. Add `from datetime import datetime, timezone` to each file. Ensure the imports are correctly structured and no duplicate imports.</action>
  <acceptance_criteria>
    - No router files contain `datetime.utcnow()`
    - Router files correctly import `datetime, timezone`
    - Calls correctly use `datetime.now(timezone.utc).replace(tzinfo=None)`
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `grep -r "utcnow" backend/app/` returns no results
</verification>
