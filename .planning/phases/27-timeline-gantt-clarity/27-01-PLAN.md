---
phase: 27-timeline-gantt-clarity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/timeline.py
  - backend/app/schemas/work.py
  - backend/tests/test_timeline.py
  - frontend/src/lib/apis/timeline.ts
autonomous: true
requirements:
  - TL-02
  - TL-03
  - TL-05
user_setup: []
must_haves:
  truths:
    - "The timeline API returns enough existing milestone and task metadata to derive milestone progress, planning-window, risk, and lightweight decision signals without adding new persistence."
    - "Timeline visibility semantics for member, supervisor, and admin users remain unchanged after the payload expansion."
    - "Frontend timeline consumers use explicit typed contracts instead of raw any-shaped payloads."
  artifacts:
    - path: "backend/app/routers/timeline.py"
      provides: "Expanded timeline payload construction with existing visibility filters preserved"
      min_lines: 50
      contains: "TimelineMilestoneOut"
    - path: "backend/app/schemas/work.py"
      provides: "Timeline-specific response schemas for richer milestone and task metadata"
      min_lines: 40
      contains: "class TimelineTaskOut"
    - path: "backend/tests/test_timeline.py"
      provides: "Visibility and response-shape regression coverage for /api/timeline"
      min_lines: 50
    - path: "frontend/src/lib/apis/timeline.ts"
      provides: "Typed frontend timeline API contract"
      min_lines: 20
      exports:
        - "TimelineProject"
        - "TimelineMilestone"
        - "TimelineTask"
  key_links:
    - from: "backend/app/routers/timeline.py"
      to: "backend/app/schemas/work.py"
      via: "Timeline response models"
      pattern: "Timeline"
    - from: "frontend/src/lib/apis/timeline.ts"
      to: "backend/app/routers/timeline.py"
      via: "Shared milestone/task payload shape"
      pattern: "timeline"
---

<objective>
Expand the timeline data contract before changing the visual timeline.

Purpose: Give the Phase 27 UI enough existing data to render milestone-first rollups, derived risk, and lightweight decision hints without inventing new storage.
Output: A richer `/api/timeline` response, typed frontend timeline interfaces, and targeted API regression coverage.
</objective>

<execution_context>
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-timeline-gantt-clarity/27-CONTEXT.md
@.planning/phases/27-timeline-gantt-clarity/27-RESEARCH.md
@backend/app/models/work.py
@backend/app/schemas/work.py
@backend/app/routers/timeline.py
@backend/tests/test_timeline.py
@frontend/src/lib/apis/timeline.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the timeline API payload using existing milestone and task fields</name>
  <files>backend/app/routers/timeline.py, backend/app/schemas/work.py</files>
  <read_first>backend/app/routers/timeline.py, backend/app/schemas/work.py, backend/app/models/work.py, .planning/phases/27-timeline-gantt-clarity/27-CONTEXT.md</read_first>
  <action>Update the timeline-specific schemas and router so `/api/timeline/` exposes the existing fields Phase 27 needs for derived milestone UI: milestone description and completed state; task metadata such as description, tags, and custom status; and any related eager-loading required for custom-status-based risk signal. Reuse current project/milestone/task models and preserve the current member/supervisor/admin filtering behavior exactly. Do not add new tables, background jobs, or stored risk/decision fields.</action>
  <verify><automated>cd backend && pytest tests/test_timeline.py -q</automated></verify>
  <acceptance_criteria>
    - timeline milestone objects expose existing fields needed for planning-window, progress, and lightweight decision derivation
    - timeline task objects expose existing fields needed for blocked/custom-status and tag/description-based signal derivation
    - current role-based visibility on `/api/timeline/` stays intact
  </acceptance_criteria>
  <done>The backend timeline contract exposes the existing milestone/task inputs the UI needs, without adding persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the contract with backend assertions and typed frontend interfaces</name>
  <files>backend/tests/test_timeline.py, frontend/src/lib/apis/timeline.ts</files>
  <read_first>backend/tests/test_timeline.py, frontend/src/lib/apis/timeline.ts, frontend/src/routes/timeline/+page.svelte</read_first>
  <action>Replace the current placeholder assertions in `backend/tests/test_timeline.py` with focused coverage that proves both visibility and payload shape for Phase 27. In `frontend/src/lib/apis/timeline.ts`, replace raw `any` usage with exported `TimelineProject`, `TimelineMilestone`, `TimelineTask`, and any supporting types needed by the timeline route/components. Keep the frontend wrapper narrow; this plan is defining the contract, not building the derived view model yet.</action>
  <verify><automated>cd backend && pytest tests/test_timeline.py -q && cd ../frontend && bun run check</automated></verify>
  <acceptance_criteria>
    - `backend/tests/test_timeline.py` asserts member, supervisor, and admin timeline responses using the real route
    - the test file checks the presence of the new milestone/task contract fields, not only status code 200
    - `frontend/src/lib/apis/timeline.ts` exports explicit timeline interfaces used by later Phase 27 UI work
  </acceptance_criteria>
  <done>The timeline API contract is typed and covered, giving the UI refactor a stable data foundation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && pytest tests/test_timeline.py -q`
- [ ] `cd frontend && bun run check`
- [ ] `backend/app/routers/timeline.py` still preserves current role-based timeline filtering
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Existing milestone/task data is sufficient for Phase 27 UI derivation without new persistence
- Frontend timeline code has an explicit typed contract instead of raw `any` payloads
</success_criteria>

<output>
After completion, create `.planning/phases/27-timeline-gantt-clarity/27-01-SUMMARY.md`
</output>
