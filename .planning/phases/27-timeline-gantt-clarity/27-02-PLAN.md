---
phase: 27-timeline-gantt-clarity
plan: 02
type: execute
wave: 2
depends_on:
  - "27-01"
files_modified:
  - frontend/src/routes/timeline/+page.svelte
  - frontend/src/lib/components/timeline/TimelineGantt.svelte
  - frontend/src/lib/components/timeline/TimelineToolbar.svelte
  - frontend/src/lib/components/timeline/timeline-view-model.ts
autonomous: true
requirements:
  - TL-01
  - TL-02
  - TL-03
  - TL-04
  - TL-05
user_setup: []
must_haves:
  truths:
    - "Project view treats milestones as expandable parent planning rows that are visually distinct from draggable task bars."
    - "Milestone rows expose progress, date span, and compact risk/decision/planning signals using derived data from the Phase 27 contract."
    - "Switching between project and member views preserves the selected date range plus the focused milestone/task context."
    - "By Member view remains people-first while still showing milestone context for each task."
  artifacts:
    - path: "frontend/src/lib/components/timeline/timeline-view-model.ts"
      provides: "Derived milestone/task grouping, sorting, signal, and focus helpers for the timeline UI"
      min_lines: 60
      exports:
        - "buildTimelineViewModel"
        - "deriveMilestoneSignals"
    - path: "frontend/src/lib/components/timeline/TimelineGantt.svelte"
      provides: "Milestone-first gantt rendering with expandable parent rows and focus-aware member view cues"
      min_lines: 120
      contains: "headerHtml"
    - path: "frontend/src/routes/timeline/+page.svelte"
      provides: "Page-level focus state and preserved project/member timeline context"
      min_lines: 60
      contains: "viewMode"
  key_links:
    - from: "frontend/src/routes/timeline/+page.svelte"
      to: "frontend/src/lib/components/timeline/timeline-view-model.ts"
      via: "derived milestone/task presentation state"
      pattern: "focus"
    - from: "frontend/src/lib/components/timeline/TimelineGantt.svelte"
      to: "frontend/src/lib/apis/timeline.ts"
      via: "typed milestone/task payload and derived signals"
      pattern: "Timeline"
---

<objective>
Rebuild `/timeline` into a milestone-first planning surface without changing the route or task-edit workflow.

Purpose: Make milestones readable as primary planning objects, keep project/member switching understandable, and expose linked task progress plus risk/decision signal in place.
Output: A milestone-first gantt UI, page-level focus continuity, and member-view milestone context cues.
</objective>

<execution_context>
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-timeline-gantt-clarity/27-CONTEXT.md
@.planning/phases/27-timeline-gantt-clarity/27-RESEARCH.md
@.planning/phases/27-timeline-gantt-clarity/27-01-SUMMARY.md
@.planning/phases/27-timeline-gantt-clarity/27-UI-SPEC.md
@frontend/src/routes/timeline/+page.svelte
@frontend/src/lib/components/timeline/TimelineGantt.svelte
@frontend/src/lib/components/timeline/TimelineToolbar.svelte
@frontend/src/lib/apis/timeline.ts
@frontend/node_modules/svelte-gantt/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the derived timeline view model and preserve focus state at the page level</name>
  <files>frontend/src/routes/timeline/+page.svelte, frontend/src/lib/components/timeline/timeline-view-model.ts</files>
  <read_first>frontend/src/routes/timeline/+page.svelte, frontend/src/lib/apis/timeline.ts, .planning/phases/27-timeline-gantt-clarity/27-CONTEXT.md, .planning/phases/27-timeline-gantt-clarity/27-UI-SPEC.md</read_first>
  <action>Create a dedicated helper module that converts the typed timeline payload into milestone-first presentation data: progress rollups, task-state counts, task ordering by status then due date, derived risk level, lightweight decision markers from existing description/tag content, and default expansion for active/risky milestones. In `+page.svelte`, add page-level focused milestone/task state that survives view-mode toggles and data reloads, and keep the selected date range untouched. Preserve the existing task edit modal and reload-after-save/reschedule flow.</action>
  <verify><automated>cd frontend && bun run check</automated></verify>
  <acceptance_criteria>
    - a dedicated helper module exists for milestone/task derivation instead of burying all logic inline in `+page.svelte`
    - focused milestone/task context survives project/member view switches and timeline reloads
    - derived milestone expansion, progress, and signal logic uses only existing payload data from Plan 01
  </acceptance_criteria>
  <done>The page has stable focus state and a reusable milestone-first view model for the timeline surface.</done>
</task>

<task type="auto">
  <name>Task 2: Render milestone-first gantt rows and member-view context cues</name>
  <files>frontend/src/lib/components/timeline/TimelineGantt.svelte, frontend/src/lib/components/timeline/TimelineToolbar.svelte, frontend/src/routes/timeline/+page.svelte</files>
  <read_first>frontend/src/lib/components/timeline/TimelineGantt.svelte, frontend/node_modules/svelte-gantt/README.md, .planning/phases/27-timeline-gantt-clarity/27-UI-SPEC.md</read_first>
  <action>Refactor `TimelineGantt.svelte` to use `svelte-gantt` tree rows and rich row headers so milestone parent rows become distinct planning objects instead of plain labels. Milestone parent rows must show title, status, date span, progress, and compact risk/decision/planning badges while task bars remain the draggable/editable items underneath. In member view, keep rows people-first but show milestone badges or links on task rows and surface a focused-milestone banner when the focused milestone is not naturally visible. Adjust `TimelineToolbar.svelte` only as needed to support the new copy or focus affordances; do not rename the existing view labels or range controls.</action>
  <verify><automated>cd frontend && bun run check && bun run build</automated></verify>
  <acceptance_criteria>
    - milestone parent rows are visually distinct, non-draggable, and expandable/collapsible
    - project view exposes linked task progress and counts without forcing a route change
    - member view stays organized around people while still preserving milestone context
    - task click still opens the edit modal and task drag still reschedules tasks
  </acceptance_criteria>
  <done>The timeline UI reads milestone-first in project view and stays understandable in member view without losing existing task interactions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && bun run check`
- [ ] `cd frontend && bun run build`
- [ ] `/timeline` still keeps the existing task edit modal and task drag-to-reschedule behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Milestones are distinct planning objects with progress and derived signal visibility
- Project/member switching preserves date range and focus context
- No new persistence or route churn is introduced
</success_criteria>

<output>
After completion, create `.planning/phases/27-timeline-gantt-clarity/27-02-SUMMARY.md`
</output>
