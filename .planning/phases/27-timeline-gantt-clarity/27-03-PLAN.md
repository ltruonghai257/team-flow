---
phase: 27-timeline-gantt-clarity
plan: 03
type: execute
wave: 3
depends_on:
  - "27-02"
files_modified:
  - frontend/tests/timeline-gantt.spec.ts
autonomous: true
requirements:
  - TL-01
  - TL-02
  - TL-03
  - TL-04
  - TL-05
user_setup: []
must_haves:
  truths:
    - "Automated regression coverage proves milestone-first timeline behavior renders correctly in a browser instead of relying only on static type/build checks."
    - "Release verification covers the backend timeline contract, frontend type/build safety, and the targeted timeline UI behavior together."
    - "The executor is told exactly what to visually confirm after automated checks pass."
  artifacts:
    - path: "frontend/tests/timeline-gantt.spec.ts"
      provides: "Targeted Playwright regression coverage for milestone-first timeline behavior"
      min_lines: 50
  key_links:
    - from: "frontend/tests/timeline-gantt.spec.ts"
      to: "frontend/src/routes/timeline/+page.svelte"
      via: "project/member switching and focus persistence assertions"
      pattern: "Timeline"
    - from: "frontend/tests/timeline-gantt.spec.ts"
      to: "frontend/src/lib/components/timeline/TimelineGantt.svelte"
      via: "milestone-first row and signal assertions"
      pattern: "milestone"
---

<objective>
Lock in the milestone-first timeline behavior with targeted regression coverage and release verification.

Purpose: Catch regressions in milestone rendering, focus continuity, and member-view context before Phase 28 builds on the new planning surface.
Output: A dedicated Playwright timeline spec plus explicit final verification steps.
</objective>

<execution_context>
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-timeline-gantt-clarity/27-CONTEXT.md
@.planning/phases/27-timeline-gantt-clarity/27-RESEARCH.md
@.planning/phases/27-timeline-gantt-clarity/27-02-SUMMARY.md
@.planning/phases/27-timeline-gantt-clarity/27-UI-SPEC.md
@frontend/tests/helpers/auth.ts
@frontend/playwright.config.ts
@frontend/src/routes/timeline/+page.svelte
@frontend/src/lib/components/timeline/TimelineGantt.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add targeted Playwright coverage for the milestone-first timeline flow</name>
  <files>frontend/tests/timeline-gantt.spec.ts</files>
  <read_first>frontend/tests/navigation-groups.spec.ts, frontend/tests/helpers/auth.ts, frontend/playwright.config.ts, frontend/src/routes/timeline/+page.svelte</read_first>
  <action>Create `frontend/tests/timeline-gantt.spec.ts` as a focused browser-level regression for Phase 27. Cover the milestone-first affordances that broad demo tests will miss: milestone parent rows are visibly distinct from task bars, collapsed milestone rows still expose progress/task counts, project/member switching preserves the chosen date range, and member view retains milestone context or the focused-milestone banner. Prefer narrow route mocking or stable fixture setup over brittle dependence on unrelated app state.</action>
  <verify><automated>cd frontend && bunx playwright test tests/timeline-gantt.spec.ts</automated></verify>
  <acceptance_criteria>
    - the new spec asserts milestone-first rendering behavior rather than only page navigation
    - the spec checks project/member switching without resetting the chosen date range
    - the spec verifies milestone context remains understandable in member view
  </acceptance_criteria>
  <done>Phase 27 has dedicated browser coverage for the milestone-first timeline experience.</done>
</task>

<task type="auto">
  <name>Task 2: Run the Phase 27 release checks and do a final visual confirmation</name>
  <files>frontend/tests/timeline-gantt.spec.ts</files>
  <read_first>frontend/package.json, backend/tests/test_timeline.py</read_first>
  <action>Run the Phase 27 release checks in a narrow, explicit order: backend timeline tests, frontend type checks, frontend build, and the targeted Playwright timeline spec. After automated checks pass, run the timeline locally and visually confirm milestone rows, progress badges, risk/decision signals, expansion defaults, focused-milestone banner behavior, and preserved date range across project/member switching. Keep the verification scoped to the Phase 27 surfaces rather than broadening into unrelated regressions.</action>
  <verify><automated>cd backend && pytest tests/test_timeline.py -q && cd ../frontend && bun run check && bun run build && bunx playwright test tests/timeline-gantt.spec.ts</automated></verify>
  <acceptance_criteria>
    - `pytest tests/test_timeline.py -q` succeeds
    - `bun run check` succeeds
    - `bun run build` succeeds
    - `bunx playwright test tests/timeline-gantt.spec.ts` succeeds
  </acceptance_criteria>
  <done>The milestone-first timeline implementation is covered by focused automated checks and a final visual pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && pytest tests/test_timeline.py -q`
- [ ] `cd frontend && bun run check`
- [ ] `cd frontend && bun run build`
- [ ] `cd frontend && bunx playwright test tests/timeline-gantt.spec.ts`
- [ ] Run the frontend locally and visually confirm: milestone parent rows are distinct from task bars, collapsed milestone rows still show progress and task-state counts, risk/decision/planning badges stay compact, task click still opens the edit modal, task drag still reschedules task bars only, and switching between By Project and By Member preserves the selected date range plus the focused milestone/task context
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 27 behavior is covered by a dedicated timeline browser regression
- Final verification explicitly checks both the milestone-first UI and preserved context rules
</success_criteria>

<output>
After completion, create `.planning/phases/27-timeline-gantt-clarity/27-03-SUMMARY.md`
</output>
