---
phase: "22-runtime-integration-regression-verification"
plan: 2
type: execute
wave: 2
depends_on: [1]
files_modified:
  - ".planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md"
autonomous: true
user_setup:
  - "If `python` (3.12) and `bun` are not available locally, document the exact blocker and run the strongest available fallback (e.g., CI run, alternate Python on PATH, npm-based svelte-check)."
requirements: ["VERIFY-01", "VERIFY-02", "RUN-02", "RUN-03"]
must_haves:
  truths:
    - "D-06.1 - Backend pytest suite runs and is recorded as part of the verification floor."
    - "D-06.2 - Frontend `bun run check` and `bun run build` run and are recorded."
    - "D-07 - Any import bug discovered during verification is fixed in this plan as a bug fix, aligned with Phase 20/21 target structures."
    - "D-11 - Environment-blocked checks document the exact blocker and the next-best fallback that ran."
  artifacts:
    - path: ".planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md"
      provides: "Recorded backend and frontend verification floor results"
      contains: "## Backend pytest"
  key_links:
    - ".planning/phases/22-runtime-integration-regression-verification/22-CONTEXT.md#verification-floor"
    - ".planning/phases/22-runtime-integration-regression-verification/22-VALIDATION.md"
---

# Phase 22 Plan 02: Backend Tests + Frontend Build Verification

<objective>
Execute the automated half of the verification floor — backend pytest, frontend type-check, and frontend production build — against the integrated post-Phase-20/21 codebase, and record the results in `22-VERIFICATION.md`. Fix any import bugs discovered as part of this plan.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-runtime-integration-regression-verification/22-CONTEXT.md
@.planning/phases/22-runtime-integration-regression-verification/22-RESEARCH.md
@.planning/phases/22-runtime-integration-regression-verification/22-VALIDATION.md
@.planning/phases/22-runtime-integration-regression-verification/22-01-PLAN.md
@.planning/phases/20-backend-package-restructure/20-SUMMARY.md
@.planning/phases/21-frontend-sveltekit-structure/21-04-SUMMARY.md
</context>

<tasks>

<task id="22-02-01" type="execute">
  <name>Run backend pytest suite and record result</name>
  <files>.planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md</files>
  <read_first>backend/tests/conftest.py, backend/tests/test_package_structure.py, .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md</read_first>
  <action>
Run the backend pytest suite from the `backend/` directory:

```
cd backend && python -m pytest -q
```

If `python` is not available, try `python3 -m pytest -q`. If neither runs, document the blocker (D-11) and run the strongest fallback in this priority order:

1. `cd backend && python -m pytest backend/tests/test_package_structure.py -q` (subset; structure tests added in Phase 20-05).
2. CI / external runner output if available — paste the summary line.
3. Static fallback: `rtk rg "def test_" backend/tests/` to enumerate test names; mark the section as fallback-only.

In `22-VERIFICATION.md`, add a section:

```
## Backend pytest

- Command run: <exact command>
- Result: <pass/fail>
- Summary line: <e.g., `45 passed in 12.3s`>
- Failed tests: <none or list>
- Blocker (if any): <description + fallback used>
- Run timestamp: <ISO date>
```

If any test fails because of a Phase 20 import bug, fix the offending import in the relevant module under `backend/app/` (D-07), re-run pytest, and update the recorded summary line. Do not weaken or skip tests, and do not modify migration files.
  </action>
  <acceptance_criteria>
    - `22-VERIFICATION.md` contains the heading `## Backend pytest`
    - The section records the exact command run, the result, and a summary line (or a fallback note)
    - If any pytest failure was fixed, the fix lives in `backend/app/` source (not in tests, not in migrations)
    - A run timestamp is present
  </acceptance_criteria>
  <verify>`rtk rg "## Backend pytest" .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md && rtk rg "Command run|Result|Summary line" .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md`</verify>
  <done>Backend test result is recorded; any introduced import regression is fixed.</done>
</task>

<task id="22-02-02" type="execute">
  <name>Run frontend bun check and record result</name>
  <files>.planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md</files>
  <read_first>frontend/package.json, .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md, .planning/phases/21-frontend-sveltekit-structure/21-04-SUMMARY.md</read_first>
  <action>
Run frontend type-check:

```
cd frontend && bun run check
```

If `bun` is not on PATH, document the exact blocker and try fallbacks in priority order:

1. `cd frontend && bunx svelte-check`
2. `cd frontend && npx svelte-check`
3. `cd frontend && npm run check` if `package.json` exposes the script and npm is available.
4. Static fallback: confirm `frontend/svelte.config.js` and `frontend/tsconfig.json` exist; record as fallback-only.

Phase 21-04 SUMMARY established a baseline of `0 errors, 9 pre-existing warnings`. Do not introduce new errors. New warnings should be inspected; if any are introduced by Phase 22 changes, fix them now.

Record in `22-VERIFICATION.md` under heading `## Frontend check`:

```
## Frontend check

- Command run: <exact command>
- Result: <pass/fail>
- Errors: <count>
- Warnings: <count> (baseline was 9 from Phase 21-04)
- New warnings introduced by Phase 22: <list or "none">
- Blocker (if any): <description + fallback used>
- Run timestamp: <ISO date>
```
  </action>
  <acceptance_criteria>
    - `22-VERIFICATION.md` contains the heading `## Frontend check`
    - Errors count is `0` (or the failure is fixed and re-recorded)
    - Warning delta against the Phase 21-04 baseline is recorded
    - Blocker + fallback documented if `bun` was unavailable
  </acceptance_criteria>
  <verify>`rtk rg "## Frontend check" .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md && rtk rg "Errors:|Warnings:" .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md`</verify>
  <done>Frontend type-check result is recorded with zero new errors.</done>
</task>

<task id="22-02-03" type="execute">
  <name>Run frontend bun build and record result</name>
  <files>.planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md</files>
  <read_first>frontend/package.json, frontend/svelte.config.js, .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md</read_first>
  <action>
Run frontend production build:

```
cd frontend && bun run build
```

Fallbacks in priority order if `bun` is unavailable:

1. `cd frontend && bunx vite build`
2. `cd frontend && npx vite build`
3. `cd frontend && npm run build`
4. Static fallback: confirm `frontend/build/` directory exists from a prior run, and document the date of the last successful build (record as fallback-only).

Record in `22-VERIFICATION.md` under heading `## Frontend build`:

```
## Frontend build

- Command run: <exact command>
- Result: <pass/fail>
- Build duration: <e.g., `built in 9.67s`>
- Output directory: `frontend/build`
- Adapter: adapter-static, fallback `200.html` (from STATE.md)
- Blocker (if any): <description + fallback used>
- Run timestamp: <ISO date>
```

Do not commit `frontend/build/` (gitignored). If the build fails because of a Phase 21 import bug discovered now, fix the import in `frontend/src/` and re-run.
  </action>
  <acceptance_criteria>
    - `22-VERIFICATION.md` contains the heading `## Frontend build`
    - The section records command, pass/fail, and build duration (or a fallback note)
    - `frontend/build/` is referenced as the output directory
    - Any introduced import regression is fixed in `frontend/src/`
  </acceptance_criteria>
  <verify>`rtk rg "## Frontend build" .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md && rtk rg "Build duration|Output directory" .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md`</verify>
  <done>Frontend production build result is recorded; build artefact path captured.</done>
</task>

<task id="22-02-04" type="execute">
  <name>Cross-check verification floor coverage</name>
  <files>.planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md</files>
  <read_first>.planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md, .planning/phases/22-runtime-integration-regression-verification/22-VALIDATION.md</read_first>
  <action>
Append a `## Verification Floor Coverage (Plan 22-02)` summary block to `22-VERIFICATION.md` that lists the four required layers with current status (Plan 22-02 is responsible for the first two automated layers; Plan 22-03 fills in the remaining two):

```
## Verification Floor Coverage (Plan 22-02)

| Layer | Required | Status After Plan 22-02 | Plan |
|-------|----------|-------------------------|------|
| Backend tests (VERIFY-01) | yes | <pass/fail/fallback> | 22-02 |
| Frontend check + build (VERIFY-02) | yes | <pass/fail/fallback> | 22-02 |
| Playwright E2E | yes | pending | 22-03 |
| Manual smoke (VERIFY-03) | yes | pending | 22-03 |
```

Do not declare the phase complete here — Plan 22-04 owns final signoff.
  </action>
  <acceptance_criteria>
    - `22-VERIFICATION.md` contains the heading `## Verification Floor Coverage (Plan 22-02)`
    - The table lists exactly the four layers above with explicit status entries for the first two and `pending` for the last two
    - The table does not mark the phase complete
  </acceptance_criteria>
  <verify>`rtk rg "## Verification Floor Coverage \(Plan 22-02\)|Backend tests \(VERIFY-01\)|Frontend check \+ build \(VERIFY-02\)|Playwright E2E|Manual smoke \(VERIFY-03\)" .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md`</verify>
  <done>Coverage matrix exists and clearly hands off the remaining two layers to Plan 22-03.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Integrity | Backend test suite | mitigate | Run pytest from `backend/`; if a failure surfaces, fix imports in `app/` only — never modify tests or migration files |
| T-22-06 | Integrity | Frontend type-check / build | mitigate | Run `bun run check` and `bun run build`; compare warning delta against Phase 21-04 baseline; fix any new error in source |
| T-22-07 | Confidentiality | Captured logs | mitigate | When recording results, include only summary lines and counts; do not paste tokens, secrets, or `.env` values into `22-VERIFICATION.md` |
</threat_model>

<verification>
- `rtk rg "## Backend pytest|## Frontend check|## Frontend build|## Verification Floor Coverage" .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md`
- `rtk rg "Command run|Result|Summary line|Errors:|Warnings:|Build duration" .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md`
- `cd backend && python -m pytest -q` (or fallback) exits 0
- `cd frontend && bun run check` (or fallback) reports 0 errors
- `cd frontend && bun run build` (or fallback) exits 0
</verification>

<success_criteria>
- Backend pytest, frontend check, and frontend build are all recorded in `22-VERIFICATION.md` with pass/fail and a timestamp.
- No new errors introduced relative to the Phase 20-05 / 21-04 baselines.
- Any environment blocker has a documented fallback that actually ran.
- The coverage matrix clearly hands the remaining two layers to Plan 22-03.
</success_criteria>
