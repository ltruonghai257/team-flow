---
phase: "22-runtime-integration-regression-verification"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - "supervisord.conf"
  - "backend/Dockerfile"
autonomous: true
user_setup:
  - "If `python` (3.12) is not on PATH during execution, document the exact blocker and use the strongest available local fallback to import-check `app.api.main`."
requirements: ["RUN-01", "RUN-03", "BACK-04"]
must_haves:
  truths:
    - "D-01 - supervisord and the backend Dockerfile use canonical `app.api.main:app` as the uvicorn target."
    - "D-02 - `alembic.ini` and `backend/alembic/env.py` resolve `Base.metadata` via the refactored package paths without rewriting migration history."
    - "D-03 - `docker-compose.yml`, root `Dockerfile`, and Azure deploy scripts continue to function with the refactored entrypoint."
    - "D-04/D-05 - Phase 20 compatibility shims remain intact; this plan does not delete them."
  artifacts:
    - path: "supervisord.conf"
      provides: "Process manager runs uvicorn against the canonical app"
      contains: "app.api.main:app"
    - path: "backend/Dockerfile"
      provides: "Compose/dev backend image starts the canonical app"
      contains: "app.api.main:app"
  key_links:
    - ".planning/phases/22-runtime-integration-regression-verification/22-CONTEXT.md#runtime-entrypoints"
    - ".planning/phases/22-runtime-integration-regression-verification/22-RESEARCH.md#current-runtime-entrypoint-state-post-phase-2021"
    - ".planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md"
    - ".planning/phases/20-backend-package-restructure/20-SUMMARY.md"
---

# Phase 22 Plan 01: Runtime Entrypoint Update

<objective>
Repoint the live uvicorn target from the Phase 20 delegate `app.main:app` to the canonical `app.api.main:app` in supervisord and the backend Dockerfile, and verify Alembic, root Dockerfile, docker-compose, and Azure deploy scripts still align with the refactored package — without deleting any Phase 20 shim.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-runtime-integration-regression-verification/22-CONTEXT.md
@.planning/phases/22-runtime-integration-regression-verification/22-RESEARCH.md
@.planning/phases/22-runtime-integration-regression-verification/22-VALIDATION.md
@.planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md
@.planning/phases/20-backend-package-restructure/20-SUMMARY.md
</context>

<tasks>

<task id="22-01-01" type="execute">
  <name>Update supervisord uvicorn target to canonical app</name>
  <files>supervisord.conf</files>
  <read_first>supervisord.conf, backend/app/api/main.py, backend/app/main.py, .planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md</read_first>
  <action>
In `supervisord.conf`, update the `[program:uvicorn]` `command=` line so the uvicorn target is `app.api.main:app` instead of `app.main:app`. Keep every other field unchanged: `directory=/app/backend`, `--host 127.0.0.1`, `--port 8000`, `--workers 1`, `autostart`, `autorestart`, `priority`, log redirection, and `environment=PYTHONUNBUFFERED="1"`.

Do not modify any other supervisord program (`[program:nginx]` stays as-is). Do not touch `nginx.conf`.

Do not delete `backend/app/main.py`; the shim must remain per CONTEXT D-04.
  </action>
  <acceptance_criteria>
    - `supervisord.conf` contains `app.api.main:app`
    - `supervisord.conf` no longer contains `app.main:app` (except inside comments, which there are none currently)
    - `[program:uvicorn]` still has `directory=/app/backend`
    - `[program:uvicorn]` still has `--host 127.0.0.1 --port 8000 --workers 1`
    - `[program:nginx]` block is unchanged
    - `backend/app/main.py` still exists
  </acceptance_criteria>
  <verify>`rtk rg "app\.api\.main:app" supervisord.conf && rtk rg "app\.main:app" supervisord.conf` — second command must return no matches; `test -f backend/app/main.py`</verify>
  <done>supervisord launches uvicorn against the canonical app entrypoint.</done>
</task>

<task id="22-01-02" type="execute">
  <name>Update backend Dockerfile uvicorn CMD to canonical app</name>
  <files>backend/Dockerfile</files>
  <read_first>backend/Dockerfile, backend/app/api/main.py, docker-compose.yml</read_first>
  <action>
In `backend/Dockerfile`, change the final `CMD` line from `CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]` to `CMD ["uvicorn", "app.api.main:app", "--host", "0.0.0.0", "--port", "8000"]`.

Do not change the base image, `WORKDIR`, `COPY` lines, `pip install` step, or `EXPOSE 8000`. Do not modify `frontend/Dockerfile` or the root `Dockerfile`.
  </action>
  <acceptance_criteria>
    - `backend/Dockerfile` contains `app.api.main:app`
    - `backend/Dockerfile` no longer contains `app.main:app`
    - `backend/Dockerfile` still has `EXPOSE 8000`
    - `backend/Dockerfile` still has `WORKDIR /app`
    - Root `Dockerfile` and `frontend/Dockerfile` are not modified
  </acceptance_criteria>
  <verify>`rtk rg "app\.api\.main:app" backend/Dockerfile && rtk rg "app\.main:app" backend/Dockerfile`</verify>
  <done>The compose/dev backend image starts the canonical app.</done>
</task>

<task id="22-01-03" type="execute">
  <name>Verify Alembic env loads canonical metadata</name>
  <files></files>
  <read_first>backend/alembic/env.py, backend/alembic.ini, backend/app/db/database.py, backend/app/models/__init__.py</read_first>
  <action>
This task is verification-only — do not modify any file unless an import bug is discovered.

From the `backend/` directory, run a focused import check that proves Alembic still resolves the canonical metadata under the refactored package. Use one of the following, in priority order, and record the chosen command + output snippet inline as a comment in `22-VERIFICATION.md` (Plan 22-02 will canonicalize this file; for now, append a temporary section if the file does not yet exist).

Priority commands:

1. `cd backend && python -c "from app.core.config import settings; from app.db.database import Base; import app.models; print('alembic_metadata_tables', len(Base.metadata.tables))"` — must print a non-zero table count.
2. `cd backend && python -c "from alembic.config import Config; from alembic import command; command.current(Config('alembic.ini'))"` — must run without ImportError.
3. If neither Python command runs (no Python on PATH), document the exact blocker in `22-VERIFICATION.md` under a `### Plan 22-01 fallback` heading and run `rtk rg "from app\.core\.config|from app\.db\.database|import app\.models" backend/alembic/env.py` as the static fallback. The grep must show all three canonical imports.

If any import fails, fix the offending import inside `backend/alembic/env.py` only — do not change migration files under `backend/alembic/versions/`.
  </action>
  <acceptance_criteria>
    - `backend/alembic/env.py` imports `from app.core.config import settings`
    - `backend/alembic/env.py` imports `from app.db.database import Base`
    - `backend/alembic/env.py` contains `import app.models`
    - At least one of the three priority commands ran successfully and its result was recorded in `22-VERIFICATION.md`
    - No file under `backend/alembic/versions/` was modified
  </acceptance_criteria>
  <verify>`rtk rg "from app\.core\.config import settings|from app\.db\.database import Base|import app\.models" backend/alembic/env.py && rtk rg "Plan 22-01" .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md`</verify>
  <done>Alembic resolves canonical metadata; either a live `alembic current` or a documented fallback proves it.</done>
</task>

<task id="22-01-04" type="execute">
  <name>Audit root Dockerfile, docker-compose, and Azure scripts for stale entrypoint references</name>
  <files></files>
  <read_first>Dockerfile, docker-compose.yml, scripts/setup-azure.sh, scripts/deploy.sh, supervisord.conf</read_first>
  <action>
This task is read-only — do not modify any file unless a stale `app.main:app` reference is found in scope.

Run the following checks:

1. `rtk rg "app\.main:app" Dockerfile docker-compose.yml scripts/` — must return no matches. The root `Dockerfile` runs supervisord (already updated by Task 22-01-01) and does not embed an uvicorn target. `docker-compose.yml` builds `backend/Dockerfile` (already updated by Task 22-01-02). The scripts deploy the monolith image and do not embed an uvicorn target.
2. `rtk rg "uvicorn" Dockerfile docker-compose.yml scripts/setup-azure.sh scripts/deploy.sh` — record any matches. There should be no embedded uvicorn invocations outside the two files updated above.
3. Confirm `docker-compose.yml` still references `./backend` build context and exposes port 8000.
4. Confirm `scripts/setup-azure.sh` still sets `WEBSITES_PORT="80"` and references `mcr.microsoft.com/appsvc/staticsite:latest` as the placeholder image — these are unchanged by this refactor.

If a stale `app.main:app` reference is found, fix it inline using the same canonical replacement as Tasks 22-01-01 and 22-01-02.

Append a short audit summary to `22-VERIFICATION.md` under `### Plan 22-01 entrypoint audit` listing each file checked and the result (`clean` or `fixed`).
  </action>
  <acceptance_criteria>
    - `rtk rg "app\.main:app" Dockerfile docker-compose.yml scripts/` returns no matches
    - `docker-compose.yml` still has `context: ./backend`
    - `docker-compose.yml` still has `8000:8000` port mapping
    - `scripts/setup-azure.sh` still contains `WEBSITES_PORT="80"`
    - `22-VERIFICATION.md` contains the heading `### Plan 22-01 entrypoint audit`
    - The audit lists `Dockerfile`, `docker-compose.yml`, `scripts/setup-azure.sh`, `scripts/deploy.sh`
  </acceptance_criteria>
  <verify>`rtk rg "Plan 22-01 entrypoint audit" .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md && rtk rg "app\.main:app" Dockerfile docker-compose.yml scripts/`</verify>
  <done>No stale `app.main:app` references remain in runtime config; the audit is recorded.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Availability | Uvicorn entrypoint | mitigate | Update supervisord and backend/Dockerfile together; verify canonical import resolves before committing |
| T-22-02 | Integrity | Alembic migration history | mitigate | Verification-only on `env.py`; never touch `alembic/versions/`; require live or static proof of canonical imports |
| T-22-03 | Availability | Compose / Azure deploy chain | mitigate | Audit Dockerfile, compose, and deploy scripts for stale `app.main:app`; record results in `22-VERIFICATION.md` |
</threat_model>

<verification>
- `rtk rg "app\.api\.main:app" supervisord.conf backend/Dockerfile`
- `rtk rg "app\.main:app" supervisord.conf backend/Dockerfile Dockerfile docker-compose.yml scripts/` returns no matches
- `cd backend && python -c "from app.api.main import app; print(any(getattr(r, 'path', None) == '/health' for r in app.routes))"` prints `True` (or fallback documented)
- `rtk rg "Plan 22-01" .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md` finds at least the alembic-check section and the entrypoint audit section
</verification>

<success_criteria>
- supervisord and backend/Dockerfile launch the canonical FastAPI app.
- Alembic env still loads canonical settings, Base, and models.
- Root Dockerfile, docker-compose, and Azure scripts contain no stale `app.main:app` references.
- No Phase 20 shim is deleted in this plan.
</success_criteria>
