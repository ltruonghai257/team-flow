---
phase: "22-runtime-integration-regression-verification"
plan: 4
type: execute
wave: 3
depends_on: [1, 2, 3]
files_modified:
  - "docs/MIGRATION-V2.1.md"
  - "README.md"
  - "backend/CLAUDE.md"
  - ".planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md"
autonomous: true
user_setup:
  - "If `rtk rg` is unavailable, fall back to `rg` or `grep -R`. The shim baseline counts must come from a real grep run, not a guess."
requirements: ["VERIFY-04", "RUN-01", "RUN-02", "RUN-03", "VERIFY-01", "VERIFY-02", "VERIFY-03"]
must_haves:
  truths:
    - "D-12 - Migration / dev-notes deliverable has three sections: old→new import path table (backend + frontend), shim ledger (location, delegate target, owner, removal trigger), runtime runbook."
    - "D-13 - Migration guide lives at `docs/MIGRATION-V2.1.md`; `README.md` and `backend/CLAUDE.md` each receive a one-line pointer."
    - "D-14 - Each shim ledger row records a concrete removal trigger plus the exact grep command and a baseline callsite count outside `.planning/phases`."
    - "D-04/D-05 - No shim is removed in this plan; cleanup is deferred."
    - "D-08 - Final deployment commands and any persistent env-var changes are documented."
  artifacts:
    - path: "docs/MIGRATION-V2.1.md"
      provides: "Persistent v2.1 migration guide for future contributors"
      contains: "## Shim Ledger"
    - path: ".planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md"
      provides: "Final phase signoff with pointers to evidence"
      contains: "## Final Signoff"
  key_links:
    - ".planning/phases/22-runtime-integration-regression-verification/22-CONTEXT.md#migration-notes--shim-cleanup"
    - "backend/docs/MIGRATION-GUIDE-20.md"
    - ".planning/phases/22-runtime-integration-regression-verification/22-RESEARCH.md#shim-inventory-from-phase-20-summary--21-04-summary"
---

# Phase 22 Plan 04: Migration Guide, Shim Ledger, and Phase Signoff

<objective>
Produce the persistent v2.1 migration guide at `docs/MIGRATION-V2.1.md` (path map, shim ledger with grep baselines, runtime runbook), wire pointers from `README.md` and `backend/CLAUDE.md`, and append the final signoff block to `22-VERIFICATION.md` once all four verification floor layers are green.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-runtime-integration-regression-verification/22-CONTEXT.md
@.planning/phases/22-runtime-integration-regression-verification/22-RESEARCH.md
@.planning/phases/22-runtime-integration-regression-verification/22-VALIDATION.md
@.planning/phases/22-runtime-integration-regression-verification/22-01-PLAN.md
@.planning/phases/22-runtime-integration-regression-verification/22-02-PLAN.md
@.planning/phases/22-runtime-integration-regression-verification/22-03-PLAN.md
@.planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md
@.planning/phases/20-backend-package-restructure/20-SUMMARY.md
@.planning/phases/21-frontend-sveltekit-structure/21-04-SUMMARY.md
@backend/docs/MIGRATION-GUIDE-20.md
</context>

<tasks>

<task id="22-04-01" type="execute">
  <name>Author docs/MIGRATION-V2.1.md skeleton with required sections</name>
  <files>docs/MIGRATION-V2.1.md</files>
  <read_first>.planning/phases/22-runtime-integration-regression-verification/22-RESEARCH.md, .planning/phases/20-backend-package-restructure/20-SUMMARY.md, .planning/phases/21-frontend-sveltekit-structure/21-04-SUMMARY.md, backend/docs/MIGRATION-GUIDE-20.md</read_first>
  <action>
Create `docs/MIGRATION-V2.1.md` with the three mandatory sections from CONTEXT D-12, in this order:

1. `# v2.1 Migration Guide: Open WebUI-Inspired Project Structure`
2. Front matter / status paragraph: completed milestone, date, link to roadmap and `backend/docs/MIGRATION-GUIDE-20.md`.
3. `## Old to New Import Paths`
   - Two sub-tables: **Backend (Python)** and **Frontend (TypeScript)**.
   - Backend rows must include: `app.config` → `app.core.config`, `app.database` → `app.db.database`, `app.limiter` → `app.core.limiter`, `app.auth` → `app.utils.auth`, `app.ai_client` → `app.utils.ai_client`, `app.email_service` → `app.utils.email_service`, `app.scheduler_jobs` → `app.internal.scheduler_jobs`, `app.websocket.manager` → `app.socket.manager`, `app.main` → `app.api.main`, `app.models` (module) → `app.models` (package, aggregate `__init__`), `app.schemas` (module) → `app.schemas` (package, aggregate `__init__`).
   - Frontend rows must include: `$lib/api` (values) → `$lib/apis`, `$lib/api` (status types) → `$lib/types/status`, `$lib/api` (reminder types) → `$lib/types/notification`, `$lib/api` (barrel types) → `$lib/types`.
4. `## Shim Ledger` — placeholder note that Task 22-04-02 fills in baselines.
5. `## Runtime Runbook` — to be filled by Task 22-04-03.

Reference (do not copy) `backend/docs/MIGRATION-GUIDE-20.md` for backend-only context; this guide is the **integrated** v2.1 picture.

Do not include speculative future paths (e.g., moving `frontend/src` to repo root) — that is deferred per REQUIREMENTS.
  </action>
  <acceptance_criteria>
    - `docs/MIGRATION-V2.1.md` exists
    - It contains `# v2.1 Migration Guide`
    - It contains `## Old to New Import Paths`
    - It contains a Backend (Python) table and a Frontend (TypeScript) table
    - Backend table contains `app.api.main`, `app.core.config`, `app.db.database`, `app.utils.auth`, `app.socket.manager`, and the `app.models` package note
    - Frontend table contains `$lib/apis`, `$lib/types`, `$lib/types/status`, `$lib/types/notification`
    - It contains `## Shim Ledger` and `## Runtime Runbook` placeholders
    - It links to `backend/docs/MIGRATION-GUIDE-20.md`
  </acceptance_criteria>
  <verify>`rtk rg "# v2.1 Migration Guide|## Old to New Import Paths|## Shim Ledger|## Runtime Runbook|app\.api\.main|app\.core\.config|\\$lib/apis|\\$lib/types/status|MIGRATION-GUIDE-20" docs/MIGRATION-V2.1.md`</verify>
  <done>Migration guide skeleton exists with all three required section headings and the path tables.</done>
</task>

<task id="22-04-02" type="execute">
  <name>Fill shim ledger with baseline grep counts</name>
  <files>docs/MIGRATION-V2.1.md</files>
  <read_first>docs/MIGRATION-V2.1.md, .planning/phases/22-runtime-integration-regression-verification/22-RESEARCH.md, backend/app/main.py, backend/app/config.py, backend/app/database.py, backend/app/limiter.py, backend/app/auth.py, backend/app/ai_client.py, backend/app/email_service.py, backend/app/scheduler_jobs.py, backend/app/websocket/manager.py</read_first>
  <action>
For each shim listed in `22-RESEARCH.md` § "Shim Inventory", run the grep that counts callsites **outside `.planning/phases/`** (the planning history references shims by name and would otherwise inflate the count). Use `rtk rg` with the `-c` flag to count matches per file, or the `--count-matches` flag, then sum. Always exclude `.planning/phases/`.

Example for `app.main` (HIGH-RISK shim — count callsites importing it):

```
rtk rg "from app\.main import|import app\.main" --glob '!.planning/phases/**' --glob '!backend/app/main.py'
```

Run analogous greps for every shim row in the table below. Record results inline in `docs/MIGRATION-V2.1.md` under `## Shim Ledger` using this exact column set:

| Shim | Location | Delegate Target | Risk | Removal Trigger | Baseline Grep | Baseline callsite count |

Required rows (one per row of the table from Task 22-04-01):

- `app.main` → `app.api.main`, HIGH (uvicorn target) — removal trigger: "all runtime entrypoints reference `app.api.main:app` AND no Python import resolves `from app.main`"; this trigger is verified by Plan 22-01.
- `app.config` → `app.core.config`, LOW
- `app.database` → `app.db.database`, LOW
- `app.limiter` → `app.core.limiter`, LOW
- `app.auth` → `app.utils.auth`, LOW
- `app.ai_client` → `app.utils.ai_client`, LOW
- `app.email_service` → `app.utils.email_service`, LOW
- `app.scheduler_jobs` → `app.internal.scheduler_jobs`, LOW
- `app.websocket.manager` → `app.socket.manager`, LOW
- `app.models` (module shim shadowed by package) — note: `models/__init__.py` re-exports; baseline grep counts `from app.models import` callsites that rely on aggregate exports.
- `app.schemas` (module shim shadowed by package) — note: `schemas/__init__.py` re-exports; same shape.
- `frontend/src/lib/api.ts` — already deleted in Phase 21-04; row records "REMOVED in Phase 21-04, baseline count = 0" without re-running the grep.

For each row, record the **exact grep command** that was run and the **integer count** observed at the moment of authoring. CONTEXT D-14: phase 22 records the baseline; the future cleanup milestone uses these greps as the trigger ("count is still zero" or "count dropped to zero").

Do not modify any shim file. Do not delete any shim.
  </action>
  <acceptance_criteria>
    - `docs/MIGRATION-V2.1.md` `## Shim Ledger` section contains a table with the columns above
    - The table has at least 11 rows (one per backend shim) plus the frontend `api.ts` removed-row
    - Every row contains an integer in the "Baseline callsite count" column
    - Every row contains a literal grep command string (or "n/a" for the deleted `api.ts` row)
    - The HIGH-risk row for `app.main` references the Plan 22-01 trigger
    - No shim file is modified by this task
  </acceptance_criteria>
  <verify>`rtk rg "## Shim Ledger|Baseline callsite count|app\.main|app\.core\.config|app\.utils\.auth|app\.socket\.manager|app\.models|frontend/src/lib/api\.ts" docs/MIGRATION-V2.1.md`</verify>
  <done>Shim ledger has concrete grep commands and integer baselines for every live shim.</done>
</task>

<task id="22-04-03" type="execute">
  <name>Write runtime runbook section with verified commands</name>
  <files>docs/MIGRATION-V2.1.md</files>
  <read_first>docs/MIGRATION-V2.1.md, supervisord.conf, backend/Dockerfile, Dockerfile, docker-compose.yml, scripts/setup-azure.sh, scripts/deploy.sh, .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md</read_first>
  <action>
Fill the `## Runtime Runbook` section of `docs/MIGRATION-V2.1.md` with **verified** commands. Each command must either have been executed during Plans 22-01/22-02/22-03 (reference the relevant section in `22-VERIFICATION.md`) or be a documented standard project command. Do not invent flags.

Required subsections in this order:

### Backend (local dev)

```
cd backend
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.api.main:app --host 127.0.0.1 --port 8000 --reload
```

State that `app.main:app` still works during the v2.1 milestone via the Phase 20 delegate but is deprecated.

### Frontend (local dev)

```
cd frontend
bun install
bun run dev      # → http://localhost:5173
bun run check    # type-check
bun run build    # production static build under frontend/build/
bunx playwright test   # E2E suite (mobile-chrome project)
```

### Docker compose (local stack)

```
docker compose up --build
```

State that `docker-compose.yml` builds `backend/Dockerfile` (now starts `app.api.main:app`) and exposes backend on `:8000` and frontend on `:3000`. Postgres on `:5432`.

### Monolith image (Azure App Service)

```
# Provision (one time)
export DB_ADMIN_PASS="<secure>"
./scripts/setup-azure.sh

# Build + deploy
./scripts/deploy.sh
```

State that the monolith image runs `supervisord` which starts nginx + `uvicorn app.api.main:app` (port 8000 internal, 80 external via nginx). App Service `WEBSITES_PORT=80`.

### Persistent environment / config notes

State the following based on Phase 20-05 outcome:

- `app.core.config.settings` is the canonical settings object; `app.config` import remains a delegate.
- No new environment variables introduced by v2.1 milestone.
- Alembic env loads canonical metadata; existing migration history unchanged.
- `RUN_MIGRATIONS=true` still triggers Alembic upgrade in the lifespan.

### Evidence

Add a short line: "Verification floor evidence: `.planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md`."
  </action>
  <acceptance_criteria>
    - `## Runtime Runbook` contains four subsections: Backend (local dev), Frontend (local dev), Docker compose (local stack), Monolith image (Azure App Service)
    - Backend section uses `uvicorn app.api.main:app`
    - Frontend section uses `bun run dev`, `bun run check`, `bun run build`, `bunx playwright test`
    - Docker compose section references `docker compose up --build`
    - Monolith section references `scripts/setup-azure.sh` and `scripts/deploy.sh`
    - Persistent config section notes no new env vars and `app.core.config.settings` as canonical
    - Evidence line points to `22-VERIFICATION.md`
  </acceptance_criteria>
  <verify>`rtk rg "uvicorn app\.api\.main:app|bun run check|bun run build|bunx playwright test|docker compose up|setup-azure\.sh|deploy\.sh|22-VERIFICATION\.md" docs/MIGRATION-V2.1.md`</verify>
  <done>Runbook documents the exact commands used during Phase 22 verification.</done>
</task>

<task id="22-04-04" type="execute">
  <name>Add migration-guide pointers to README and backend/CLAUDE.md</name>
  <files>README.md, backend/CLAUDE.md</files>
  <read_first>README.md, backend/CLAUDE.md, docs/MIGRATION-V2.1.md</read_first>
  <action>
Add a one-line pointer to `docs/MIGRATION-V2.1.md` in each file. Keep the existing structure of each file intact.

In `README.md`: add a single line under the most appropriate existing top-level heading (likely a "Documentation" or "Project Structure" section). Example wording:

> v2.1 structural refactor migration: see [`docs/MIGRATION-V2.1.md`](docs/MIGRATION-V2.1.md).

If no obvious section exists, add a new `## Documentation` heading at the bottom (above any license block) containing the line.

In `backend/CLAUDE.md`: add a single line under an existing "Recent changes", "Architecture", or "Migration" heading. Example wording:

> Backend uses the Open WebUI-inspired package layout introduced in v2.1; see `docs/MIGRATION-V2.1.md` for the integrated guide and `backend/docs/MIGRATION-GUIDE-20.md` for the backend-only path map.

Do not duplicate existing content. Do not rewrite either file.
  </action>
  <acceptance_criteria>
    - `README.md` contains the literal string `MIGRATION-V2.1`
    - `backend/CLAUDE.md` contains the literal string `MIGRATION-V2.1`
    - `backend/CLAUDE.md` references `MIGRATION-GUIDE-20`
    - Neither file's existing structure is rewritten
  </acceptance_criteria>
  <verify>`rtk rg "MIGRATION-V2\.1" README.md backend/CLAUDE.md && rtk rg "MIGRATION-GUIDE-20" backend/CLAUDE.md`</verify>
  <done>Both files point to the new migration guide.</done>
</task>

<task id="22-04-05" type="execute">
  <name>Append final signoff block to 22-VERIFICATION.md</name>
  <files>.planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md</files>
  <read_first>.planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md, .planning/phases/22-runtime-integration-regression-verification/22-VALIDATION.md, docs/MIGRATION-V2.1.md</read_first>
  <action>
Append a `## Final Signoff` section at the end of `22-VERIFICATION.md`. This task may only be performed when all four verification layers (Plan 22-02 backend pytest, Plan 22-02 frontend check + build, Plan 22-03 Playwright, Plan 22-03 manual smoke) are recorded as `pass` or `pass-with-fallbacks` per CONTEXT D-11.

Section template:

```
## Final Signoff

**Phase 22 verified:** <YYYY-MM-DD>

| Layer | Plan | Status | Evidence |
|-------|------|--------|----------|
| Runtime entrypoint update (RUN-01) | 22-01 | <pass/fail> | `## Plan 22-01 entrypoint audit` |
| Alembic canonical metadata (BACK-04) | 22-01 | <pass/fail> | `### Plan 22-01 fallback` (or live alembic current) |
| Backend pytest (VERIFY-01) | 22-02 | <pass/fail> | `## Backend pytest` |
| Frontend check + build (VERIFY-02, RUN-02) | 22-02 | <pass/fail> | `## Frontend check`, `## Frontend build` |
| Playwright E2E | 22-03 | <pass/fail> | `## Playwright E2E` (HTML report path) |
| Manual smoke (VERIFY-03) | 22-03 | <pass/fail> | `## Manual Smoke (Plan 22-03)` |
| Migration guide (VERIFY-04) | 22-04 | <pass/fail> | `docs/MIGRATION-V2.1.md` |

**Requirements covered by Phase 22:** RUN-01, RUN-02, RUN-03, VERIFY-01, VERIFY-02, VERIFY-03, VERIFY-04.

**No new dependencies introduced (RUN-03):** confirmed by `rtk rg "^[a-zA-Z]" backend/requirements.txt` head and `frontend/package.json` diff against pre-Phase-22 state.

**Shims left in place (CONTEXT D-04/D-05):** see `docs/MIGRATION-V2.1.md` `## Shim Ledger` for baseline counts; cleanup deferred to a future milestone.

**Signoff:** <name or "Phase 22 executor">.
```

If any row is `fail`, do **not** mark the section "Phase 22 verified". Instead, record the failures and stop the plan. Re-run the relevant earlier task to remediate before re-attempting this signoff.
  </action>
  <acceptance_criteria>
    - `22-VERIFICATION.md` contains the heading `## Final Signoff`
    - The signoff table has exactly the seven rows above
    - The "Phase 22 verified" line carries an ISO date if and only if every row is `pass` or `pass-with-fallbacks`
    - The section references `docs/MIGRATION-V2.1.md`
    - The section references RUN-01..RUN-03 and VERIFY-01..VERIFY-04
    - No row in the table is left as a placeholder (e.g., `<pass/fail>`)
  </acceptance_criteria>
  <verify>`rtk rg "## Final Signoff|Phase 22 verified|RUN-01.*RUN-02.*RUN-03|VERIFY-01.*VERIFY-04|MIGRATION-V2\.1" .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md`</verify>
  <done>Phase 22 has a final signoff block tying every requirement to recorded evidence.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Integrity | Migration documentation | mitigate | Write only commands that ran (or are documented project standards); link evidence in `22-VERIFICATION.md` |
| T-22-10 | Availability | Premature signoff | mitigate | Final signoff task explicitly forbids marking the phase verified if any layer is `fail` |
| T-22-11 | Integrity | Shim removal drift | mitigate | This plan never deletes a shim; it only records baseline counts so a future cleanup milestone has an objective trigger |
</threat_model>

<verification>
- `test -f docs/MIGRATION-V2.1.md`
- `rtk rg "## Old to New Import Paths|## Shim Ledger|## Runtime Runbook" docs/MIGRATION-V2.1.md`
- `rtk rg "Baseline callsite count" docs/MIGRATION-V2.1.md`
- `rtk rg "MIGRATION-V2\.1" README.md backend/CLAUDE.md`
- `rtk rg "## Final Signoff|Phase 22 verified" .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md`
- `rtk rg -i "access_token=ey|password=|sk-[A-Za-z0-9]" docs/MIGRATION-V2.1.md .planning/phases/22-runtime-integration-regression-verification/22-VERIFICATION.md` returns no matches
</verification>

<success_criteria>
- `docs/MIGRATION-V2.1.md` contains the three required sections with concrete content.
- The shim ledger records grep commands and integer baseline counts for every live shim.
- `README.md` and `backend/CLAUDE.md` point to the new migration guide.
- `22-VERIFICATION.md` has a `## Final Signoff` block tying every Phase 22 requirement to recorded evidence.
- No shims are deleted; cleanup is deferred per CONTEXT D-04/D-05/D-14.
</success_criteria>
