---
phase: "24-knowledge-sharing-scheduler"
plan: 3
type: execute
wave: 3
depends_on: ["24-01", "24-02"]
files_modified:
  - "frontend/src/lib/apis/knowledge-sessions.ts"
  - "frontend/src/lib/apis/index.ts"
  - "frontend/src/lib/apis/notifications.ts"
  - "frontend/src/lib/components/knowledge/SessionForm.svelte"
  - "frontend/src/lib/components/knowledge/SessionCard.svelte"
  - "frontend/src/routes/schedule/+page.svelte"
autonomous: true
requirements: ["KS-01", "KS-02", "KS-03", "KS-04", "KS-05"]
must_haves:
  truths:
    - "D-01: Knowledge Sessions live inside `/schedule` as a dedicated tab and never as a new top-level route."
    - "D-02: The Knowledge Sessions tab opens in agenda/list view by default."
    - "D-03: The tab also offers a calendar view toggle without leaving `/schedule`."
    - "D-07: Reminder offsets reuse the existing chip-style schedule reminder pattern."
    - "D-09: References are captured in a freeform textarea, not a structured URL grid."
    - "D-10: Tags are edited through chip-style entry, not a raw comma-separated text field."
    - "D-12: Members only see sessions in scope, while supervisors/admins get role-appropriate create/edit actions."
  artifacts:
    - path: "frontend/src/routes/schedule/+page.svelte"
      provides: "Tabbed scheduler UI with Knowledge Sessions agenda/calendar views"
      contains: "Knowledge Sessions"
    - path: "frontend/src/lib/components/knowledge/SessionForm.svelte"
      provides: "Dedicated knowledge-session modal with presenter, tags, reminders, and references"
      contains: "Create Session"
    - path: "frontend/src/lib/components/knowledge/SessionCard.svelte"
      provides: "Compact agenda tile and upcoming rail presentation"
      contains: "Org-wide"
  key_links:
    - from: "frontend/src/routes/schedule/+page.svelte"
      to: "frontend/src/lib/apis/knowledge-sessions.ts"
      via: "load and mutate knowledge sessions through the new API module"
      pattern: "knowledgeSessions"
    - from: "frontend/src/lib/components/knowledge/SessionForm.svelte"
      to: "frontend/src/lib/apis/notifications.ts"
      via: "hydrate reminder selections from knowledge_session by-event lookups when editing"
      pattern: "knowledge_session"
---

<objective>
Extend the existing `/schedule` page with the approved Knowledge Sessions UX: tabbed browsing, agenda-first default, calendar toggle, dedicated session modal, scoped actions, and frontend verification.

Purpose: This plan delivers the Phase 24 user-facing experience without breaking the existing personal schedule flow.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-knowledge-sharing-scheduler/24-CONTEXT.md
@.planning/phases/24-knowledge-sharing-scheduler/24-RESEARCH.md
@.planning/phases/24-knowledge-sharing-scheduler/24-UI-SPEC.md
@.planning/phases/24-knowledge-sharing-scheduler/24-VALIDATION.md
@.planning/phases/24-knowledge-sharing-scheduler/24-01-PLAN.md
@.planning/phases/24-knowledge-sharing-scheduler/24-02-PLAN.md
</context>

<tasks>

<task id="24-03-01" type="execute">
  <name>Add knowledge session frontend API module and notification event support</name>
  <read_first>
    frontend/src/lib/apis/schedules.ts
    frontend/src/lib/apis/index.ts
    frontend/src/lib/apis/notifications.ts
    frontend/src/lib/apis/request.ts
  </read_first>
  <action>
Create `frontend/src/lib/apis/knowledge-sessions.ts` with methods:

- `list(start?: string, end?: string)`
- `get(id: number)`
- `create(data: object)`
- `update(id: number, data: object)`
- `delete(id: number)`

Match the existing API module style from `schedules.ts` and `tasks.ts`.

Update `frontend/src/lib/apis/index.ts` to export `knowledgeSessions`.

Update `frontend/src/lib/apis/notifications.ts` so all event-type unions that currently allow `'schedule' | 'task'` also allow `'knowledge_session'`. This includes:
- `create`
- `bulkSet` if still used elsewhere
- `byEvent`

Do not change `request.ts`; it already injects `X-SubTeam-ID` for admin sub-team context.
  </action>
  <acceptance_criteria>
    - `frontend/src/lib/apis/knowledge-sessions.ts` exists and contains `export const knowledgeSessions`
    - `frontend/src/lib/apis/index.ts` contains `export { knowledgeSessions } from './knowledge-sessions'`
    - `frontend/src/lib/apis/notifications.ts` contains `'knowledge_session'`
  </acceptance_criteria>
</task>

<task id="24-03-02" type="execute">
  <name>Create dedicated SessionForm and SessionCard components</name>
  <read_first>
    frontend/src/routes/schedule/+page.svelte
    frontend/src/app.css
    .planning/phases/24-knowledge-sharing-scheduler/24-UI-SPEC.md
  </read_first>
  <action>
Create `frontend/src/lib/components/knowledge/SessionForm.svelte` and `frontend/src/lib/components/knowledge/SessionCard.svelte`.

`SessionForm.svelte` must implement the UI-SPEC contract exactly:
- dedicated modal shell
- fields in this order:
  1. Topic
  2. Description
  3. References
  4. Presenter
  5. Session Type
  6. Duration
  7. Start
  8. Tags
  9. Reminders
- freeform textarea for references
- chip-style tag entry with enter/comma commit and inline remove buttons
- reminder chips reusing the visual pattern from the schedule page
- supervisor/admin-only usage; members never open this form

`SessionCard.svelte` must render:
- topic
- start time
- duration
- presenter
- scope badge (`Org-wide` or `Team Session`)
- session type badge
- tag chips
- compact description preview

Keep both components inside the existing gray card language from `app.css`; do not create a new visual theme.
  </action>
  <acceptance_criteria>
    - `frontend/src/lib/components/knowledge/SessionForm.svelte` exists
    - `SessionForm.svelte` contains `Topic`, `References`, `Presenter`, `Session Type`, `Duration`, `Reminders`
    - `SessionForm.svelte` contains tag chip add/remove behavior
    - `frontend/src/lib/components/knowledge/SessionCard.svelte` exists
    - `SessionCard.svelte` contains `Org-wide` and `Team Session`
  </acceptance_criteria>
</task>

<task id="24-03-03" type="execute">
  <name>Refactor `/schedule` into tabbed schedule and knowledge-session flows with isolated state</name>
  <read_first>
    frontend/src/routes/schedule/+page.svelte
    frontend/src/lib/stores/auth.ts
    frontend/src/lib/apis/knowledge-sessions.ts
    frontend/src/lib/components/knowledge/SessionForm.svelte
    frontend/src/lib/components/knowledge/SessionCard.svelte
    .planning/phases/24-knowledge-sharing-scheduler/24-UI-SPEC.md
  </read_first>
  <action>
Update `frontend/src/routes/schedule/+page.svelte` to preserve the current personal schedule experience while adding a separate Knowledge Sessions tab.

Add exact local state families:
- `activeTab`
- `knowledgeViewMode`
- `knowledgeSessions`
- `showKnowledgeModal`
- `editingKnowledgeSession`
- `knowledgeSelectedDay`
- `knowledgeLoading`

Keep the existing personal schedule state untouched:
- `showModal`
- `editingSchedule`
- `form`
- `formReminders`

UI changes:
- add segmented top-level tabs: `My Schedule` and `Knowledge Sessions`
- default `activeTab = 'schedule'`
- when `activeTab === 'knowledge'`, default `knowledgeViewMode = 'agenda'`
- show `Create Session` button only for supervisors/admins while in the knowledge tab
- members see browse-only knowledge content
- load knowledge sessions from the new API module
- use agenda list as the default knowledge tab surface
- add a secondary `Agenda` / `Calendar` toggle inside the knowledge tab
- in knowledge calendar mode, clicking a day filters the side rail to that day rather than opening the personal schedule form
- in knowledge agenda mode, the right rail shows `Upcoming Sessions`

State-isolation rules:
- switching tabs closes both modal families
- editing a personal schedule event must never populate `SessionForm`
- editing a knowledge session must never mutate the personal schedule `form`

Do not add a new route and do not modify sidebar navigation.
  </action>
  <acceptance_criteria>
    - `frontend/src/routes/schedule/+page.svelte` contains `My Schedule`
    - `frontend/src/routes/schedule/+page.svelte` contains `Knowledge Sessions`
    - the file contains separate `showKnowledgeModal` state
    - the file still contains the existing personal `showModal` state
    - `Create Session` renders only in the knowledge tab path
    - no new file under `frontend/src/routes/knowledge*` or `frontend/src/routes/sessions*` is created
  </acceptance_criteria>
</task>

<task id="24-03-04" type="execute">
  <name>Wire edit/delete/reminder hydration and scoped empty states</name>
  <read_first>
    frontend/src/routes/schedule/+page.svelte
    frontend/src/lib/apis/notifications.ts
    frontend/src/lib/components/knowledge/SessionForm.svelte
    .planning/phases/24-knowledge-sharing-scheduler/24-UI-SPEC.md
  </read_first>
  <action>
Finish the frontend behavior details:

- when editing a knowledge session, hydrate reminder selections through `notifApi.byEvent('knowledge_session', session.id)`
- show knowledge-tab empty states using the exact approved copy:
  - `No knowledge sessions yet`
  - `Schedule a session to share upcoming demos, workshops, or Q&A time.`
  - `Sessions for your scope will appear here when they're scheduled.`
- show day-filter empty state:
  - `Nothing scheduled for this day`
  - `Choose another date or create a new session here.`
- use explicit delete confirmation UI or modal copy instead of browser `confirm()`
- keep personal schedule delete behavior unchanged unless a minimal local refactor is required by the shared page structure

For successful mutations, use these exact toast messages:
- `Session created`
- `Session updated`
- `Session deleted`

For failures:
- `Failed to load knowledge sessions.`
- `Failed to save session. Try again.`
- `Failed to delete session. Try again.`
- `Reminders were not updated.`
  </action>
  <acceptance_criteria>
    - `frontend/src/routes/schedule/+page.svelte` contains `knowledge_session`
    - the file contains `No knowledge sessions yet`
    - the file contains `Nothing scheduled for this day`
    - the file contains `Session created`
    - the knowledge-session delete flow does not use browser `confirm()`
  </acceptance_criteria>
</task>

<task id="24-03-05" type="execute">
  <name>Run frontend verification and document manual knowledge-tab checks</name>
  <read_first>
    frontend/package.json
    frontend/src/routes/schedule/+page.svelte
    .planning/phases/24-knowledge-sharing-scheduler/24-VALIDATION.md
  </read_first>
  <action>
Run these exact commands:

```bash
cd frontend && rtk bun run check
cd frontend && rtk bun run build
```

Then perform or document these manual checks:
- `/schedule` still loads with the personal schedule tab intact
- Knowledge Sessions tab exists inside `/schedule`
- agenda mode is the default view for the knowledge tab
- calendar toggle switches knowledge-session browsing without opening the personal event form
- a member account cannot see `Create Session`, edit, or delete controls
- a supervisor/admin account can open the knowledge session modal, manage tags as chips, and edit reminder offsets

If the browser/manual checks cannot be run in this environment, record that exact limitation in the summary.
  </action>
  <acceptance_criteria>
    - `cd frontend && rtk bun run check` exits 0
    - `cd frontend && rtk bun run build` exits 0
    - manual verification notes cover personal schedule regression, knowledge-tab default agenda view, calendar toggle behavior, member permissions, and supervisor/admin create flow
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-08 | Information Disclosure | Knowledge tab UI | mitigate | Members only receive browse surfaces for sessions already filtered by the backend; no admin/supervisor actions render for them |
| T-24-09 | UX Integrity | Shared `/schedule` page state | mitigate | Personal schedule and knowledge-session modal state are split into separate variables and reset on tab switch |
| T-24-10 | Tampering | Reminder hydration/edit UI | mitigate | Reminder chips hydrate from `knowledge_session` notification rows and write back through scoped API endpoints only |
</threat_model>

<verification>
- `cd frontend && rtk bun run check`
- `cd frontend && rtk bun run build`
- Manual checks listed in task `24-03-05`
</verification>

<success_criteria>
- `/schedule` contains a dedicated Knowledge Sessions tab with agenda-first default and a calendar toggle.
- The frontend uses isolated state for personal schedules and knowledge sessions, preventing modal/form leakage.
- Supervisor/admin users can create and manage scoped sessions, while members browse only sessions visible to them.
</success_criteria>

<output>
After completion, create `.planning/phases/24-knowledge-sharing-scheduler/24-03-SUMMARY.md`
</output>
