---
phase: "21-frontend-sveltekit-structure"
plan: 4
type: execute
wave: 4
depends_on: ["21-03"]
files_modified:
  - "frontend/src/routes/+layout.svelte"
  - "frontend/src/routes/+page.svelte"
  - "frontend/src/routes/ai/+page.svelte"
  - "frontend/src/routes/invite/accept/+page.svelte"
  - "frontend/src/routes/milestones/+page.svelte"
  - "frontend/src/routes/performance/+page.svelte"
  - "frontend/src/routes/performance/[id]/+page.svelte"
  - "frontend/src/routes/projects/+page.svelte"
  - "frontend/src/routes/register/+page.svelte"
  - "frontend/src/routes/schedule/+page.svelte"
  - "frontend/src/routes/tasks/+page.svelte"
  - "frontend/src/routes/team/+page.svelte"
  - "frontend/src/routes/timeline/+page.svelte"
  - "frontend/src/lib/stores/auth.ts"
  - "frontend/src/lib/stores/notifications.ts"
  - "frontend/src/lib/components/timeline/TimelineGantt.svelte"
  - "frontend/src/lib/components/sprints/SprintForm.svelte"
  - "frontend/src/lib/components/sprints/SprintCloseModal.svelte"
  - "frontend/src/lib/components/performance/KpiWarnButton.svelte"
  - "frontend/src/lib/components/tasks/AiTaskInput.svelte"
  - "frontend/src/lib/components/tasks/KanbanBoard.svelte"
  - "frontend/src/lib/components/statuses/ProjectStatusPanel.svelte"
  - "frontend/src/lib/components/statuses/StatusDeleteDialog.svelte"
  - "frontend/src/lib/components/statuses/StatusEditorRow.svelte"
  - "frontend/src/lib/components/statuses/StatusReorderList.svelte"
  - "frontend/src/lib/components/statuses/StatusSetManager.svelte"
  - "frontend/src/lib/components/statuses/StatusTransitionEditor.svelte"
  - "frontend/src/lib/components/statuses/StatusTransitionPreview.svelte"
  - "frontend/src/lib/api.ts"
autonomous: true
user_setup:
  - "If `bun` is not available on PATH during execution, document the exact blocker and use the strongest available local fallback for Svelte type checking and build verification."
requirements: ["FRONT-01", "FRONT-02", "FRONT-03", "FRONT-04", "FRONT-05"]
must_haves:
  truths:
    - "D-05 - Remove the temporary `$lib/api` shim by the end of Phase 21 after all callsites have migrated."
    - "D-09 - API callsites should import namespaces from `$lib/apis`."
    - "D-10 - Shared type imports use a mixed rule where common route/component callsites may import from `$lib/types`, while API/domain internals may import direct modules."
    - "D-12 - Skip optional route-local component extraction in Phase 21."
    - "D-13 - Keep Phase 21 surgical with API modules, type modules, import updates, frontend check/build, and focused smoke verification only."
    - "D-14 - Preserve all current route URLs and visual behavior."
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "Deleted compatibility shim"
      contains: "file absent by end of Phase 21"
    - path: "frontend/src/lib/apis/index.ts"
      provides: "Final API import surface"
      contains: "export { tasks }"
    - path: "frontend/src/lib/types/index.ts"
      provides: "Common shared type import surface"
      contains: "export type"
  key_links:
    - ".planning/phases/21-frontend-sveltekit-structure/21-CONTEXT.md#import-conventions"
    - ".planning/phases/21-frontend-sveltekit-structure/21-IMPORT-MAP.md#importer-inventory"
---

# Phase 21 Plan 04: Callsite Migration, Shim Removal, and Final Verification

<objective>
Migrate all frontend callsites from `$lib/api` to `$lib/apis` and `$lib/types`, delete the temporary shim, and run final frontend verification without changing route URLs or visual UI behavior.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-frontend-sveltekit-structure/21-CONTEXT.md
@.planning/phases/21-frontend-sveltekit-structure/21-RESEARCH.md
@.planning/phases/21-frontend-sveltekit-structure/21-VALIDATION.md
@.planning/phases/21-frontend-sveltekit-structure/21-PATTERNS.md
@.planning/phases/21-frontend-sveltekit-structure/21-IMPORT-MAP.md
@.planning/phases/21-frontend-sveltekit-structure/21-03-PLAN.md
</context>

<tasks>

<task id="21-04-01" type="execute">
  <name>Update route API imports to `$lib/apis` and `$lib/types`</name>
  <files>frontend/src/routes/+layout.svelte, frontend/src/routes/+page.svelte, frontend/src/routes/ai/+page.svelte, frontend/src/routes/invite/accept/+page.svelte, frontend/src/routes/milestones/+page.svelte, frontend/src/routes/performance/+page.svelte, frontend/src/routes/performance/[id]/+page.svelte, frontend/src/routes/projects/+page.svelte, frontend/src/routes/register/+page.svelte, frontend/src/routes/schedule/+page.svelte, frontend/src/routes/tasks/+page.svelte, frontend/src/routes/team/+page.svelte, frontend/src/routes/timeline/+page.svelte</files>
  <read_first>.planning/phases/21-frontend-sveltekit-structure/21-IMPORT-MAP.md, frontend/src/lib/apis/index.ts, frontend/src/lib/types/index.ts, frontend/src/routes/+layout.svelte, frontend/src/routes/+page.svelte, frontend/src/routes/ai/+page.svelte, frontend/src/routes/invite/accept/+page.svelte, frontend/src/routes/milestones/+page.svelte, frontend/src/routes/performance/+page.svelte, frontend/src/routes/performance/[id]/+page.svelte, frontend/src/routes/projects/+page.svelte, frontend/src/routes/register/+page.svelte, frontend/src/routes/schedule/+page.svelte, frontend/src/routes/tasks/+page.svelte, frontend/src/routes/team/+page.svelte, frontend/src/routes/timeline/+page.svelte</read_first>
  <action>
Update route imports according to the Phase 21 convention:

- value imports from `$lib/api` become value imports from `$lib/apis`
- common shared type imports from `$lib/api` become type imports from `$lib/types`

Update every route listed in `<files>`.

Examples:

- `import { dashboard } from '$lib/api';` becomes `import { dashboard } from '$lib/apis';`
- `import { milestones as milestonesApi, projects as projectsApi } from '$lib/api';` becomes `import { milestones as milestonesApi, projects as projectsApi } from '$lib/apis';`
- `import type { CustomStatus, StatusSet, StatusTransition } from '$lib/api';` becomes `import type { CustomStatus, StatusSet, StatusTransition } from '$lib/types';`

Do not move route files. Do not extract route-local components. Do not change route URLs or visual markup beyond import statements needed for the refactor.
  </action>
  <acceptance_criteria>
    - No route file imports from `$lib/api`
    - Route value imports use `$lib/apis`
    - Route shared type imports use `$lib/types`
    - All existing route files remain in their current directories
  </acceptance_criteria>
  <verify>`rtk rg "\\$lib/api" frontend/src/routes` should return no matches; `rtk rg "\\$lib/apis|\\$lib/types" frontend/src/routes` should find updated imports</verify>
  <done>All route API/type imports use the new shared surfaces without route movement.</done>
</task>

<task id="21-04-02" type="execute">
  <name>Update store and component API imports</name>
  <files>frontend/src/lib/stores/auth.ts, frontend/src/lib/stores/notifications.ts, frontend/src/lib/components/timeline/TimelineGantt.svelte, frontend/src/lib/components/sprints/SprintForm.svelte, frontend/src/lib/components/sprints/SprintCloseModal.svelte, frontend/src/lib/components/performance/KpiWarnButton.svelte, frontend/src/lib/components/tasks/AiTaskInput.svelte, frontend/src/lib/components/tasks/KanbanBoard.svelte, frontend/src/lib/components/statuses/ProjectStatusPanel.svelte, frontend/src/lib/components/statuses/StatusDeleteDialog.svelte, frontend/src/lib/components/statuses/StatusEditorRow.svelte, frontend/src/lib/components/statuses/StatusReorderList.svelte, frontend/src/lib/components/statuses/StatusSetManager.svelte, frontend/src/lib/components/statuses/StatusTransitionEditor.svelte, frontend/src/lib/components/statuses/StatusTransitionPreview.svelte</files>
  <read_first>.planning/phases/21-frontend-sveltekit-structure/21-IMPORT-MAP.md, frontend/src/lib/apis/index.ts, frontend/src/lib/types/index.ts, frontend/src/lib/stores/auth.ts, frontend/src/lib/stores/notifications.ts, frontend/src/lib/components/timeline/TimelineGantt.svelte, frontend/src/lib/components/sprints/SprintForm.svelte, frontend/src/lib/components/sprints/SprintCloseModal.svelte, frontend/src/lib/components/performance/KpiWarnButton.svelte, frontend/src/lib/components/tasks/AiTaskInput.svelte, frontend/src/lib/components/tasks/KanbanBoard.svelte, frontend/src/lib/components/statuses/ProjectStatusPanel.svelte, frontend/src/lib/components/statuses/StatusDeleteDialog.svelte, frontend/src/lib/components/statuses/StatusEditorRow.svelte, frontend/src/lib/components/statuses/StatusReorderList.svelte, frontend/src/lib/components/statuses/StatusSetManager.svelte, frontend/src/lib/components/statuses/StatusTransitionEditor.svelte, frontend/src/lib/components/statuses/StatusTransitionPreview.svelte</read_first>
  <action>
Update store and component imports according to the Phase 21 convention:

- value imports from `$lib/api` become value imports from `$lib/apis`
- common shared type imports from `$lib/api` become type imports from `$lib/types`
- API/domain internals may use direct type modules such as `$lib/types/status` only when it is clearer and does not spread unnecessary direct imports into route code

Examples:

- `import { auth as authApi } from '$lib/api';` becomes `import { auth as authApi } from '$lib/apis';`
- `import { notifications as notifApi } from '$lib/api';` becomes `import { notifications as notifApi } from '$lib/apis';`
- `import type { CustomStatus } from '$lib/api';` becomes `import type { CustomStatus } from '$lib/types';`

Do not change component markup, styling, route layout, drag/drop behavior, charts, or notification behavior.
  </action>
  <acceptance_criteria>
    - No store or component file imports from `$lib/api`
    - Store/component value imports use `$lib/apis`
    - Shared type imports use `$lib/types` or a justified direct type module
    - No component `.svelte` file is moved or renamed
  </acceptance_criteria>
  <verify>`rtk rg "\\$lib/api" frontend/src/lib/stores frontend/src/lib/components` should return no matches; `rtk rg "\\$lib/apis|\\$lib/types" frontend/src/lib/stores frontend/src/lib/components` should find updated imports</verify>
  <done>Stores and components use the new API/type import surfaces without visual changes.</done>
</task>

<task id="21-04-03" type="execute">
  <name>Delete temporary `$lib/api` shim</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>frontend/src/lib/api.ts, frontend/src/lib/apis/index.ts, frontend/src/lib/types/index.ts, .planning/phases/21-frontend-sveltekit-structure/21-CONTEXT.md</read_first>
  <action>
After route, store, and component imports have migrated, delete `frontend/src/lib/api.ts`.

Before deletion, run:

`rtk rg "\\$lib/api" frontend/src`

If any callsites remain besides `frontend/src/lib/api.ts` itself, stop and update those imports first. Do not delete the shim while callsites still depend on it.

After deletion, rerun:

`rtk rg "\\$lib/api" frontend/src`

The final command must return no matches.
  </action>
  <acceptance_criteria>
    - `frontend/src/lib/api.ts` does not exist
    - `rtk rg "\\$lib/api" frontend/src` returns no matches
    - `$lib/apis` and `$lib/types` still exist
  </acceptance_criteria>
  <verify>`rtk proxy test ! -f frontend/src/lib/api.ts && rtk rg "\\$lib/api" frontend/src` should return no matches for the `rg` command</verify>
  <done>The temporary shim is removed by the end of Phase 21 as required by D-05.</done>
</task>

<task id="21-04-04" type="execute">
  <name>Run final frontend verification and smoke checklist</name>
  <files>frontend/src/lib/apis, frontend/src/lib/types, frontend/src/routes, frontend/src/lib/components, frontend/src/lib/stores</files>
  <read_first>.planning/phases/19-refactor-map-safety-baseline/19-SAFETY-BASELINE.md, .planning/phases/21-frontend-sveltekit-structure/21-VALIDATION.md, frontend/package.json, frontend/svelte.config.js, frontend/playwright.config.ts</read_first>
  <action>
Run final static verification:

- `rtk rg "\\$lib/api" frontend/src` must return no matches
- `rtk rg "BASE = '/api'|credentials: 'include'|X-SubTeam-ID|res.status === 204" frontend/src/lib/apis/request.ts`
- `rtk rg "export \\{ tasks \\}|export \\{ statusSets \\}|export \\{ reminderSettings \\}" frontend/src/lib/apis/index.ts`
- `rtk rg "export type .*CustomStatus|export type .*ReminderSettings" frontend/src/lib/types/index.ts`
- `cd frontend && bun run check`
- `cd frontend && bun run build`

If the full stack is running, perform focused smoke checks for:

- `/`
- `/tasks`
- `/projects`
- `/milestones`
- `/team`
- `/timeline`
- `/performance`
- `/schedule`
- `/ai`
- `/login`
- `/register`
- `/invite/accept`
- notification bell polling
- admin sub-team switch sending `X-SubTeam-ID`

If Bun or the full stack is not available, document the exact blocker and the strongest fallback evidence in the Phase 21 summary when execution completes.
  </action>
  <acceptance_criteria>
    - No `$lib/api` imports remain
    - Protected request behavior remains present in `apis/request.ts`
    - API barrel exports key namespaces
    - Type barrel exports key shared types
    - `bun run check` passes or exact environment blocker is documented
    - `bun run build` passes or exact environment blocker is documented
    - No route file was moved or renamed
  </acceptance_criteria>
  <verify>`rtk rg "\\$lib/api" frontend/src` returns no matches; `cd frontend && bun run check`; `cd frontend && bun run build`</verify>
  <done>Frontend shared-code refactor is complete, type/build verification is green or environment blockers are documented, and protected routes remain stable.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-12 | Availability | Svelte route imports | mitigate | Update route imports only; do not move route files or alter route directories |
| T-21-13 | Integrity | Temporary compatibility shim | mitigate | Delete `frontend/src/lib/api.ts` only after `rtk rg "\\$lib/api" frontend/src` finds no dependent callsites |
| T-21-14 | Tampering | Request behavior | mitigate | Re-verify protected request wrapper strings after final import migration |
| T-21-15 | Availability | Production frontend build | mitigate | Run `bun run check` and `bun run build` before declaring Phase 21 complete |
</threat_model>

<verification>
- `rtk rg "\\$lib/api" frontend/src` should return no matches
- `rtk proxy test ! -f frontend/src/lib/api.ts`
- `rtk rg "BASE = '/api'|credentials: 'include'|X-SubTeam-ID|res.status === 204" frontend/src/lib/apis/request.ts`
- `rtk rg "export \\{ tasks \\}|export \\{ statusSets \\}|export \\{ reminderSettings \\}" frontend/src/lib/apis/index.ts`
- `cd frontend && bun run check`
- `cd frontend && bun run build`
</verification>

<success_criteria>
- All callsites import API namespaces from `$lib/apis`.
- Shared types are imported from `$lib/types` or justified direct type modules.
- The temporary `$lib/api` shim is removed before Phase 21 ends.
- Frontend check/build pass or exact environment blockers are documented.
- Route URLs and visual behavior are preserved.
</success_criteria>

