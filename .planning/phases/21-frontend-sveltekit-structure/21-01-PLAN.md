---
phase: "21-frontend-sveltekit-structure"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - ".planning/phases/21-frontend-sveltekit-structure/21-IMPORT-MAP.md"
  - "frontend/src/lib/types/status.ts"
  - "frontend/src/lib/types/notification.ts"
  - "frontend/src/lib/types/index.ts"
  - "frontend/src/lib/api.ts"
autonomous: true
user_setup:
  - "If `bun` is not available on PATH during execution, document the exact blocker and use the strongest available local fallback for Svelte type checking."
requirements: ["FRONT-01", "FRONT-03", "FRONT-04", "FRONT-05"]
must_haves:
  truths:
    - "D-06 - Move shared/exported API-facing types into `frontend/src/lib/types`, including status and reminder/notification types currently exported from `api.ts`."
    - "D-07 - Use feature-domain type modules such as `status.ts`, `notification.ts`, `task.ts`, and `project.ts` when new shared type files are needed."
    - "D-08 - Planner decides borderline local-vs-shared cases during import mapping; reused API-facing types move to `src/lib/types`."
    - "D-12 - Skip optional route-local component extraction in Phase 21."
    - "D-14 - Preserve all current route URLs and visual behavior."
  artifacts:
    - path: ".planning/phases/21-frontend-sveltekit-structure/21-IMPORT-MAP.md"
      provides: "Complete `$lib/api` importer inventory for final migration"
      contains: "## Importer Inventory"
    - path: "frontend/src/lib/types/status.ts"
      provides: "Shared status API-facing types"
      contains: "export interface CustomStatus"
    - path: "frontend/src/lib/types/notification.ts"
      provides: "Shared reminder settings types"
      contains: "export interface ReminderSettings"
  key_links:
    - ".planning/phases/21-frontend-sveltekit-structure/21-CONTEXT.md#type-centralization"
    - ".planning/phases/21-frontend-sveltekit-structure/21-PATTERNS.md#import-migration-pattern"
---

# Phase 21 Plan 01: Import Inventory and Shared Types

<objective>
Create the frontend import inventory and move shared API-facing status/reminder types into `src/lib/types` while keeping existing `$lib/api` callsites working.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-frontend-sveltekit-structure/21-CONTEXT.md
@.planning/phases/21-frontend-sveltekit-structure/21-RESEARCH.md
@.planning/phases/21-frontend-sveltekit-structure/21-VALIDATION.md
@.planning/phases/21-frontend-sveltekit-structure/21-PATTERNS.md
@.planning/phases/19-refactor-map-safety-baseline/19-FRONTEND-MAP.md
</context>

<tasks>

<task id="21-01-01" type="execute">
  <name>Create `$lib/api` importer inventory</name>
  <files>.planning/phases/21-frontend-sveltekit-structure/21-IMPORT-MAP.md</files>
  <read_first>.planning/phases/21-frontend-sveltekit-structure/21-PATTERNS.md, frontend/src/lib/api.ts, frontend/src/routes, frontend/src/lib/components, frontend/src/lib/stores</read_first>
  <action>
Create `.planning/phases/21-frontend-sveltekit-structure/21-IMPORT-MAP.md`.

Include:

- `## Importer Inventory`
- the command used: `rtk rg "\\$lib/api" frontend/src`
- one table row per current importer with columns: `File`, `Import Kind`, `Current Import`, `Target Import`
- value-import target convention: `$lib/apis`
- common type-import target convention: `$lib/types`
- direct type-module option for internals: `$lib/types/status` or `$lib/types/notification`

Do not modify any frontend source in this task.
  </action>
  <acceptance_criteria>
    - `21-IMPORT-MAP.md` exists
    - It contains `## Importer Inventory`
    - It contains `frontend/src/routes/tasks/+page.svelte`
    - It contains `frontend/src/lib/stores/auth.ts`
    - It contains `Target Import`
    - No frontend source file is modified by this task
  </acceptance_criteria>
  <verify>`rtk rg "## Importer Inventory|frontend/src/routes/tasks/\\+page.svelte|frontend/src/lib/stores/auth.ts|Target Import" .planning/phases/21-frontend-sveltekit-structure/21-IMPORT-MAP.md`</verify>
  <done>Importer map exists and identifies all `$lib/api` callsites for final migration.</done>
</task>

<task id="21-01-02" type="execute">
  <name>Create shared status type module</name>
  <files>frontend/src/lib/types/status.ts, frontend/src/lib/types/index.ts</files>
  <read_first>frontend/src/lib/api.ts, .planning/phases/21-frontend-sveltekit-structure/21-CONTEXT.md</read_first>
  <action>
Create `frontend/src/lib/types/status.ts` with the exact exported API-facing status types currently defined in `frontend/src/lib/api.ts`:

- `export type StatusSetScope = 'sub_team_default' | 'project';`
- `export interface CustomStatus`
- `export interface StatusSet`
- `export interface StatusTransition`
- `export interface StatusTransitionPair`

Preserve all field names and field types from the existing `api.ts` definitions:

- `CustomStatus`: `id`, `status_set_id`, `name`, `slug`, `color`, `position`, `is_done`, `is_archived`, `legacy_status`, `task_count`, `created_at`, `updated_at`
- `StatusSet`: `id`, `scope`, `sub_team_id`, `project_id`, `created_at`, `updated_at`, `statuses`
- `StatusTransition`: `id`, `status_set_id`, `from_status_id`, `to_status_id`, `created_at`
- `StatusTransitionPair`: `from_status_id`, `to_status_id`

Create `frontend/src/lib/types/index.ts` if it does not exist, and re-export status types with:

`export type { StatusSetScope, CustomStatus, StatusSet, StatusTransition, StatusTransitionPair } from './status';`
  </action>
  <acceptance_criteria>
    - `frontend/src/lib/types/status.ts` exists
    - `status.ts` exports `StatusSetScope`, `CustomStatus`, `StatusSet`, `StatusTransition`, and `StatusTransitionPair`
    - `frontend/src/lib/types/index.ts` re-exports the status types
    - Field names match the original `api.ts` status definitions
  </acceptance_criteria>
  <verify>`rtk rg "export type StatusSetScope|export interface CustomStatus|export interface StatusSet|export interface StatusTransition|export interface StatusTransitionPair" frontend/src/lib/types/status.ts && rtk rg "from './status'" frontend/src/lib/types/index.ts`</verify>
  <done>Status types live in `src/lib/types/status.ts` and are available through the type barrel.</done>
</task>

<task id="21-01-03" type="execute">
  <name>Create shared notification type module</name>
  <files>frontend/src/lib/types/notification.ts, frontend/src/lib/types/index.ts</files>
  <read_first>frontend/src/lib/api.ts, .planning/phases/21-frontend-sveltekit-structure/21-CONTEXT.md</read_first>
  <action>
Create `frontend/src/lib/types/notification.ts` with the exact exported reminder settings types currently defined in `frontend/src/lib/api.ts`:

- `export interface ReminderSettings`
- `export interface ReminderSettingsProposal`

Preserve all field names and field types from the existing `api.ts` definitions:

- `ReminderSettings`: `id`, `sub_team_id`, `lead_time_days`, `sprint_reminders_enabled`, `milestone_reminders_enabled`, `created_at`, `updated_at`
- `ReminderSettingsProposal`: `id`, `sub_team_id`, `proposed_by_id`, `reviewed_by_id`, `lead_time_days`, `sprint_reminders_enabled`, `milestone_reminders_enabled`, `status`, `created_at`, `reviewed_at`
- `ReminderSettingsProposal.status`: `'pending' | 'approved' | 'rejected'`

Update `frontend/src/lib/types/index.ts` to re-export notification types with:

`export type { ReminderSettings, ReminderSettingsProposal } from './notification';`
  </action>
  <acceptance_criteria>
    - `frontend/src/lib/types/notification.ts` exists
    - `notification.ts` exports `ReminderSettings` and `ReminderSettingsProposal`
    - `ReminderSettingsProposal.status` keeps the union `'pending' | 'approved' | 'rejected'`
    - `frontend/src/lib/types/index.ts` re-exports the notification types
  </acceptance_criteria>
  <verify>`rtk rg "export interface ReminderSettings|export interface ReminderSettingsProposal|'pending' \\| 'approved' \\| 'rejected'" frontend/src/lib/types/notification.ts && rtk rg "from './notification'" frontend/src/lib/types/index.ts`</verify>
  <done>Reminder settings types live in `src/lib/types/notification.ts` and are available through the type barrel.</done>
</task>

<task id="21-01-04" type="execute">
  <name>Update api.ts to consume and re-export shared types</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>frontend/src/lib/api.ts, frontend/src/lib/types/status.ts, frontend/src/lib/types/notification.ts, frontend/src/lib/types/index.ts</read_first>
  <action>
Update `frontend/src/lib/api.ts` so it no longer defines the moved status/reminder interfaces inline.

At the top of `api.ts`, add type imports needed by API namespace implementations:

`import type { StatusTransition, StatusTransitionPair } from '$lib/types/status';`

Add public type re-exports so existing callsites using `$lib/api` keep working during migration:

`export type { StatusSetScope, CustomStatus, StatusSet, StatusTransition, StatusTransitionPair } from '$lib/types/status';`

`export type { ReminderSettings, ReminderSettingsProposal } from '$lib/types/notification';`

Remove the inline `export type StatusSetScope`, `export interface CustomStatus`, `export interface StatusSet`, `export interface StatusTransition`, `export interface StatusTransitionPair`, `export interface ReminderSettings`, and `export interface ReminderSettingsProposal` blocks from `api.ts`.
  </action>
  <acceptance_criteria>
    - `api.ts` imports `StatusTransition` and `StatusTransitionPair` from `$lib/types/status`
    - `api.ts` re-exports status types from `$lib/types/status`
    - `api.ts` re-exports reminder settings types from `$lib/types/notification`
    - `api.ts` no longer contains inline `export interface CustomStatus`
    - `$lib/api` type callsites still compile
  </acceptance_criteria>
  <verify>`rtk rg "\\$lib/types/status|\\$lib/types/notification" frontend/src/lib/api.ts && rtk rg "export interface CustomStatus|export interface ReminderSettings" frontend/src/lib/api.ts` should return no matches for the second command; then run `cd frontend && bun run check` if Bun is available</verify>
  <done>Shared types are centralized while `$lib/api` compatibility remains intact.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Integrity | Import migration scope | mitigate | Create a complete `$lib/api` importer inventory before changing callsites |
| T-21-02 | Integrity | Shared TypeScript types | mitigate | Move only API-facing reused types and re-export them through `$lib/api` until final migration |
| T-21-03 | Availability | Svelte routes | mitigate | Do not move route files or extract route-local components in this plan |
</threat_model>

<verification>
- `rtk rg "## Importer Inventory|Target Import" .planning/phases/21-frontend-sveltekit-structure/21-IMPORT-MAP.md`
- `rtk rg "export interface CustomStatus|export interface ReminderSettings" frontend/src/lib/types`
- `rtk rg "\\$lib/types/status|\\$lib/types/notification" frontend/src/lib/api.ts`
- `cd frontend && bun run check` when Bun is available; if blocked, document the exact missing executable and run the strongest local fallback available.
</verification>

<success_criteria>
- Importer map exists before source migration begins.
- Shared status and reminder types live under `frontend/src/lib/types`.
- Existing `$lib/api` type imports continue to work.
- No route files are moved and no UI behavior is changed.
</success_criteria>

