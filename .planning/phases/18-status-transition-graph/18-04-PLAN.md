---
phase: "18-status-transition-graph"
plan: 4
type: execute
wave: 4
depends_on: ["18-01-PLAN.md", "18-02-PLAN.md", "18-03-PLAN.md"]
files_modified:
  - "frontend/src/routes/tasks/+page.svelte"
  - "frontend/src/lib/components/tasks/KanbanBoard.svelte"
  - "frontend/src/lib/api.ts"
  - "backend/tests/test_tasks.py"
  - "backend/tests/test_status_sets.py"
autonomous: true
requirements: ["TRANS-02", "TRANS-03"]
must_haves:
  truths:
    - "D-13: Backend blocked transitions return HTTP 422 with structured detail"
    - "D-14: Kanban uses hybrid feedback: prevent obvious invalid targets client-side and handle backend 422 by reverting and showing a toast"
    - "D-15: Blocked-move toast copy is direct and status-specific from structured backend response"
    - "D-16: Task edit dropdown shows allowed target statuses plus current status"
    - "D-17: Kanban column headers use small hint icons/tooltips for transition restrictions"
    - "D-18: Backend blocked-transition 422 causes frontend to refresh status set/transition data"
  artifacts:
    - path: "frontend/src/lib/components/tasks/KanbanBoard.svelte"
      provides: "Kanban transition hints and client-side blocked target handling"
      contains: "transition"
    - path: "frontend/src/routes/tasks/+page.svelte"
      provides: "Transition-aware task edit dropdown and structured 422 handling"
      contains: "status_transition_blocked"
---

<objective>
Connect transition rules to task movement UX: Kanban drag/drop, task edit dropdown filtering, structured blocked-move feedback, and final regression verification.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-status-transition-graph/18-CONTEXT.md
@.planning/phases/18-status-transition-graph/18-RESEARCH.md
@.planning/phases/18-status-transition-graph/18-UI-SPEC.md
@.planning/phases/18-status-transition-graph/18-VALIDATION.md
@.planning/phases/18-status-transition-graph/18-01-PLAN.md
@.planning/phases/18-status-transition-graph/18-02-PLAN.md
@.planning/phases/18-status-transition-graph/18-03-PLAN.md
</context>

<tasks>

<task id="18-04-01" type="execute">
  <name>Load transitions for task page workflows</name>
  <read_first>frontend/src/routes/tasks/+page.svelte, frontend/src/lib/api.ts</read_first>
  <action>
Update `/tasks/+page.svelte`:

- Load transitions after loading the effective status set.
- Store transition rows in page state.
- Refresh transition rows when `filterProjectId` or effective status set changes.
- Treat empty transition list as unrestricted.
- Build helpers:
  - `hasTransitionRules`
  - `allowedTargetStatusIds(currentStatusId)`
  - `isMoveAllowed(task, targetStatusId)`

Keep helpers local unless another component needs them directly.
  </action>
  <acceptance_criteria>
    - `/tasks/+page.svelte` calls `statusSets.getTransitions`
    - Empty transition list allows all active statuses
    - Helper includes current status as allowed/no-op
    - Transition data refreshes when effective status set refreshes
  </acceptance_criteria>
</task>

<task id="18-04-02" type="execute">
  <name>Filter task edit dropdown to allowed targets plus current status</name>
  <read_first>frontend/src/routes/tasks/+page.svelte, .planning/phases/18-status-transition-graph/18-UI-SPEC.md</read_first>
  <action>
Update the edit task modal status dropdown:

- For existing tasks, show allowed target statuses plus the current status.
- The current selected status must never disappear from the dropdown.
- Empty transition list means all active statuses are shown.
- Creation form remains unconstrained because there is no prior status move.
- Legacy fallback options remain available when no DB-backed active statuses are loaded.
  </action>
  <acceptance_criteria>
    - Edit mode dropdown is derived from allowed targets
    - Current status remains present even if not otherwise allowed
    - Create mode status behavior remains unchanged
    - Legacy fallback remains usable without DB statuses
  </acceptance_criteria>
</task>

<task id="18-04-03" type="execute">
  <name>Add Kanban transition hints and client-side invalid target handling</name>
  <read_first>frontend/src/lib/components/tasks/KanbanBoard.svelte, .planning/phases/18-status-transition-graph/18-UI-SPEC.md</read_first>
  <action>
Update `KanbanBoard.svelte`:

- Accept transition data or allowed-move callbacks from `/tasks/+page.svelte`.
- Add small `lucide-svelte` hint icons/tooltips in restricted column headers.
- Prevent or revert obvious invalid drops when the current task and target status are known.
- Dispatch enough detail for parent to handle status updates consistently.
- Do not resize columns/cards when hint icons appear.
- Preserve backend attempted-move path for ambiguous/stale cases.
  </action>
  <acceptance_criteria>
    - `KanbanBoard.svelte` renders a compact restriction hint for restricted columns
    - Invalid known drops are blocked or reverted client-side
    - Column/card layout dimensions do not change when hints render
    - Backend 422 path remains handled by parent
  </acceptance_criteria>
</task>

<task id="18-04-04" type="execute">
  <name>Handle structured backend 422 feedback and refresh transition data</name>
  <read_first>frontend/src/routes/tasks/+page.svelte, frontend/src/lib/api.ts, backend/app/schemas.py</read_first>
  <action>
Update task update/move error handling:

- Preserve previous task visual state on failed move.
- Detect structured error detail with `code === "status_transition_blocked"`.
- Toast `Cannot move from {current} to {target}` when names are available.
- Fallback toast: `This move is not allowed by the workflow rules.`
- Refresh status set and transition data after a blocked-transition 422.
- If the existing API `request` helper discards structured detail, minimally extend it so callers can inspect structured `detail` without breaking existing `.message` callers.
  </action>
  <acceptance_criteria>
    - `/tasks/+page.svelte` contains `status_transition_blocked`
    - Blocked move toast uses current/target names when available
    - Transition data refreshes after backend 422
    - Existing error message behavior continues for non-transition errors
  </acceptance_criteria>
</task>

<task id="18-04-05" type="execute">
  <name>Run full phase regression and document verification</name>
  <read_first>.planning/phases/18-status-transition-graph/18-VALIDATION.md, backend/tests/test_tasks.py, backend/tests/test_status_sets.py, frontend/package.json</read_first>
  <action>
Run and fix issues from:

- `rtk pytest backend/tests/test_status_sets.py backend/tests/test_tasks.py`
- `rtk pytest backend/tests`
- `cd frontend && rtk bun run check`

Manual checks to perform or document if no dev server/browser is available:

- Status manager shows transition matrix and preview.
- Generate linear flow produces ordered forward transitions.
- Empty transition list leaves Kanban/edit movement unrestricted.
- Restricted graph blocks invalid Kanban move with toast.
- Task edit dropdown shows allowed targets plus current status.

Create the execution summary later during execute-phase; for this plan, ensure the implementation leaves enough test/manual notes for verification.
  </action>
  <acceptance_criteria>
    - Targeted backend tests pass
    - Full backend tests pass or blockers are documented
    - Frontend Svelte check passes
    - Manual verification notes cover matrix, preview, empty graph, blocked Kanban move, and dropdown filtering
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-13 | Client-side trust | Kanban/dropdown filtering | mitigate | Client hints are advisory; backend 422 remains authoritative and handled |
| T-18-14 | Spoofing/stale state | Transition data in browser | mitigate | Refresh status set and transitions after backend blocked-transition 422 |
| T-18-15 | UX/data mismatch | Optimistic moves | mitigate | Preserve/revert prior task state on failed moves |
| T-18-16 | Information disclosure | Error toast | mitigate | Use only current/target status names returned for the attempted move |
</threat_model>

<verification>
- `rtk pytest backend/tests/test_status_sets.py backend/tests/test_tasks.py`
- `rtk pytest backend/tests`
- `cd frontend && rtk bun run check`
- Manual UI checks listed in task 18-04-05
</verification>

<success_criteria>
- Kanban gives compact transition hints and handles blocked moves without stale visual state.
- Task edit dropdown only offers allowed target statuses plus the current status when rules exist.
- Backend structured 422 errors produce direct, status-specific frontend feedback.
- Full phase regression checks pass or any environment blockers are documented for verify-work.
</success_criteria>
