---
phase: "25-team-weekly-board-ai-summary"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - "backend/app/models/board.py"
  - "backend/app/models/__init__.py"
  - "backend/app/schemas/board.py"
  - "backend/app/schemas/__init__.py"
  - "backend/alembic/versions/"
autonomous: true
requirements: ["BOARD-01", "BOARD-02", "BOARD-06", "BOARD-07", "BOARD-08"]
must_haves:
  truths:
    - "D-01: Each member has exactly one primary weekly post per ISO week, plus optional follow-up append entries."
    - "D-02: Follow-up entries belong to the author's weekly post model and are not comments on another member's post."
    - "D-08: The current ISO week remains the default landing state, so persistence must expose week identity directly."
    - "D-13: Weekly summary regeneration overwrites the stored summary for that ISO week."
  artifacts:
    - path: "backend/app/models/board.py"
      provides: "Weekly board persistence for primary posts, append entries, and stored summaries"
      contains: "class WeeklyPost"
    - path: "backend/app/schemas/board.py"
      provides: "Board request/response contracts"
      contains: "class WeeklyPostCreate"
    - path: "backend/alembic/versions/"
      provides: "Migration adding weekly board tables and uniqueness constraints"
      contains: "weekly_board"
---

<objective>
Create the backend data contract for the weekly board: SQLAlchemy models, Pydantic schemas, and Alembic migration.

Purpose: Phase 25 cannot safely implement week navigation, author ownership, append entries, or cached summaries until the board domain is explicit and versioned.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-team-weekly-board-ai-summary/25-CONTEXT.md
@.planning/phases/25-team-weekly-board-ai-summary/25-RESEARCH.md
@.planning/phases/25-team-weekly-board-ai-summary/25-UI-SPEC.md
@.planning/phases/25-team-weekly-board-ai-summary/25-VALIDATION.md
</context>

<tasks>

<task id="25-01-01" type="execute">
  <name>Add weekly board persistence models and exports</name>
  <read_first>
    backend/app/models/updates.py
    backend/app/models/knowledge.py
    backend/app/models/__init__.py
    backend/app/models/notifications.py
  </read_first>
  <action>
Create `backend/app/models/board.py` and export its symbols from `backend/app/models/__init__.py`.

Add these SQLAlchemy models:

- `WeeklyPost`
- `WeeklyPostAppend`
- `WeeklyBoardSummary`

Recommended persistence contract:

`WeeklyPost`
- `id`
- `author_id`
- `sub_team_id`
- `iso_year`
- `iso_week`
- `week_start_date`
- `content`
- `created_at`
- `updated_at`
- unique constraint on `(author_id, iso_year, iso_week)`

`WeeklyPostAppend`
- `id`
- `post_id`
- `author_id`
- `content`
- `created_at`
- `updated_at`

`WeeklyBoardSummary`
- `id`
- `sub_team_id`
- `iso_year`
- `iso_week`
- `week_start_date`
- `summary_text`
- `source_post_count`
- `generated_by_mode` (`manual` or `scheduled`)
- `generated_at`
- unique constraint on `(sub_team_id, iso_year, iso_week)`

Use the existing UTC-naive timestamp pattern from the current models. Keep append entries separate from the primary post row instead of storing them in a JSON blob.
  </action>
  <acceptance_criteria>
    - `backend/app/models/board.py` exists
    - the file contains `class WeeklyPost`
    - the file contains `class WeeklyPostAppend`
    - the file contains `class WeeklyBoardSummary`
    - the file contains a unique constraint for one primary post per author per ISO week
    - `backend/app/models/__init__.py` re-exports the new board models
  </acceptance_criteria>
</task>

<task id="25-01-02" type="execute">
  <name>Add board schemas for posts, append entries, week payloads, and summaries</name>
  <read_first>
    backend/app/schemas/updates.py
    backend/app/schemas/knowledge.py
    backend/app/schemas/__init__.py
    backend/app/models/board.py
  </read_first>
  <action>
Create `backend/app/schemas/board.py` and re-export it from `backend/app/schemas/__init__.py`.

Add schema types for:

- `WeeklyPostCreate`
- `WeeklyPostUpdate`
- `WeeklyPostAppendCreate`
- `WeeklyPostAppendUpdate`
- `WeeklyPostAppendOut`
- `WeeklyPostOut`
- `WeeklyBoardSummaryOut`
- `WeeklyBoardWeekOptionOut`
- `WeeklyBoardWeekResponse`

Field expectations:

- primary and append create/update payloads accept raw markdown `content`
- responses expose nested author data the same way nearby domains do
- `WeeklyBoardWeekResponse` contains:
  - selected week metadata
  - summary object or null
  - post list with nested append entries
  - available week options
  - `viewer_can_post`
  - `is_current_week`

Normalize and validate week parameters in schemas/helpers where useful, but do not move business rules like ownership or cooldown into the schema layer.
  </action>
  <acceptance_criteria>
    - `backend/app/schemas/board.py` exists
    - the file contains `class WeeklyPostCreate`
    - the file contains `class WeeklyPostAppendCreate`
    - the file contains `class WeeklyBoardWeekResponse`
    - `backend/app/schemas/__init__.py` re-exports the board schemas
  </acceptance_criteria>
</task>

<task id="25-01-03" type="execute">
  <name>Add Alembic migration for weekly board tables and uniqueness rules</name>
  <read_first>
    backend/alembic/versions/
    backend/app/models/board.py
    backend/app/models/knowledge.py
  </read_first>
  <action>
Create a new Alembic migration chained to the current head.

The migration must:

1. Create `weekly_posts`
2. Create `weekly_post_appends`
3. Create `weekly_board_summaries`
4. Add foreign keys for authors, parent post, and sub-team scope
5. Add indexes for:
   - week lookup fields
   - author lookup fields
   - summary lookup fields
6. Add uniqueness for:
   - one primary post per author per ISO week
   - one summary per scoped week

Do not add a comments/threads table and do not encode append entries inside the primary post row.
  </action>
  <acceptance_criteria>
    - a new migration exists under `backend/alembic/versions/`
    - the migration contains `weekly_posts`
    - the migration contains `weekly_post_appends`
    - the migration contains `weekly_board_summaries`
    - the migration includes uniqueness for author/week and summary/week
  </acceptance_criteria>
</task>

<task id="25-01-04" type="execute">
  <name>Verify imports and migration parsing for the board data contract</name>
  <read_first>
    backend/app/models/__init__.py
    backend/app/schemas/__init__.py
    backend/alembic/versions/
  </read_first>
  <action>
Run these exact verification commands:

```bash
rtk python -m compileall backend/app
cd backend && rtk python -c "from app.models import WeeklyPost, WeeklyPostAppend, WeeklyBoardSummary; from app.schemas import WeeklyPostCreate, WeeklyBoardWeekResponse; print('board contracts ok')"
```

If a migration parse spot-check is useful, add a Python import check for the new migration module and record the exact result in the summary.
  </action>
  <acceptance_criteria>
    - `rtk python -m compileall backend/app` exits 0
    - board model imports succeed
    - board schema imports succeed
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure | Weekly board persistence | mitigate | Store sub-team scope explicitly on posts and summaries so queries can filter correctly |
| T-25-02 | Tampering | Author-week uniqueness | mitigate | Enforce one primary post per author per ISO week in the database |
| T-25-03 | Integrity | Append-entry structure | mitigate | Keep append entries in a dedicated table tied to the parent post instead of free-form nested blobs |
</threat_model>

<verification>
- `rtk python -m compileall backend/app`
- `cd backend && rtk python -c "from app.models import WeeklyPost, WeeklyPostAppend, WeeklyBoardSummary; from app.schemas import WeeklyPostCreate, WeeklyBoardWeekResponse; print('board contracts ok')"`
</verification>

<success_criteria>
- The codebase has a dedicated persistence model for weekly posts, append entries, and stored summaries.
- Week identity and one-primary-post-per-author-per-week are enforceable in the schema.
- The board domain is importable from the aggregate model/schema surfaces used elsewhere in the app.
</success_criteria>

<output>
After completion, create `.planning/phases/25-team-weekly-board-ai-summary/25-01-SUMMARY.md`
</output>
