---
phase: "25-team-weekly-board-ai-summary"
plan: 4
type: execute
wave: 3
depends_on: ["25-02", "25-03"]
files_modified:
  - "frontend/src/lib/apis/board.ts"
  - "frontend/src/lib/apis/index.ts"
  - "frontend/src/lib/stores/board.ts"
  - "frontend/src/lib/components/board/WeeklySummaryPanel.svelte"
  - "frontend/src/lib/components/board/WeeklyPostComposer.svelte"
  - "frontend/src/lib/components/board/WeeklyPostCard.svelte"
  - "frontend/src/lib/components/board/AppendEntryComposer.svelte"
  - "frontend/src/lib/components/board/WeekNavigator.svelte"
  - "frontend/src/routes/board/+page.svelte"
  - "frontend/src/routes/+layout.svelte"
autonomous: true
requirements: ["BOARD-01", "BOARD-02", "BOARD-03", "BOARD-04", "BOARD-06", "BOARD-07", "BOARD-08"]
must_haves:
  truths:
    - "D-04: `/board` is a summary-first week view with the summary before the digest list."
    - "D-05: The board should read like a weekly digest, not a chat feed."
    - "D-07: Week navigation includes previous/current/next controls plus a compact week picker."
    - "D-12: Cooldown feedback remains visible while the stored summary stays on screen."
    - "D-14: Create/edit uses a markdown textarea with a preview toggle."
    - "D-15: Every markdown render path must use `DOMPurify.sanitize(marked.parse(raw))` before `{@html}`."
    - "D-16: Preview is toggle-based, not split-pane."
  artifacts:
    - path: "frontend/src/routes/board/+page.svelte"
      provides: "Weekly board route with summary-first layout and current/past week behavior"
      contains: "Weekly Board"
    - path: "frontend/src/lib/components/board/WeeklySummaryPanel.svelte"
      provides: "Stored summary, empty, cooldown, and no-updates states"
      contains: "AI Weekly Summary"
    - path: "frontend/src/lib/components/board/WeekNavigator.svelte"
      provides: "ISO week navigation controls"
      contains: "Current week"
  key_links:
    - from: "frontend/src/routes/board/+page.svelte"
      to: "frontend/src/lib/apis/board.ts"
      via: "load and mutate board data through the dedicated API module"
      pattern: "board"
    - from: "frontend/src/routes/+layout.svelte"
      to: "frontend/src/routes/board/+page.svelte"
      via: "add the Weekly Board nav entry near Updates"
      pattern: "/board"
---

<objective>
Deliver the `/board` frontend experience: nav entry, week navigation, summary panel, markdown composer with safe preview, digest cards, append-entry UX, and frontend verification.

Purpose: This plan makes the weekly board usable by members in the way defined by the UI-SPEC and roadmap success criteria.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-team-weekly-board-ai-summary/25-CONTEXT.md
@.planning/phases/25-team-weekly-board-ai-summary/25-RESEARCH.md
@.planning/phases/25-team-weekly-board-ai-summary/25-UI-SPEC.md
@.planning/phases/25-team-weekly-board-ai-summary/25-VALIDATION.md
@.planning/phases/25-team-weekly-board-ai-summary/25-02-PLAN.md
@.planning/phases/25-team-weekly-board-ai-summary/25-03-PLAN.md
</context>

<tasks>

<task id="25-04-01" type="execute">
  <name>Add board API module, store, and sidebar navigation entry</name>
  <read_first>
    frontend/src/lib/apis/updates.ts
    frontend/src/lib/apis/index.ts
    frontend/src/lib/stores/updates.ts
    frontend/src/routes/+layout.svelte
  </read_first>
  <action>
Create `frontend/src/lib/apis/board.ts` and `frontend/src/lib/stores/board.ts`, and update `frontend/src/lib/apis/index.ts` plus `frontend/src/routes/+layout.svelte`.

API module should cover:

- `getWeek(...)`
- `createPost(...)`
- `updatePost(...)`
- `deletePost(...)`
- `createAppend(...)`
- `updateAppend(...)`
- `deleteAppend(...)`
- `summarizeWeek(...)`

Store should hold:

- selected week
- week payload
- loading flags
- summary action state

Add a `Weekly Board` sidebar item near `Updates` using the icon defined in the UI-SPEC.
  </action>
  <acceptance_criteria>
    - `frontend/src/lib/apis/board.ts` exists
    - `frontend/src/lib/stores/board.ts` exists
    - `frontend/src/lib/apis/index.ts` exports `board`
    - `frontend/src/routes/+layout.svelte` contains `/board`
    - the nav label is `Weekly Board`
  </acceptance_criteria>
</task>

<task id="25-04-02" type="execute">
  <name>Create board UI components for summary, navigation, composer, posts, and append entries</name>
  <read_first>
    frontend/src/lib/components/updates/StandupCard.svelte
    frontend/src/routes/updates/+page.svelte
    .planning/phases/25-team-weekly-board-ai-summary/25-UI-SPEC.md
  </read_first>
  <action>
Create these components:

- `WeeklySummaryPanel.svelte`
- `WeeklyPostComposer.svelte`
- `WeeklyPostCard.svelte`
- `AppendEntryComposer.svelte`
- `WeekNavigator.svelte`

Implement the UI-SPEC contract exactly:

- summary-first hierarchy
- write/preview toggle, not split-pane
- sanitized markdown preview/rendering with `DOMPurify.sanitize(marked.parse(raw))`
- quiet empty/cooldown/no-updates states
- author-only edit/delete/add-follow-up controls
- compact digest styling rather than chat styling

Do not add a comment-thread UI and do not introduce a new visual system.
  </action>
  <acceptance_criteria>
    - the summary panel contains `AI Weekly Summary`
    - the navigator contains `Current week`
    - the composer contains `Write` and `Preview`
    - markdown render paths sanitize before `{@html}`
    - append-entry UI exists as a separate component
  </acceptance_criteria>
</task>

<task id="25-04-03" type="execute">
  <name>Assemble the `/board` route with current-week and historical-week behavior</name>
  <read_first>
    frontend/src/routes/updates/+page.svelte
    frontend/src/lib/apis/board.ts
    frontend/src/lib/stores/board.ts
    frontend/src/lib/components/board/
    .planning/phases/25-team-weekly-board-ai-summary/25-UI-SPEC.md
  </read_first>
  <action>
Create `frontend/src/routes/board/+page.svelte`.

The route must:

- default to the current ISO week on first load
- render summary panel above the composer and digest list
- support previous/current/next week navigation and a compact week picker
- hide the composer and mutation controls for past weeks
- show the approved empty states for current and past weeks
- keep follow-up entries nested under their parent primary post
- refresh store state after post, append, and summary mutations

Keep the page consistent with existing TeamFlow density and spacing. Do not add a second board route or a full-page modal workflow.
  </action>
  <acceptance_criteria>
    - `frontend/src/routes/board/+page.svelte` exists
    - the page title is `Weekly Board · TeamFlow`
    - the route shows summary before the digest list
    - past weeks are read-only in the UI
  </acceptance_criteria>
</task>

<task id="25-04-04" type="execute">
  <name>Run frontend verification and document manual board checks</name>
  <read_first>
    frontend/package.json
    frontend/src/routes/board/+page.svelte
    .planning/phases/25-team-weekly-board-ai-summary/25-VALIDATION.md
  </read_first>
  <action>
Run these exact commands:

```bash
cd frontend && rtk bun run check
cd frontend && rtk bun run build
```

Then perform or document these manual checks:

- `/board` appears in the sidebar and loads
- current week is the default view
- past weeks are read-only
- summary panel shows stored, empty, no-updates, and cooldown states correctly
- preview rendering is sanitized
- only the author sees edit/delete/add-follow-up actions

If browser/manual checks cannot be run in this environment, record that exact limitation in the summary.
  </action>
  <acceptance_criteria>
    - `cd frontend && rtk bun run check` exits 0
    - `cd frontend && rtk bun run build` exits 0
    - manual verification notes cover navigation, current-week default, past-week read-only behavior, summary states, sanitization, and author-only actions
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Stored XSS | Markdown preview and render | mitigate | Sanitize all markdown-derived HTML before `{@html}` |
| T-25-04 | Abuse / Cost | Summary action UX | mitigate | Reflect server cooldown state clearly and disable rapid regeneration attempts |
| T-25-07 | UX Integrity | Current vs past week behavior | mitigate | Hide composer and mutation controls for past weeks and keep digest ordering stable |
</threat_model>

<verification>
- `cd frontend && rtk bun run check`
- `cd frontend && rtk bun run build`
- Manual checks listed in task `25-04-04`
</verification>

<success_criteria>
- `/board` is accessible from the main nav and loads as a summary-first weekly digest.
- Members can write current-week markdown updates with safe preview/rendering and author-only follow-up actions.
- The summary panel and week navigator expose current, historical, empty, and cooldown states cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/25-team-weekly-board-ai-summary/25-04-SUMMARY.md`
</output>
