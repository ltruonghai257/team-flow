---
phase: "25-team-weekly-board-ai-summary"
plan: 3
type: execute
wave: 2
depends_on: ["25-01", "25-02"]
files_modified:
  - "backend/app/services/weekly_board.py"
  - "backend/app/routers/board.py"
  - "backend/app/internal/scheduler_jobs.py"
  - "backend/tests/test_board.py"
autonomous: true
requirements: ["BOARD-04", "BOARD-05", "BOARD-06"]
must_haves:
  truths:
    - "D-09: A persistent AI summary panel exists for the selected week."
    - "D-10: If no stored summary exists, the UI gets an empty-state path with a summarize action."
    - "D-11: Stored summaries are visible immediately for current and past weeks."
    - "D-12: On-demand regeneration is blocked for 30 minutes and surfaces clear cooldown metadata."
    - "D-13: Regeneration overwrites the stored summary for that ISO week."
  artifacts:
    - path: "backend/app/services/weekly_board.py"
      provides: "Shared summary-generation and cooldown logic for manual and scheduled runs"
      contains: "generate_weekly_board_summary"
    - path: "backend/app/internal/scheduler_jobs.py"
      provides: "Sunday 23:00 weekly board summary automation"
      contains: "CronTrigger"
    - path: "backend/app/routers/board.py"
      provides: "On-demand summary trigger endpoint"
      contains: "summary"
---

<objective>
Implement AI summary generation, caching/cooldown behavior, and Sunday 23:00 scheduler automation for the weekly board.

Purpose: This plan delivers the phase's core AI behavior without duplicating logic between user-triggered summaries and scheduled summaries.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-team-weekly-board-ai-summary/25-CONTEXT.md
@.planning/phases/25-team-weekly-board-ai-summary/25-RESEARCH.md
@.planning/phases/25-team-weekly-board-ai-summary/25-UI-SPEC.md
@.planning/phases/25-team-weekly-board-ai-summary/25-VALIDATION.md
@.planning/phases/25-team-weekly-board-ai-summary/25-01-PLAN.md
@.planning/phases/25-team-weekly-board-ai-summary/25-02-PLAN.md
</context>

<tasks>

<task id="25-03-01" type="execute">
  <name>Add shared weekly summary generation and cooldown helpers</name>
  <read_first>
    backend/app/routers/ai.py
    backend/app/utils/ai_client.py
    backend/app/services/weekly_board.py
    backend/app/models/board.py
  </read_first>
  <action>
Extend `backend/app/services/weekly_board.py` with reusable summary helpers such as:

- `generate_weekly_board_summary(...)`
- `get_summary_cooldown(...)`
- `upsert_weekly_board_summary(...)`

Behavior rules:

- build prompts only from stored weekly post and append-entry text
- use the existing AI client helper, not a second integration path
- if the selected scoped week has no posts, persist and return the exact fallback summary `No updates this week`
- on manual requests, return cached/stored summary during the 30-minute cooldown instead of calling the AI again
- after cooldown expiry, regeneration overwrites the stored summary row for that week

Keep manual and scheduled generation paths on the same service code.
  </action>
  <acceptance_criteria>
    - `backend/app/services/weekly_board.py` contains `generate_weekly_board_summary`
    - the service contains 30-minute cooldown logic
    - the service short-circuits to `No updates this week` when no posts exist
    - the service writes through `WeeklyBoardSummary`
  </acceptance_criteria>
</task>

<task id="25-03-02" type="execute">
  <name>Add on-demand summary endpoint to the board router</name>
  <read_first>
    backend/app/routers/board.py
    backend/app/services/weekly_board.py
    backend/app/schemas/board.py
  </read_first>
  <action>
Extend `backend/app/routers/board.py` with an endpoint for triggering and returning a weekly summary for the selected week.

Recommended shape:

- `POST /api/board/week/summary`
  - current week by default
  - optional year/week if historical regeneration is intentionally supported
  - returns stored summary content plus cooldown metadata

Required behavior:

- visible users can fetch stored summaries for current and past weeks
- on-demand generation uses the shared service
- manual re-click within cooldown returns cached content and remaining-time metadata
- response shape is compatible with a summary panel that can show:
  - available summary
  - empty/no summary state
  - no-updates state
  - cooldown-blocked state
  </action>
  <acceptance_criteria>
    - `backend/app/routers/board.py` contains a summary trigger endpoint
    - the endpoint returns stored summary data
    - cooldown responses do not require a second AI call
  </acceptance_criteria>
</task>

<task id="25-03-03" type="execute">
  <name>Add Sunday 23:00 scheduler integration for automatic summaries</name>
  <read_first>
    backend/app/internal/scheduler_jobs.py
    backend/app/services/weekly_board.py
    backend/app/models/board.py
  </read_first>
  <action>
Update `backend/app/internal/scheduler_jobs.py` to run a weekly summary job at Sunday 23:00 using APScheduler CronTrigger.

The job should:

- iterate over relevant sub-team scopes
- target the current ISO week at execution time
- call the shared summary service in `scheduled` mode
- reuse the same empty-week short-circuit path
- avoid breaking existing notification jobs

Do not introduce a separate scheduler module for board summaries.
  </action>
  <acceptance_criteria>
    - `backend/app/internal/scheduler_jobs.py` contains a weekly board summary job
    - the scheduler uses a Sunday 23:00 cron trigger
    - existing jobs remain registered
  </acceptance_criteria>
</task>

<task id="25-03-04" type="execute">
  <name>Add backend tests for summary caching, cooldown, and scheduler behavior</name>
  <read_first>
    backend/tests/test_board.py
    backend/app/services/weekly_board.py
    backend/app/internal/scheduler_jobs.py
    backend/app/routers/board.py
  </read_first>
  <action>
Extend `backend/tests/test_board.py` to cover:

1. manual summary generation for a week with posts
2. immediate re-trigger within 30 minutes returns cached content
3. regeneration after cooldown overwrites the stored summary
4. empty week stores `No updates this week`
5. scheduled summary path writes through the same summary table

Stub or patch the AI client so tests assert prompt-path behavior without depending on a live model.
  </action>
  <acceptance_criteria>
    - `backend/tests/test_board.py` covers cooldown behavior
    - the tests prove empty weeks do not need an AI call
    - the tests cover scheduled summary generation
  </acceptance_criteria>
</task>

<task id="25-03-05" type="execute">
  <name>Run focused backend verification for summary generation and scheduler hooks</name>
  <read_first>
    backend/tests/test_board.py
    backend/app/services/weekly_board.py
    backend/app/internal/scheduler_jobs.py
  </read_first>
  <action>
Run these exact verification commands:

```bash
rtk python -m compileall backend/app
cd backend && rtk pytest tests/test_board.py -q
```

If unrelated failures appear, record the blocker exactly and preserve the new summary assertions.
  </action>
  <acceptance_criteria>
    - `rtk python -m compileall backend/app` exits 0
    - `cd backend && rtk pytest tests/test_board.py -q` exits 0 or the unrelated blocker is recorded exactly
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Abuse / Cost | On-demand summary endpoint | mitigate | Enforce 30-minute cooldown server-side and reuse cached summaries |
| T-25-05 | Integrity | Summary generation | mitigate | Build summaries only from stored weekly content and short-circuit empty weeks |
| T-25-06 | Reliability | Scheduler integration | mitigate | Use the existing scheduler bootstrap and keep the job in the same module as current jobs |
</threat_model>

<verification>
- `rtk python -m compileall backend/app`
- `cd backend && rtk pytest tests/test_board.py -q`
</verification>

<success_criteria>
- The board has one shared summary-generation path for manual and scheduled runs.
- On-demand summary generation respects the 30-minute cooldown and returns cached results when blocked.
- Sunday 23:00 automation writes weekly summaries without calling the AI for empty weeks.
</success_criteria>

<output>
After completion, create `.planning/phases/25-team-weekly-board-ai-summary/25-03-SUMMARY.md`
</output>
