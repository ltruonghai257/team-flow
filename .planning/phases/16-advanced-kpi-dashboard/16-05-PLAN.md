---
phase: "16-advanced-kpi-dashboard"
plan: 5
type: execute
wave: 5
depends_on: ["16-01", "16-02", "16-03", "16-04"]
files_modified:
  - ".planning/phases/16-advanced-kpi-dashboard/16-VALIDATION.md"
  - "frontend/src/routes/performance/+page.svelte"
  - "backend/app/routers/performance.py"
autonomous: true
requirements: ["KPI-01", "KPI-02", "KPI-03", "KPI-04", "KPI-05"]
must_haves:
  truths:
    - "Phase 16 satisfies all KPI requirements"
    - "Backend and frontend verification commands pass"
    - "Manual UAT checklist is available"
  artifacts:
    - path: ".planning/phases/16-advanced-kpi-dashboard/16-VALIDATION.md"
      provides: "Validation checklist"
      contains: "Manual UAT"
  key_links:
    - from: ".planning/phases/16-advanced-kpi-dashboard/16-VALIDATION.md"
      to: ".planning/phases/16-advanced-kpi-dashboard/16-CONTEXT.md"
      via: "Validation requirements"
      pattern: "KPI score"
---

<objective>
Run final implementation verification and fill any remaining validation gaps for Phase 16.

Purpose: Confirm the completed phase satisfies KPI-01 through KPI-05, the UI-SPEC, and the custom status completion dependency.
Output: Verified Phase 16 implementation with updated validation notes if needed.
</objective>

<execution_context>
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-advanced-kpi-dashboard/16-CONTEXT.md
@.planning/phases/16-advanced-kpi-dashboard/16-UI-SPEC.md
@.planning/phases/16-advanced-kpi-dashboard/16-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify KPI requirement coverage in code</name>
  <files>backend/app/routers/performance.py, frontend/src/routes/performance/+page.svelte</files>
  <read_first>backend/app/routers/performance.py, frontend/src/routes/performance/+page.svelte, .planning/REQUIREMENTS.md</read_first>
  <action>
Check that implementation covers each KPI requirement:
- KPI-01: velocity chart for last 6 sprints per member
- KPI-02: burndown chart for active and closed sprint selection
- KPI-03: cycle time by task type for last 3 months
- KPI-04: throughput by member and type for last 8 weeks
- KPI-05: bugs reported/resolved and MTTR per member for last 30 days

If any required chart or endpoint is missing, implement the smallest missing piece in the relevant prior-plan files before continuing.
  </action>
  <acceptance_criteria>
    - `grep "velocity" -i backend/app/routers/performance.py` returns a match
    - `grep "burndown" -i backend/app/routers/performance.py` returns a match
    - `grep "cycle" -i backend/app/routers/performance.py` returns a match
    - `grep "throughput" -i backend/app/routers/performance.py` returns a match
    - `grep "MTTR" frontend/src/routes/performance/+page.svelte` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify custom status completion semantics</name>
  <files>backend/app/routers/performance.py</files>
  <read_first>backend/app/routers/performance.py, backend/app/models.py, .planning/phases/15-custom-kanban-statuses/15-CONTEXT.md</read_first>
  <action>
Audit `backend/app/routers/performance.py` to ensure KPI and existing performance aggregates use Phase 15 custom status completion semantics.

Required:
- Completion logic references `is_done`.
- KPI aggregate logic does not use `TaskStatus.done`.
- Active task logic treats tasks in `is_done` statuses as inactive.
  </action>
  <acceptance_criteria>
    - `grep "is_done" backend/app/routers/performance.py` returns a match
    - `grep "TaskStatus.done" backend/app/routers/performance.py` returns no matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify UI-SPEC contract in page</name>
  <files>frontend/src/routes/performance/+page.svelte</files>
  <read_first>frontend/src/routes/performance/+page.svelte, .planning/phases/16-advanced-kpi-dashboard/16-UI-SPEC.md</read_first>
  <action>
Audit `/performance` for required UI-SPEC elements:
- Tabs: Overview, Sprint, Quality, Members, Settings
- `Needs attention`
- `Export KPI CSV`
- `Save weights`
- `Reset defaults`
- `Total weight must equal 100%.`
- `Not enough data`

Add missing copy or controls if any are absent.
  </action>
  <acceptance_criteria>
    - `grep "Overview" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "Needs attention" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "Export KPI CSV" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "Save weights" frontend/src/routes/performance/+page.svelte` or `grep "Save weights" frontend/src/lib/components/performance/KpiWeightSettings.svelte` returns a match
    - `grep "Not enough data" frontend/src/routes/performance/+page.svelte` or `grep "Not enough data" frontend/src/lib/components/performance/KpiChartPanel.svelte` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Run full verification commands</name>
  <files>backend/app/routers/performance.py, frontend/src/routes/performance/+page.svelte</files>
  <read_first>frontend/package.json, backend/app/routers/performance.py</read_first>
  <action>
Run:
- `python -m compileall backend/app`
- `cd frontend && bun run check`

If either command fails, fix the relevant issue and rerun until both pass.
  </action>
  <acceptance_criteria>
    - `python -m compileall backend/app` exits 0
    - `cd frontend && bun run check` exits 0
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 5: Manual supervisor UAT</name>
  <files>.planning/phases/16-advanced-kpi-dashboard/16-VALIDATION.md</files>
  <read_first>.planning/phases/16-advanced-kpi-dashboard/16-VALIDATION.md, .planning/phases/16-advanced-kpi-dashboard/16-UI-SPEC.md</read_first>
  <action>
Run the manual UAT checklist from `16-VALIDATION.md`.

Minimum checks:
- Open `/performance`
- Confirm member KPI scorecards appear first
- Switch every tab
- Apply one Sprint tab filter
- Open one chart drill-down
- Export one chart CSV
- Open Settings and validate that weights not totaling 100 cannot be saved

Record any gaps in the eventual SUMMARY.md.
  </action>
  <acceptance_criteria>
    - SUMMARY.md for this plan includes a `Manual UAT` section
    - `Manual UAT` section lists pass/fail for scorecards, tabs, filters, drill-down, CSV export, and KPI settings
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification process | Confirms sensitive KPI data remains scoped and UI meets contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-09 | Information Disclosure | Final KPI implementation | mitigate | Explicit grep and UAT checks for sub-team scoped API usage and custom status completion |
</threat_model>

<verification>
- KPI requirements covered
- UI-SPEC covered
- Custom status completion used
- Backend and frontend checks pass
- Manual UAT recorded
</verification>

<success_criteria>
- Phase 16 is ready for verification with no known plan-level gaps
- All KPI charts, scoring, filters, drill-down, exports, and settings are implemented
</success_criteria>

<output>
After completion, create `.planning/phases/16-advanced-kpi-dashboard/16-05-SUMMARY.md`.
</output>
