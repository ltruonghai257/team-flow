---
phase: "16-advanced-kpi-dashboard"
plan: 4
type: execute
wave: 4
depends_on: ["16-03"]
files_modified:
  - "frontend/src/routes/performance/+page.svelte"
  - "frontend/src/routes/performance/[id]/+page.svelte"
autonomous: true
requirements: ["KPI-01", "KPI-02", "KPI-03", "KPI-04", "KPI-05"]
must_haves:
  truths:
    - "/performance is a tabbed people-first KPI dashboard"
    - "Dashboard exposes all required KPI charts, filters, drill-down, export, and settings"
  artifacts:
    - path: "frontend/src/routes/performance/+page.svelte"
      provides: "Tabbed KPI dashboard"
      contains: "Overview"
  key_links:
    - from: "frontend/src/routes/performance/+page.svelte"
      to: "frontend/src/lib/components/performance/KpiWeightSettings.svelte"
      via: "Settings tab"
      pattern: "KpiWeightSettings"
---

<objective>
Assemble the Phase 16 tabbed KPI dashboard on `/performance`.

Purpose: Deliver the supervisor-facing UI promised by the context and UI-SPEC.
Output: `/performance` loads KPI data, renders tabs, scorecards, charts, filters, drill-downs, exports, and settings.
</objective>

<execution_context>
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-advanced-kpi-dashboard/16-CONTEXT.md
@.planning/phases/16-advanced-kpi-dashboard/16-UI-SPEC.md
@.planning/phases/16-advanced-kpi-dashboard/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert performance page to tab shell</name>
  <files>frontend/src/routes/performance/+page.svelte</files>
  <read_first>frontend/src/routes/performance/+page.svelte, frontend/src/lib/components/performance/KpiTabs.svelte, .planning/phases/16-advanced-kpi-dashboard/16-UI-SPEC.md</read_first>
  <action>
Refactor `frontend/src/routes/performance/+page.svelte` into a tabbed dashboard shell.

Required tabs:
- `overview`
- `sprint`
- `quality`
- `members`
- `settings`

Use labels:
- `Overview`
- `Sprint`
- `Quality`
- `Members`
- `Settings`

Keep page title exactly `Performance Dashboard`.
Set subtitle exactly `Track member KPI health, sprint delivery, and quality trends.`

Add header controls:
- sub-team scope label using selected sub-team from existing store if available
- refresh timestamp
- primary button text `Export KPI CSV`
  </action>
  <acceptance_criteria>
    - `grep "Performance Dashboard" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "Track member KPI health, sprint delivery, and quality trends." frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "Overview" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "Sprint" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "Settings" frontend/src/routes/performance/+page.svelte` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement Overview tab</name>
  <files>frontend/src/routes/performance/+page.svelte</files>
  <read_first>frontend/src/routes/performance/+page.svelte, frontend/src/lib/components/performance/KpiScoreCard.svelte, frontend/src/lib/api.ts</read_first>
  <action>
In the Overview tab:
- Load data from `performance.kpiOverview()`.
- Render member scorecards before chart/table content.
- Render a section titled `Needs attention`.
- Show empty state text exactly `No members need attention for the selected period.` when no exception items exist.
- Render summary tiles for average score, active tasks, completed tasks, average cycle time, and defects.
- Use `KpiScoreCard` for member scorecards.
  </action>
  <acceptance_criteria>
    - `grep "kpiOverview" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "Needs attention" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "No members need attention for the selected period." frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "KpiScoreCard" frontend/src/routes/performance/+page.svelte` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement Sprint, Quality, and Members tabs</name>
  <files>frontend/src/routes/performance/+page.svelte</files>
  <read_first>frontend/src/routes/performance/+page.svelte, frontend/src/lib/components/performance/KpiChartPanel.svelte, frontend/src/lib/components/performance/KpiFilters.svelte</read_first>
  <action>
Implement:

Sprint tab:
- Load `performance.kpiSprint(sprintFilters)`.
- Render velocity and burndown chart panels.
- Include sprint/project/member/task type/date filters.

Quality tab:
- Load `performance.kpiQuality(qualityFilters)`.
- Render bugs reported vs resolved and MTTR by member chart panels.
- Include project/member/task type/date filters.

Members tab:
- Load `performance.kpiMembers(memberFilters)`.
- Render throughput by member/type and cycle time by task type chart panels.
- Include project/member/task type/date filters.

All chart panels must include `Export CSV`.
  </action>
  <acceptance_criteria>
    - `grep "kpiSprint" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "kpiQuality" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "kpiMembers" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "Velocity" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "Burndown" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "MTTR" frontend/src/routes/performance/+page.svelte` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Implement drill-down and CSV export interactions</name>
  <files>frontend/src/routes/performance/+page.svelte</files>
  <read_first>frontend/src/routes/performance/+page.svelte, frontend/src/lib/components/performance/KpiDrilldown.svelte, frontend/src/lib/components/performance/csv.ts</read_first>
  <action>
Add drill-down behavior:
- Store `drilldownOpen`, `drilldownTitle`, `drilldownFilters`, and `drilldownTasks` state.
- On chart point/bar click, call `performance.kpiDrilldown(...)`.
- Render `KpiDrilldown` with task rows.
- Preserve selected tab and filters when drill-down closes.

Add CSV behavior:
- Use `downloadCsv` for chart panel exports.
- Use `downloadCsv` for drill-down task export.
- Top-level `Export KPI CSV` should export the currently selected tab data.
  </action>
  <acceptance_criteria>
    - `grep "KpiDrilldown" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "kpiDrilldown" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "downloadCsv" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "Export KPI CSV" frontend/src/routes/performance/+page.svelte` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Implement Settings tab</name>
  <files>frontend/src/routes/performance/+page.svelte</files>
  <read_first>frontend/src/routes/performance/+page.svelte, frontend/src/lib/components/performance/KpiWeightSettings.svelte, frontend/src/lib/api.ts</read_first>
  <action>
In the Settings tab:
- Load `performance.kpiWeights()`.
- Render `KpiWeightSettings`.
- On save, call `performance.updateKpiWeights(weights)`.
- On successful save, show toast text exactly `KPI weights updated.`
- On reset, set defaults: workload 20, velocity 25, cycle time 20, on-time 20, defects 15.
- If backend returns validation error, show text containing `Total weight must equal 100%.`
  </action>
  <acceptance_criteria>
    - `grep "kpiWeights" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "updateKpiWeights" frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "KPI weights updated." frontend/src/routes/performance/+page.svelte` returns a match
    - `grep "Total weight must equal 100%." frontend/src/routes/performance/+page.svelte` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Refresh member detail page for KPI vocabulary</name>
  <files>frontend/src/routes/performance/[id]/+page.svelte</files>
  <read_first>frontend/src/routes/performance/[id]/+page.svelte, .planning/phases/16-advanced-kpi-dashboard/16-UI-SPEC.md</read_first>
  <action>
Update `frontend/src/routes/performance/[id]/+page.svelte` copy and layout so it remains compatible with the Phase 16 dashboard.

Required copy changes:
- Keep `Back to Dashboard`
- Add or preserve member KPI score if the API returns `kpi_score`
- Replace `TRENDING STABLE` static copy with dynamic trend text if available, falling back to `Trend unavailable`
- Keep recent completed tasks visible
  </action>
  <acceptance_criteria>
    - `grep "Back to Dashboard" 'frontend/src/routes/performance/[id]/+page.svelte'` returns a match
    - `grep "kpi_score" 'frontend/src/routes/performance/[id]/+page.svelte'` returns a match or `grep "KPI" 'frontend/src/routes/performance/[id]/+page.svelte'` returns a match
    - `grep "Trend unavailable" 'frontend/src/routes/performance/[id]/+page.svelte'` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Frontend verification</name>
  <files>frontend/src/routes/performance/+page.svelte, frontend/src/routes/performance/[id]/+page.svelte</files>
  <read_first>frontend/package.json</read_first>
  <action>
Run frontend verification:
`cd frontend && bun run check`
  </action>
  <acceptance_criteria>
    - `cd frontend && bun run check` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supervisor browser session | Displays sensitive KPI data scoped by backend auth |
| Client filters | User-controlled filter values are sent to API endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Information Disclosure | Dashboard UI | mitigate | Use existing authenticated API client only; never query unscoped raw task endpoints for KPI data |
| T-16-08 | Repudiation | KPI weight changes | accept | Phase 16 does not include audit logs; settings changes are authenticated but not audited |
</threat_model>

<verification>
- `/performance` contains all required tabs
- Overview is people-first and exception-aware
- Sprint, Quality, and Members tabs render KPI charts
- Settings tab saves weights
- CSV export and drill-down interactions exist
- `cd frontend && bun run check` exits 0
</verification>

<success_criteria>
- Supervisor can manage member performance and KPIs from `/performance`
- UI-SPEC contract is implemented
- KPI-01 through KPI-05 are visible in the dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/16-advanced-kpi-dashboard/16-04-SUMMARY.md`.
</output>

