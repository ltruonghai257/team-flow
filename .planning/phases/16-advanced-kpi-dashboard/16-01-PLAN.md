---
phase: "16-advanced-kpi-dashboard"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - "backend/app/models.py"
  - "backend/app/schemas.py"
  - "backend/app/routers/performance.py"
  - "backend/alembic/versions/"
autonomous: true
requirements: ["KPI-01", "KPI-02", "KPI-03", "KPI-04", "KPI-05"]
must_haves:
  truths:
    - "KPI weight settings are persisted per sub-team"
    - "KPI response schemas expose scorecards, chart series, filters, drill-down rows, and weights"
    - "Plan includes blocking prerequisite that Phase 15 custom status completion semantics exist before KPI endpoint execution"
  artifacts:
    - path: "backend/app/models.py"
      provides: "KPIWeightSettings model"
      contains: "class KPIWeightSettings(Base):"
    - path: "backend/app/schemas.py"
      provides: "KPI response and settings schemas"
      contains: "class KPIWeightSettingsOut(BaseModel):"
  key_links:
    - from: "backend/app/routers/performance.py"
      to: "backend/app/models.py"
      via: "KPIWeightSettings"
      pattern: "KPIWeightSettings"
---

<objective>
Add backend persistence and typed contracts for the Advanced KPI Dashboard.

Purpose: Create the durable settings and API schemas needed by later KPI metric endpoints and frontend tabs.
Output: Model, migration, schemas, and basic weight settings endpoints.
</objective>

<execution_context>
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-advanced-kpi-dashboard/16-CONTEXT.md
@.planning/phases/16-advanced-kpi-dashboard/16-UI-SPEC.md
@.planning/phases/16-advanced-kpi-dashboard/16-RESEARCH.md
@.planning/phases/16-advanced-kpi-dashboard/16-PATTERNS.md
</context>

<tasks>

<task type="auto" gate="blocking">
  <name>Task 1: Confirm execution prerequisites</name>
  <files>backend/app/models.py</files>
  <read_first>backend/app/models.py, .planning/phases/15-custom-kanban-statuses/15-CONTEXT.md, .planning/phases/14-sprint-model/14-CONTEXT.md</read_first>
  <action>
Before editing Phase 16 code, confirm Phase 14 and Phase 15 implementation prerequisites exist in `backend/app/models.py`.

Required Phase 14 structures:
- `class Sprint(Base):`
- `class SprintStatus(str, enum.Enum):`
- `sprint_id = Column(Integer, ForeignKey("sprints.id"), nullable=True)` on `Task`

Required Phase 15 structures:
- `custom_status_id` on `Task`
- a DB model for custom statuses with an `is_done` boolean field

If Phase 15 structures are missing, stop this execution plan and report: `Phase 16 execution is blocked until Phase 15 custom status is implemented.`
  </action>
  <acceptance_criteria>
    - `grep "class Sprint(Base):" backend/app/models.py` returns a match
    - `grep "sprint_id = Column" backend/app/models.py` returns a match
    - `grep "custom_status_id" backend/app/models.py` returns a match before continuing beyond this task
    - `grep "is_done" backend/app/models.py` returns a match before continuing beyond this task
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add KPI weight settings model</name>
  <files>backend/app/models.py</files>
  <read_first>backend/app/models.py</read_first>
  <action>
Add a new SQLAlchemy model to `backend/app/models.py` named `KPIWeightSettings` with `__tablename__ = "kpi_weight_settings"`.

Add these columns:
- `id = Column(Integer, primary_key=True, index=True)`
- `sub_team_id = Column(Integer, ForeignKey("sub_teams.id"), nullable=True, unique=True, index=True)`
- `workload_weight = Column(Integer, nullable=False, default=20)`
- `velocity_weight = Column(Integer, nullable=False, default=25)`
- `cycle_time_weight = Column(Integer, nullable=False, default=20)`
- `on_time_weight = Column(Integer, nullable=False, default=20)`
- `defect_weight = Column(Integer, nullable=False, default=15)`
- `updated_at = Column(DateTime, default=lambda: datetime.now(timezone.utc).replace(tzinfo=None), onupdate=lambda: datetime.now(timezone.utc).replace(tzinfo=None))`

Add relationship:
- `sub_team = relationship("SubTeam", foreign_keys=[sub_team_id])`

Default weights must sum to 100.
  </action>
  <acceptance_criteria>
    - `grep "class KPIWeightSettings(Base):" backend/app/models.py` returns a match
    - `grep "__tablename__ = \"kpi_weight_settings\"" backend/app/models.py` returns a match
    - `grep "workload_weight = Column(Integer, nullable=False, default=20)" backend/app/models.py` returns a match
    - `grep "defect_weight = Column(Integer, nullable=False, default=15)" backend/app/models.py` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add KPI schemas</name>
  <files>backend/app/schemas.py</files>
  <read_first>backend/app/schemas.py, .planning/phases/16-advanced-kpi-dashboard/16-CONTEXT.md</read_first>
  <action>
Add Pydantic schemas to `backend/app/schemas.py` after the existing performance schemas.

Create these classes with exact names:
- `KPIWeightSettingsOut`
- `KPIWeightSettingsUpdate`
- `KPIFilterOptions`
- `KPIReason`
- `KPIScoreBreakdown`
- `KPIMemberScorecard`
- `KPIChartPoint`
- `KPIChartSeries`
- `KPIOverviewResponse`
- `KPISprintResponse`
- `KPIQualityResponse`
- `KPIMembersResponse`
- `KPIDrilldownTask`
- `KPIDrilldownResponse`

`KPIWeightSettingsUpdate` must validate that the five weights total exactly 100 when all are provided. Use the existing Pydantic v2 style in this file.

`KPIMemberScorecard` must include:
- `user_id: int`
- `full_name: str`
- `avatar_url: Optional[str]`
- `kpi_score: int`
- `trend: str`
- `reasons: List[KPIReason]`
- `breakdown: KPIScoreBreakdown`
  </action>
  <acceptance_criteria>
    - `grep "class KPIWeightSettingsOut(BaseModel):" backend/app/schemas.py` returns a match
    - `grep "class KPIMemberScorecard(BaseModel):" backend/app/schemas.py` returns a match
    - `grep "class KPIDrilldownTask(BaseModel):" backend/app/schemas.py` returns a match
    - `grep "kpi_score: int" backend/app/schemas.py` returns a match
    - `grep "Total weight must equal 100" backend/app/schemas.py` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add KPI weight endpoints</name>
  <files>backend/app/routers/performance.py</files>
  <read_first>backend/app/routers/performance.py, backend/app/models.py, backend/app/schemas.py</read_first>
  <action>
Update `backend/app/routers/performance.py` imports to include `KPIWeightSettings` from `app.models` and `KPIWeightSettingsOut`, `KPIWeightSettingsUpdate` from `app.schemas`.

Add helper function:
`async def _get_or_create_kpi_weights(db: AsyncSession, sub_team: Optional[SubTeam]) -> KPIWeightSettings:`

Behavior:
- Use `sub_team.id` when sub_team is provided.
- Use `None` for admin all-teams scope.
- Query for existing `KPIWeightSettings.sub_team_id == target_sub_team_id`.
- If none exists, create default `KPIWeightSettings(sub_team_id=target_sub_team_id)` and `await db.flush()`.

Add endpoints:
- `@router.get("/kpi/weights", response_model=KPIWeightSettingsOut)`
- `@router.patch("/kpi/weights", response_model=KPIWeightSettingsOut)`

The PATCH endpoint must update only provided fields via `payload.model_dump(exclude_unset=True)`.
  </action>
  <acceptance_criteria>
    - `grep "@router.get(\"/kpi/weights\"" backend/app/routers/performance.py` returns a match
    - `grep "@router.patch(\"/kpi/weights\"" backend/app/routers/performance.py` returns a match
    - `grep "def _get_or_create_kpi_weights" backend/app/routers/performance.py` returns a match
    - `grep "model_dump(exclude_unset=True)" backend/app/routers/performance.py` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Generate Alembic migration for KPI weights</name>
  <files>backend/alembic/versions/</files>
  <read_first>backend/app/models.py, backend/alembic/versions/</read_first>
  <action>
Create a new Alembic migration named `add_kpi_weight_settings`.

The migration must:
- Create table `kpi_weight_settings`
- Include columns `id`, `sub_team_id`, `workload_weight`, `velocity_weight`, `cycle_time_weight`, `on_time_weight`, `defect_weight`, `updated_at`
- Add a foreign key from `sub_team_id` to `sub_teams.id`
- Add a unique constraint or unique index for `sub_team_id`
- Drop the table in downgrade

Use the project's existing Alembic style in `backend/alembic/versions/`.
  </action>
  <acceptance_criteria>
    - `ls backend/alembic/versions | grep "add_kpi_weight_settings"` returns a match
    - Migration file contains `create_table('kpi_weight_settings'`
    - Migration file contains `sa.Column('workload_weight'`
    - Migration file contains `sa.ForeignKeyConstraint(['sub_team_id'], ['sub_teams.id']`
  </acceptance_criteria>
</task>

<task type="auto" gate="blocking">
  <name>Task 6: [BLOCKING] Apply schema migration</name>
  <files>backend/alembic/</files>
  <read_first>backend/alembic/versions/, backend/app/models.py</read_first>
  <action>
Run the schema migration after the model and migration file are complete:
`cd backend && .venv/bin/alembic upgrade head`

If `.venv/bin/alembic` does not exist, use the repository's documented backend environment to run `alembic upgrade head`.
  </action>
  <acceptance_criteria>
    - Command `cd backend && .venv/bin/alembic upgrade head` completes with exit code 0, or the equivalent project backend Alembic command completes with exit code 0
    - `cd backend && .venv/bin/alembic current` shows the new KPI migration as current head
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Backend syntax verification</name>
  <files>backend/app/models.py, backend/app/schemas.py, backend/app/routers/performance.py</files>
  <read_first>backend/app/models.py, backend/app/schemas.py, backend/app/routers/performance.py</read_first>
  <action>
Run Python compile checks for modified backend app files:
`python -m compileall backend/app`
  </action>
  <acceptance_criteria>
    - `python -m compileall backend/app` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supervisor settings API | Authenticated supervisors/admins can change KPI weighting policy |
| Database migration | Adds persistent settings table used by future score calculations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | KPI weight settings | mitigate | Require `require_supervisor`, validate total weight equals 100, scope settings by `get_sub_team` |
| T-16-02 | Information Disclosure | KPI settings lookup | mitigate | Use existing `X-SubTeam-ID` scoping and do not return settings for unauthorized sub-teams |
</threat_model>

<verification>
- `backend/app/models.py` contains `KPIWeightSettings`
- `backend/app/schemas.py` contains KPI response and settings schemas
- `backend/app/routers/performance.py` exposes `/kpi/weights` GET/PATCH
- Alembic migration exists and applies successfully
- `python -m compileall backend/app` exits 0
</verification>

<success_criteria>
- KPI weight settings can be read and updated per sub-team
- Default weights sum to 100
- Backend schemas are ready for all KPI chart endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/16-advanced-kpi-dashboard/16-01-SUMMARY.md`.
</output>

