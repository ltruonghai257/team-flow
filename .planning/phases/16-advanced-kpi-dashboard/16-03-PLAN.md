---
phase: "16-advanced-kpi-dashboard"
plan: 3
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - "frontend/src/lib/api.ts"
  - "frontend/src/lib/components/performance/KpiTabs.svelte"
  - "frontend/src/lib/components/performance/KpiScoreCard.svelte"
  - "frontend/src/lib/components/performance/KpiChartPanel.svelte"
  - "frontend/src/lib/components/performance/KpiFilters.svelte"
  - "frontend/src/lib/components/performance/KpiDrilldown.svelte"
  - "frontend/src/lib/components/performance/KpiWeightSettings.svelte"
  - "frontend/src/lib/components/performance/csv.ts"
autonomous: true
requirements: ["KPI-01", "KPI-02", "KPI-03", "KPI-04", "KPI-05"]
must_haves:
  truths:
    - "Frontend has reusable KPI API methods and dashboard components"
    - "Components follow 16-UI-SPEC.md visual and interaction contracts"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "KPI API client methods"
      contains: "kpiOverview"
    - path: "frontend/src/lib/components/performance/KpiScoreCard.svelte"
      provides: "Member KPI scorecard"
      contains: "kpi_score"
  key_links:
    - from: "frontend/src/routes/performance/+page.svelte"
      to: "frontend/src/lib/components/performance/KpiScoreCard.svelte"
      via: "Overview scorecards"
      pattern: "KpiScoreCard"
---

<objective>
Create the frontend API helpers and reusable KPI dashboard components.

Purpose: Keep `/performance` maintainable by moving Phase 16 tabs, scorecards, filters, charts, drill-down, settings, and CSV export into small components.
Output: New performance component library and API client methods.
</objective>

<execution_context>
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/workflows/execute-plan.md
@/Users/haila/Desktop/personal-app/CascadeProjects/windsurf-project/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-advanced-kpi-dashboard/16-CONTEXT.md
@.planning/phases/16-advanced-kpi-dashboard/16-UI-SPEC.md
@.planning/phases/16-advanced-kpi-dashboard/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add KPI API client methods</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>frontend/src/lib/api.ts</read_first>
  <action>
Extend the existing `performance` export in `frontend/src/lib/api.ts`.

Add helper function near the performance export:
`function qs(params?: Record<string, string | number | boolean | null | undefined>): string`

It must omit `null`, `undefined`, and empty string values.

Add methods:
- `kpiOverview: (params?: Record<string, string | number | boolean | null | undefined>) => request(\`/performance/kpi/overview${qs(params)}\`)`
- `kpiSprint: (params?: Record<string, string | number | boolean | null | undefined>) => request(\`/performance/kpi/sprint${qs(params)}\`)`
- `kpiQuality: (params?: Record<string, string | number | boolean | null | undefined>) => request(\`/performance/kpi/quality${qs(params)}\`)`
- `kpiMembers: (params?: Record<string, string | number | boolean | null | undefined>) => request(\`/performance/kpi/members${qs(params)}\`)`
- `kpiDrilldown: (params?: Record<string, string | number | boolean | null | undefined>) => request(\`/performance/kpi/drilldown${qs(params)}\`)`
- `kpiWeights: () => request('/performance/kpi/weights')`
- `updateKpiWeights: (data: object) => request('/performance/kpi/weights', { method: 'PATCH', body: JSON.stringify(data) })`
  </action>
  <acceptance_criteria>
    - `grep "kpiOverview" frontend/src/lib/api.ts` returns a match
    - `grep "kpiSprint" frontend/src/lib/api.ts` returns a match
    - `grep "kpiQuality" frontend/src/lib/api.ts` returns a match
    - `grep "updateKpiWeights" frontend/src/lib/api.ts` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add CSV export utility</name>
  <files>frontend/src/lib/components/performance/csv.ts</files>
  <read_first>frontend/src/lib/api.ts</read_first>
  <action>
Create `frontend/src/lib/components/performance/csv.ts`.

Export function:
`export function downloadCsv(filename: string, rows: Record<string, unknown>[]): void`

Behavior:
- If rows is empty, create a CSV with one column `message` and one row `No data`.
- Escape quotes by doubling them.
- Wrap every cell in double quotes.
- Create a Blob with `type: 'text/csv;charset=utf-8;'`.
- Create a temporary anchor, set `download`, click it, then revoke the object URL.
  </action>
  <acceptance_criteria>
    - `test -f frontend/src/lib/components/performance/csv.ts` exits 0
    - `grep "downloadCsv" frontend/src/lib/components/performance/csv.ts` returns a match
    - `grep "text/csv" frontend/src/lib/components/performance/csv.ts` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add KPI tabs and filter components</name>
  <files>frontend/src/lib/components/performance/KpiTabs.svelte, frontend/src/lib/components/performance/KpiFilters.svelte</files>
  <read_first>frontend/src/routes/performance/+page.svelte, .planning/phases/16-advanced-kpi-dashboard/16-UI-SPEC.md</read_first>
  <action>
Create `KpiTabs.svelte` with props:
- `active: string`
- `tabs: { id: string; label: string; icon?: any }[]`
- `onSelect: (id: string) => void`

It must render horizontally scrollable tab buttons, use `aria-current={active === tab.id ? 'page' : undefined}`, and visually highlight the active tab using `bg-primary-600/20 text-primary-400`.

Create `KpiFilters.svelte` with props:
- `filters: any`
- `options: any`
- `mode: 'sprint' | 'quality' | 'members'`
- `onChange: (filters: any) => void`

It must support controls for sprint, project, member, task type, start date, end date, and a clear/reset button. Controls can be hidden when options are missing, but labels must be present for rendered inputs.
  </action>
  <acceptance_criteria>
    - `test -f frontend/src/lib/components/performance/KpiTabs.svelte` exits 0
    - `test -f frontend/src/lib/components/performance/KpiFilters.svelte` exits 0
    - `grep "aria-current" frontend/src/lib/components/performance/KpiTabs.svelte` returns a match
    - `grep "type=\"date\"" frontend/src/lib/components/performance/KpiFilters.svelte` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add KPI scorecard and chart panel components</name>
  <files>frontend/src/lib/components/performance/KpiScoreCard.svelte, frontend/src/lib/components/performance/KpiChartPanel.svelte</files>
  <read_first>frontend/src/routes/performance/+page.svelte, .planning/phases/16-advanced-kpi-dashboard/16-UI-SPEC.md</read_first>
  <action>
Create `KpiScoreCard.svelte` with prop `member: any` and optional `onDrilldown: (member: any) => void`.

It must render:
- member avatar/initials
- `member.kpi_score`
- `member.trend`
- up to 3 `member.reasons`
- breakdown values for workload, velocity, cycle time, on-time, defects
- a `View detail` link to `/performance/{member.user_id}`

Create `KpiChartPanel.svelte` with props:
- `title: string`
- `subtitle: string`
- `series: any[]`
- `emptyHeading?: string`
- `emptyBody?: string`
- `onExport?: () => void`
- `onPointClick?: (point: any) => void`

It must render heading, subtitle, `Export CSV` button when `onExport` exists, empty state copy from UI-SPEC, and an inline SVG area/bar fallback for simple series data.
  </action>
  <acceptance_criteria>
    - `grep "kpi_score" frontend/src/lib/components/performance/KpiScoreCard.svelte` returns a match
    - `grep "View detail" frontend/src/lib/components/performance/KpiScoreCard.svelte` returns a match
    - `grep "Export CSV" frontend/src/lib/components/performance/KpiChartPanel.svelte` returns a match
    - `grep "<svg" frontend/src/lib/components/performance/KpiChartPanel.svelte` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Add drill-down and KPI settings components</name>
  <files>frontend/src/lib/components/performance/KpiDrilldown.svelte, frontend/src/lib/components/performance/KpiWeightSettings.svelte</files>
  <read_first>.planning/phases/16-advanced-kpi-dashboard/16-UI-SPEC.md, frontend/src/routes/performance/+page.svelte</read_first>
  <action>
Create `KpiDrilldown.svelte` with props:
- `open: boolean`
- `title: string`
- `filters: any`
- `tasks: any[]`
- `onClose: () => void`
- `onExport: () => void`

It must render a modal or side panel with applied filters, a task table, `Export CSV`, and close button.

Create `KpiWeightSettings.svelte` with props:
- `weights: any`
- `saving: boolean`
- `onSave: (weights: any) => void`
- `onReset: () => void`

It must render editable numeric inputs for `workload_weight`, `velocity_weight`, `cycle_time_weight`, `on_time_weight`, and `defect_weight`; show total weight; block save visually when total is not 100; include `Save weights` and `Reset defaults`.
  </action>
  <acceptance_criteria>
    - `grep "Export CSV" frontend/src/lib/components/performance/KpiDrilldown.svelte` returns a match
    - `grep "Save weights" frontend/src/lib/components/performance/KpiWeightSettings.svelte` returns a match
    - `grep "Total weight must equal 100" frontend/src/lib/components/performance/KpiWeightSettings.svelte` returns a match
    - `grep "Reset defaults" frontend/src/lib/components/performance/KpiWeightSettings.svelte` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Frontend component verification</name>
  <files>frontend/src/lib/api.ts, frontend/src/lib/components/performance/</files>
  <read_first>frontend/package.json</read_first>
  <action>
Run frontend type verification using the project preference for Bun:
`cd frontend && bun run check`
  </action>
  <acceptance_criteria>
    - `cd frontend && bun run check` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser UI | Displays sensitive member KPI data to authorized supervisors/admins |
| CSV export | Moves KPI data out of the app into downloaded files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Information Disclosure | CSV export | mitigate | Export only data returned by already-authorized API calls; do not fetch bypass endpoints |
| T-16-06 | Spoofing | KPI score visuals | mitigate | Display reason labels and breakdown, not just color or score |
</threat_model>

<verification>
- Performance API client exposes all KPI methods
- Component files exist and compile
- `cd frontend && bun run check` exits 0
</verification>

<success_criteria>
- Reusable frontend components are ready for the Phase 16 dashboard assembly
- Components meet UI-SPEC copy, accessibility, filter, export, and drill-down contracts
</success_criteria>

<output>
After completion, create `.planning/phases/16-advanced-kpi-dashboard/16-03-SUMMARY.md`.
</output>

